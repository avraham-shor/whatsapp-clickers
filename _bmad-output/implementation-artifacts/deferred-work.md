# Deferred Work

Items deferred from reviews and workflows. Revisit when the referenced trigger arrives.

## Deferred from: code review of 1-1-project-scaffold-ci-and-deployed-walking-skeleton (2026-07-13)

- ~~Spacing-scale enforcement ("no values between stops", DESIGN.md)~~ **closed in 1.3 (2026-07-15): disposition "convention + review".** Mechanical enforcement via `--spacing: initial` stays rejected — it breaks shadcn component defaults (e.g. button `h-9`→36px), and the shadcn inheritance contract (DESIGN.md) says component internals keep their defaults. The binding convention, applied throughout 1.3's builder UI: hand-written classes use only on-scale spacing stops (4/8/12/16/24/32/48/64px ↔ Tailwind 1/2/3/4/6/8/12/16) for padding/margin/gap; buttons are always `h-10` (40px), never shadcn's default `h-9`. Enforcement is code-review attention, not tooling.
- Fail-fast config accepts placeholder secrets — `require()` in `server/internal/config/config.go` validates presence only, so dummy WhatsApp/Anthropic values pass in production (the runbook currently instructs exactly that for unconsumed vars). Add strength/placeholder validation when the vars become live: ~~`SESSION_SECRET` at Story 1.2 (session signing)~~ **done in 1.2** (≥32 chars + `change-me*` rejected, boot fails fast); WhatsApp secrets remain for Story 2.1 (webhook signature verification).

## Deferred from: code review of 1-3-create-a-game-and-author-questions (2026-07-15)

- Concurrent `CreateQuestion` can assign duplicate `position` values — `CreateQuestion` computes `position = COALESCE(MAX(position),0)+1` inside a single `INSERT ... SELECT`, but under READ COMMITTED two concurrent creates for the same game can both read the same MAX before either commits, and the migration deliberately omits `UNIQUE (game_id, position)` (the story chose transactional reorder over fighting a deferred constraint). Result: two questions briefly share a position, order falling to the `created_at` tiebreaker. Self-healing — the next `ReorderQuestions` rewrites positions to a clean 1..N. Low severity for a single-organizer builder. Revisit trigger: if multi-tab / concurrent authoring or an ordering-sensitive consumer of `position` appears, add a serialization guard (advisory lock or `SELECT ... FOR UPDATE` on the game) or a `UNIQUE (game_id, position) DEFERRABLE` constraint with retry.

## Deferred from: code review of 1-4-configure-scoring-rules (2026-07-16)

- Scoring save error handling / unconditional card render — `ScoringEditor`'s mutation has no `onError`, so any non-2xx collapses into the single generic `strings.scoring.errorServer` ("try again in a moment"), and the card renders regardless of `game.state`. If the game were non-draft (409 `GAME_NOT_EDITABLE`) or deleted (404 `GAME_NOT_FOUND`), the user would see an un-recoverable retry loop with nothing invalidating the query to reflect reality. **Not reachable today**: every game is `draft` until game-start (story 2.3), and no game-delete exists — 404/409 cannot occur yet. Revisit trigger: when story 2.3 lets a game leave `draft`, add `onError` handling that distinguishes 409/404 (surface a "reload"/"not editable" state and invalidate `['games', gameId]`), and gate or disable the card when `game.state !== 'draft'`.
- Draft-only scoring guard is check-then-write (TOCTOU) — `UpdateGameScoring`'s `UPDATE games … WHERE id = $1 AND organizer_id = $2` omits `state = 'draft'`, so a draft→lobby transition landing between the `requireDraftGame` read and the write would still persist scoring to a running game. ~~Pre-existing pattern, not introduced here: every existing question mutation (`UpdateQuestion`/`DeleteQuestion`/`UpdateQuestionPosition`) relies on the same `requireDraftGame` check-then-write with no SQL `state` predicate~~ **question-mutation half closed in 3.1's code review (2026-08-04)**: `CreateQuestion`/`UpdateQuestion`/`DeleteQuestion`/`UpdateQuestionPosition` (and the `ReorderQuestions` transaction's up-front check) now all guard on `state = 'draft'` in the `WHERE` clause. `UpdateGameScoring` itself was out of that patch's scope and remains unguarded. Revisit trigger: the next time `UpdateGameScoring` is touched — add the same `AND state = 'draft'` guard, mapping a lost race to `ErrNotFound`/404, matching the pattern now established for questions. [server/internal/store/games.go, server/internal/store/queries/games.sql]

## Deferred from: code review of 1-5-question-bank-package-import (2026-07-16)

- Package import inherits the two already-tracked concurrency postures above — extend the same fix to it when 2.3 lands: (1) **position race** — `ImportPackageQuestions` appends with `COALESCE(MAX(position),0)+row_number()` and no txn / no `UNIQUE (game_id, position)`, so two overlapping imports (or an import racing a `CreateQuestion`) both read the same MAX and duplicate positions (self-healing via the next reorder); same class as the 1.3 `CreateQuestion` item. (2) **draft TOCTOU** — the `INSERT … SELECT` WHERE scopes only on `g.id`/`g.organizer_id` (no `state = 'draft'`), so a draft→lobby transition or a delete between `requireDraftGame` and the write is not observed (append to a non-draft game, or a bogus 201 with empty items); same class as the 1.4 draft-only guard item. Revisit trigger unchanged: at game-start (2.3), decide the posture for ALL draft-only mutations together (import included) — `AND state = 'draft'` in the WHERE and/or a row lock, plus the `UNIQUE (game_id, position)` / serialization guard for positions.
- Import-dialog UX polish — one shared `importPackage.isPending` disables every package card's import button (no per-card affordance showing which import is in flight), and the dialog stays dismissable mid-import (the mutation still resolves → `invalidateQueries` + `onClose` after unmount). Harmless today (one import at a time; React Query settle-after-unmount is a no-op). Revisit if the bank grows to many packages or import latency becomes user-visible: add per-card pending state and block dismiss while a mutation is in flight. [web/src/features/builder/package-import-dialog.tsx]
- Hebrew dual-form count — `questionBank.questionsCount` renders `${count} שאלות` for every n≠1, so n=2 reads "2 שאלות" rather than the grammatical dual "שתי שאלות" (and n=0 could read "אין שאלות"). Pure i18n polish, no path impact. Revisit if Hebrew grammatical number becomes a broader UI concern. [web/src/lib/strings.he.ts]

## Deferred from: code review of story-2.1 (2026-07-20)

- Token-bucket burst can momentarily exceed 80 msg/sec — `rate.NewLimiter(70,70)` (burst=70) can pass ~140 sends in a straddling 1-second window; AC-4's "under 80 msg/sec" governs the sustained rate (70/s) and the limiter config is exactly what the spec instructed. Non-issue at pilot scale (~80-message bursts drain in ~1.2s). Revisit trigger: if outbound throughput requirements grow, or a strict instantaneous ceiling is ever mandated, lower the burst (e.g. `burst = 1`) or reserve headroom for shared inbound traffic. [server/internal/wa/dispatch.go:72]
- ~~No empty recipient/body guard in SendText/Enqueue~~ **closed in 2.2 (2026-08-02, Task 3).** `Dispatcher.Enqueue` now drops an empty `to` or `body` with a WARN before the message reaches the queue, so it can no longer burn 3 attempts, the 1s/2s backoff and 3 rate tokens. The guard sits at `Enqueue` only, not inside `Client.SendText`: the dispatcher is the sole caller of `SendText`, and duplicating the check there would add a second place to keep in sync for no reachable case.
- Retry backoff sleeps inside the worker goroutine — under a Meta outage all 8 workers can park in `contextSleep` (1s then 2s) simultaneously, collapsing queue-drain throughput exactly when the queue is filling, contributing to the queue-full drops the design treats as pathology-only. Pilot non-issue. Revisit trigger: if resilience under sustained Meta degradation becomes a requirement, move retries off the worker (dedicated retry queue / delayed re-enqueue) so backoff never blocks draining. [server/internal/wa/dispatch.go:149]

## Deferred from: code review of story-2.1 (2026-08-02, second round)

- Token-bucket burst can pass ~140 sends across a one-second boundary — re-raised independently by two review layers; already deferred in the 2026-07-20 round and the reasoning stands (AC-4 governs the sustained rate of 70/s; the limiter config is exactly what the spec instructed). Same revisit trigger as the original entry above. The one new sub-point — `dispatch.go`'s comment asserting a hard ceiling the limiter does not provide — was patched as a documentation fix, not deferred. [server/internal/wa/dispatch.go:82]
- ~~Dispatcher shutdown ordering: `Enqueue` after `Run` returns black-holes messages with zero observability~~ **closed in 2.2 (2026-08-02, Task 4).** The dispatcher now runs on its own `context.Background()`-derived context, and the shutdown sequence is: `srv.Shutdown` (all handlers return, so everything they queued is in the queue) → `dispatcher.Drain(5s)` → cancel the dispatcher context → join `Run` with a 2s grace so its unsent-message summary WARN is logged before exit. Verified by a negative control in the 2.2 E2E: with the pre-fix wiring a 40-message burst arriving during shutdown delivered 14 and logged only 10 as dropped (16 vanished silently); with the fix all 40 are delivered. Original text below for the record.<br>Original: SIGTERM cancels the ctx shared by the dispatcher and the HTTP server — SIGTERM cancels the ctx shared by the dispatcher and the HTTP server; `Run`'s workers exit and its drain-and-count snapshot completes, but `srv.Shutdown` keeps serving in-flight webhook requests for up to 15s. From 2.2 those requests call `Enqueue`, which writes into a 1000-slot buffer no goroutine will read again: no drop WARN (the queue is not full), no `dropped_count` (already snapshotted), no error to the caller (fire-and-forget by design). The drain loop's `default` arm is additionally racy against a concurrent `Enqueue`. Unreachable in 2.1 — no production `Enqueue` caller exists. Revisit trigger: **2.2's first `Enqueue` caller** — give the dispatcher its own cancel sequenced after `srv.Shutdown`, await `Run` before process exit, and make post-shutdown `Enqueue` log a drop rather than buffering into the void. [server/cmd/server/main.go:83, server/internal/wa/dispatch.go:99-134]
- The rate limiter has zero executable coverage — every dispatcher test enqueues at most 3 messages, far inside a burst of 70, so `limiter.Wait` never blocks on any exercised path; deleting the call or raising the rate to 100,000 leaves the suite green. AC-4's only quantitative requirement is the only untested one. Deferred rather than patched because a deterministic limiter test requires injecting its clock — which story 2.1 explicitly locked as "overkill" and which the testing standards ("deterministic tests only, no `time.Sleep` assertions") reinforce; reopening it is a spec decision, not a patch. Revisit trigger: if `dispatchRateLimit` is ever changed, or throughput becomes a measured NFR. [server/internal/wa/dispatch_test.go]
- ~~Two different clocks for one "authoritative receipt time" — `InboundMessage.ReceivedAt` uses the Go process clock and is declared authoritative per FR-7, but the ledger row written in the same function stamps `received_at` from the Postgres transaction clock (`DEFAULT now()`), and the Go value is never passed to the INSERT. Two timestamps for one event, differing by network latency, transaction start offset and host clock skew. Dormant: `ReceivedAt` is write-only today (set, handed to a stub, asserted on only via `!IsZero()`). Revisit trigger: **story 3.3**, where receipt-time ordering becomes load-bearing for speed bonuses — decide there which clock wins and make both sites use it.~~ **closed in 3.3 (2026-08-04).** `wa.InboundMessage.ReceivedAt` (the Go webhook-ingestion clock) is now threaded through `Registrar.RecordAnswer` → `store.RecordAnswerParams.ReceivedAt` → `answers.received_at` — the Go clock wins for the persisted receipt time. The unrelated `wa_inbound_messages` dedupe ledger's own `received_at` (migration 00006) is untouched — it was never the receipt time this item was about; the load-bearing one (speed-bonus ordering, story 3.7) is `answers.received_at`, which now has exactly one clock. [server/internal/wa/webhook.go:297, server/internal/game/answers.go, server/migrations/00010_answers.sql]
- No freshness window: a captured signed webhook is replayable forever — a signed POST never expires, the dedupe ledger is the only replay defense, and the code deliberately steps around that ledger whenever the DB errors (the locked "process anyway" posture). During development the endpoint sits behind a public, unauthenticated `trycloudflare.com` URL. `messages[].timestamp` is not parsed at all, so no freshness window can be added later without re-modelling the message struct. Deferred: pilot threat model, and the ledger covers the non-degraded case. Revisit trigger: any production exposure beyond the pilot, or if the fail-open dedupe posture changes. [server/internal/wa/webhook.go]
- Permanent send errors are retried 3x and over-length bodies have no guard — an expired/revoked access token (401), a recipient outside the test-number allow-list (403/131030), or a body over WhatsApp's 4096-character text limit all burn the full 3 attempts plus 1s/2s backoff, tripling rate-token consumption and WARN volume at exactly the wrong moment (e.g. a token expiring mid-game). `SendText` returns an untyped `fmt.Errorf` with the status formatted into the string, so no `isPermanent(err)` classification is possible without introducing a typed error. Still open after 2.2: Task 3 closed only the empty-argument half of the 2026-07-20 item (a locally detectable unsendable message); classifying a *remote* permanent rejection needs a typed error from `SendText`, which no 2.2 task called for. Revisit trigger re-pointed (the original "2.2's first `Enqueue` caller" has now passed): **when an access token expires or a recipient falls outside the allow-list during a real run**, or at the first story that sends bodies whose length is not bounded by a template — introduce a typed error and skip retries for 4xx classes. [server/internal/wa/dispatch.go:147-174, server/internal/wa/client.go:113-123]
- `WaMessageIDDigest` is an unsalted SHA-256 truncated to 64 bits — 64 bits puts a birthday collision around 4 billion messages, at which point two distinct messages log under one identifier and log correlation silently produces the wrong answer. Separately, because the wamid's structure is now documented in the same file, anyone holding both the logs and a candidate phone list can confirm membership by recomputation — a weaker property than "log-safe digest" implies. Neither matters at pilot volume; both are free to fix by keeping the full 64-hex digest instead of truncating. Revisit trigger: if log-derived identifiers ever leave the operator's trust boundary. [server/internal/wa/redact.go:37-42]

## Deferred from: code review of 2-2-universal-reply-the-chat-never-goes-silent (2026-08-02)

- Zero in-flight grace between `Drain` returning and dispatcher-context cancellation — `Drain` polls `len(d.queue)`, which cannot see the up-to-8 messages workers have already dequeued; the very next line (`stopDispatcher()`) cancels the context that `SendText` and the retry backoff derive from, so a message mid-HTTP-flight or parked in a 1s/2s backoff is aborted even when most of the 5s drain budget is unused (a single transient Meta failure moves a message from the queue, which Drain watches, into backoff, which it cannot see). Not silent: every abort path logs a "send abandoned at shutdown" WARN, and `Run`'s doc records this as accepted pilot posture ("a redeploy mid-game already interrupts the room"). Revisit trigger: **Epic 3**, when replies carry answers/results and an aborted send costs a Participant real game state — have `Drain` wait on queue-empty AND an in-flight counter (or WaitGroup) within its ctx budget. [server/cmd/server/main.go:148-150, server/internal/wa/dispatch.go:174-187]
- Router logs "universal reply queued" INFO even when `Enqueue` dropped the message on a full queue — `Enqueue` returns nothing, so `Handle` cannot know; the log stream then asserts AC-2 was met for a message that actually got silence, though the adjacent "outbound queue full, message dropped" WARN records the truth one line earlier. A truthful fix means `Enqueue` returning accepted/dropped, which changes the spec-pinned `Replier` interface. Revisit trigger: **story 3.3** (acknowledgments), the first caller that genuinely needs delivery feedback — widen the interface there. [server/internal/wa/inbound.go:104-108]
- No per-sender damping on the universal reply → mutual auto-responder loop — an inbound from an auto-responder (WhatsApp Business away-message, another bot) triggers our Help reply, which triggers its auto-reply, unbounded: nothing rate-limits per sender, deduplicates repeats, or detects repetition, and reaction add+remove toggles on our own messages (distinct wamids — dedupe never intervenes) each earn a fresh Help reply. Burns the shared 70/s budget and Meta quality rating. Deferred (user decision, 2026-08-02): Meta test-mode prevents the loop today — only verified recipients receive our replies, so an arbitrary bot cannot sustain it — and a damping policy (per-sender cooldown, TTL map) is a design decision deserving its own story. Revisit trigger: **before the platform number leaves Meta test-mode** or is opened to a non-allow-listed audience. [server/internal/wa/inbound.go:92-108]
- `dispatcherDrainTimeout` (5s) mathematically cannot empty the queue it guards — at 70/s sustained + 70 burst, 5 seconds drains ≤ ~420 messages of the 1000-slot capacity, so a SIGTERM landing on a deep queue discards the remainder (recorded only by `Run`'s `dropped_count` WARN). Spec-endorsed sizing ("5s drain + short join is fine") and pilot depth never approaches 420. Revisit trigger: beyond-pilot scale, or when `RAILWAY_DEPLOYMENT_DRAINING_SECONDS` gets sized on the service — budget the HTTP drain, dispatcher drain, and platform window together. [server/cmd/server/main.go:31]

## Deferred from: code review of 2-4-join-the-game-with-one-whatsapp-message (2026-08-03)

- Unbounded user-controlled strings reach replies, the DB, and the dashboard — no length/content guard on the echoed JOIN code, `שם:` display names, or WhatsApp profile names. A `JOIN <~4000-char token>` makes `invalidCodeMessage` exceed WhatsApp's 4096-char text-body limit, so Meta rejects the send and the sender gets silence (breaks FR-2's no-silence guarantee, burns 3 retries + rate tokens per 2.1's dispatch posture). A `שם:` body up to 4096 chars — including interior newlines and invisible bidi/zero-width characters (`ltr()` isolates but does not strip them) — becomes a stored display name broadcast to every dashboard snapshot. Deferred (user decision, 2026-08-03): Meta test-mode's allow-list already bounds who can send arbitrary strings today; bundle the fix (length caps on the parsed code/name, and likely stripping bidi/zero-width control characters) with the pre-existing per-sender-damping trigger from 2.2 rather than solving it in isolation now. Revisit trigger: **before the platform number leaves Meta test-mode** or opens to a non-allow-listed audience (same trigger as the 2.2 auto-responder-loop and this story's code-enumeration-oracle deferrals — solve all three together). [server/internal/wa/inbound.go:61-82, server/internal/game/participants.go:84-92]
- ~~Join's state-check → INSERT is check-then-write (TOCTOU)~~ **closed in 3.1's code review (2026-08-04).** `CreateParticipant`'s INSERT now sources from `games g WHERE g.state = ANY(allowed_states)`, re-reading state at write time instead of trusting the engine's earlier `GetGameByJoinCode` read; `joinLobby` passes `{'lobby'}`. A lost race (state changed between read and write) now surfaces as `store.ErrNotFound` from the same zero-rows path as a genuine phone conflict — `wa/inbound.go`'s existing default branch degrades any unrecognized `Join` error to the generic Help reply, so no new error type or outcome was needed. Original text below for the record.<br>Original: `game.Engine.Join` reads the game state via `GetGameByJoinCode`, then `CreateParticipant`'s `INSERT ... ON CONFLICT (game_id, phone) DO NOTHING` carried no `state = 'lobby'` predicate, so a game leaving `lobby` between the read and the INSERT would still register a player into a started game. The ON CONFLICT closed only the duplicate-phone race (AC-4), not the state race. [server/internal/game/participants.go, server/internal/store/queries/participants.sql, server/internal/store/participants.go]
- Concurrent joins can broadcast lobby snapshots out of order — each webhook request independently does INSERT → `buildSnapshot` (ListParticipants) → `Broadcast`; interleaving A-insert, B-insert, B-list(=2), B-broadcast, A-list-captured-1-earlier, A-broadcast leaves the dashboard showing 1 while 2 exist, until the next join/snapshot/reconnect self-heals. Distinct from the documented snapshot-*failure* skip (that path is honest; this one is reordering). Pilot-scale non-issue (joins trickle in; self-heals within one event). Revisit trigger: if lobby counts become load-bearing (Epic 4 audience display) — add a monotonic snapshot version/sequence the hub or client can use to drop stale frames. [server/internal/game/participants.go:96-111, server/internal/ws/hub.go]
- JOIN replies form a code-enumeration oracle with no rate limiting — invalid code / pre-lobby / started produce three distinguishable replies (spec-canonical copy, so the distinction itself is mandated), letting anyone probe the short human-friendly code keyspace over WhatsApp. Pre-existing webhook posture: no per-sender damping since 2.1/2.2 (see the 2.2 auto-responder-loop deferral). Revisit trigger: same as that item — **before the platform number leaves Meta test-mode** or opens to a non-allow-listed audience; a per-sender cooldown covers both. [server/internal/wa/inbound.go:181-232]

## Deferred from: code review of 2-5-late-join-becomes-spectator (2026-08-03)

- A lobby-joined Player who re-sends JOIN mid-game is told they are a spectator — `joinSpectator` ignores the existing row's `role`: a `role='player'` row refetched via `ON CONFLICT (game_id, phone) DO NOTHING` still yields `Outcome: JoinSpectator` and the "נרשמת כצופה — התוצאות יגיעו לכאן בסוף המשחק" reply, falsely telling an active mid-game Player they were demoted to spectator (no data corrupted — the conflict path never overwrites `role`). Spec-anticipated: story 2.5's Dev Notes explicitly chose not to special-case it and asked triage to log it here. *(Re-checked in 3.1's code review, 2026-08-04: genuinely reachable now — 3.1 shipped the control actions that move a game past `lobby` — but this story's patch only guarded CreateParticipant's insert against a state race, it did not touch this role-blind branch, so the item stands unchanged.)* Revisit trigger: **the next story touching the Join/Rename reply flow** — decide whether a role-aware reply is warranted (e.g. `if !created && p.Role == RolePlayer` → a player-flavored reply, or surface `p.Role` in `JoinResult` for the router to branch on). [server/internal/game/participants.go, server/internal/wa/inbound.go]
- ~~Spectator flavor of the state-read→INSERT TOCTOU~~ **closed in 3.1's code review (2026-08-04)**, same fix as the 2.4 Join-TOCTOU closure above: `CreateParticipant`'s guarded INSERT now re-checks state at write time; `joinSpectator` passes the live-state set (`question_open`/`question_closed`/`revealed`/`leaderboard`), so a `leaderboard`→`finished` transition landing in the race window is observed (surfaces as `store.ErrNotFound`, degraded to the generic Help reply) instead of silently registering a spectator into a finished game. Original text below for the record.<br>Original: a `leaderboard`→`finished` transition between `GetGameByJoinCode` and `CreateParticipant` registers a spectator into a finished game (the combination AC 2 forbids), after its results dispatch has already fired, so the notice's "results will arrive" promise is silently never fulfilled. [server/internal/game/participants.go, server/internal/store/queries/participants.sql]
- New spectator row hijacks the cross-game `שם:` rename from the sender's live player row — `UpdateParticipantNameByPhone` picks the phone's most-recently-joined row across all games, role-blind; a Player in game A who JOINs already-started game B gains a newer spectator row in B, so a subsequent `שם:` renames B's spectator row while A's live surfaces keep the stale name and the reply claims "עודכן ✓". The 2.4 rename-scoping decision predates mid-game JOINs creating rows at all. *(Re-checked in 3.1's code review, 2026-08-04: genuinely reachable now, but `UpdateParticipantNameByPhone` and its query were not touched by 3.1's patch — the item stands unchanged.)* Revisit trigger: **the next story touching the rename/`UpdateParticipantNameByPhone` flow** — revisit the rename scoping (prefer the most recent `role='player'` row, or scope by role). [server/internal/game/participants.go, server/internal/store/queries/participants.sql]
- Spectator rows silently inflate the role-blind snapshot roster/count — `buildSnapshot` uses role-unfiltered `ListParticipants` and `ParticipantSummary` has no role field, so any live-state snapshot served on a WS (re)connect counts spectators indistinguishably from players. No live-state consumer exists today (story 2.5's Dev Notes document this deliberately). Revisit trigger *(re-pointed by 3.1's code review, 2026-08-04: 3.1's control panel renders only the current question + CTAs, no roster — the trigger's assumption did not materialize)*: **the first surface rendering a live roster past lobby** — plausibly Epic 4's audience display, possibly never — decides: filter `role='player'` in `buildSnapshot` or add `role` to `ParticipantSummary`. [server/internal/game/engine.go, server/internal/game/snapshot.go, server/internal/ws/handler.go]

## Deferred from: code review of 3-1-run-the-game-live-control-state-machine (2026-08-04)

- Control mutations discard the REST response snapshot; the panel's advance depends on a single WS frame — every control action is `api<void>`, throwing away the snapshot the backend deliberately returns, and the hub drops a full/slow connection's frame (8-slot queue) with no redelivery. A dropped frame leaves the organizer's panel on stale state (a re-press produces a spurious 409); a dropped `finished` frame is terminal until a manual reconnect, since finished never re-broadcasts. Deferred: this is the established single-source-of-truth WS posture from 2.3 (open-lobby behaves identically), the drop window is small at pilot scale, and applying the REST snapshot into the module-level socket store (or refetch-on-settle) is an architecture change deserving its own decision. Revisit trigger: **the first real-world report of a stuck control panel**, or **Epic 4** (audience displays raise the cost of a stale terminal state) — apply the returned snapshot into the socket store, or add a snapshot version enabling safe refetch. [web/src/features/live/control-page.tsx:55-59, web/src/lib/use-game-socket.ts, server/internal/ws/hub.go:172-178]
- Broadcast `seq` is assigned at `Broadcast()`-call time, not DB-commit order — two concurrent control-panel actions (e.g. two organizer tabs) that commit their guarded UPDATEs in order A→B can have their goroutines call `hub.Broadcast` in order B→A (descheduling between the engine's write and the handler's broadcast); the *older* snapshot then receives the *higher* seq, and the client's stale-drop guard (`use-game-socket.ts`, `seq < lastSeq`) accepts it as newer — every viewer regresses to a stale state and sticks there, since a `finished` game never broadcasts again to self-correct. The WS *connect* path already guards this exact class of race (`seqBeforeBuild`/`seqAfterRegister` in `handler.go`); the five REST control handlers added by this story have no equivalent — `Broadcast` assigns `seq` under its own lock, independent of when the underlying transaction actually committed. Same family as 2.4's "concurrent joins can broadcast lobby snapshots out of order" (deferred 2026-08-03), now landing on terminal states where self-healing doesn't exist. User decision (2026-08-04): defer rather than patch now — the race requires two organizer sessions racing the same game, effectively unreachable at pilot scale (one organizer, one tab, per game). Revisit trigger: **before Epic 4** (audience displays make a stuck stale state visible to a whole room, not just one organizer tab) or **the first report of a control panel stuck on a stale state** — fix by deriving `seq` from something commit-ordered (e.g. `games.updated_at`/a DB sequence returned by the guarded UPDATE and threaded through to `Broadcast`) rather than assignment-order, or serialize broadcast-after-write per game. [server/internal/ws/hub.go:153-161, server/internal/httpapi/control.go, server/internal/ws/handler.go]

## Deferred from: code review of 3-2-question-delivery-to-every-phone (2026-08-04)

- `PlayerRecipients` is unscoped by `organizerID` on `ControlEngine` — authorization-by-convention, relying on the caller having already validated ownership via the preceding state transition. Mirrors the pre-existing `buildSnapshot`/`ListParticipants` pattern; not a new risk surface introduced by this diff, but the pattern is now used twice. Revisit if `PlayerRecipients` or a sibling unscoped method ever gains a direct HTTP-reachable call site. [server/internal/httpapi/control.go, server/internal/game/engine.go]
- Question text and MCQ options are not bidi-isolated, unlike every digit token in the same templates — organizer-authored Latin/mixed content (e.g. "C++", English question text) can render direction-scrambled in RTL. Deliberate, documented tradeoff per this story's Dev Notes ("free-form sentence content never has [isolation]"), not an oversight. Revisit if organizers author non-Hebrew question/option content in practice. [server/internal/wa/messages_he.go]
- The displayed time limit doesn't account for dispatch/delivery latency — a slow burst under retry/backoff could show "20 seconds" with meaningfully less time actually remaining once answer-cutoff enforcement lands. Inherent to the pre-existing async `Dispatcher` model, unchanged by this story per its own AC-2 note. Revisit at story 3.3 (answer-cutoff enforcement) if delivery-lag complaints surface. [server/internal/wa/messages_he.go, server/internal/wa/dispatch.go]
- `dispatchQuestionOpened`'s recipient-lookup-failure WARN uses the package-level `slog` instead of an injected/testable logger — matches every other `slog.Info`/`slog.Warn` call already in `control.go`; not a new departure introduced by this diff. [server/internal/httpapi/control.go]

## Deferred from: code review of 2-3-open-the-lobby-and-watch-it-live (2026-08-03)

- `role=display` authenticates with a full organizer session — whatever machine drives the public display must carry the organizer's `wc_session` cookie, which authorizes `POST /open-lobby` and every game mutation; after authentication the role parameter gates nothing (it is only logged). A projector/shared screen holding write-capable credentials is a real exposure the role query param appears to address but does not. No display UI consumes `role=display` yet — the WS protocol accepts it so the wire contract is ready. Revisit trigger: **Epic 4** (audience display shell, story 4.1) — design display auth there (e.g. a scoped read-only display token or signed display URL) instead of reusing the organizer session. [server/internal/ws/handler.go:44-65]
- Live WS connections survive logout and session expiry — the `/ws` handshake authenticates once; the read/write loops never re-validate, and neither the hub nor the auth service can terminate connections for a revoked session, so an already-open socket keeps receiving snapshots after `POST /auth/logout` or session-row expiry until the tab closes. Logout does not actually cut off live game data on a shared machine. Pilot posture accepts it (snapshots are low-sensitivity lobby/game state; sessions are long-lived). Revisit trigger: **when the display-auth design lands (Epic 4)** or if session revocation becomes a security requirement — index hub connections by organizer/session and tear them down on logout, or periodically re-authenticate long-lived connections. [server/internal/ws/handler.go:117-121]

## Deferred from: code review of 3-3-answer-intake-with-immediate-acknowledgment (2026-08-04)

- `handleTextOrAnswer`'s unexpected-error path logs the raw `err.Error()` at WARN — pre-existing pattern, not introduced by this story (`handleJoin`/`handleRename`'s default-error paths already do the same). An unclassified store/DB error could in principle embed parameter values, in tension with the rest of `wa` package's phone-digest-only logging posture. Revisit trigger: if/when error classification is generalized across the router (a typed-error effort touching all three default-error paths at once, not just this story's new call site). [server/internal/wa/inbound.go:221, server/internal/wa/inbound.go:294, server/internal/wa/inbound.go:318]

## Deferred from: code review of 3-4-grading-pipeline-mcq-and-exact-match (2026-08-05)

- Every SQL artifact this story introduces has zero automated coverage — the rewritten `RevealCurrentQuestion` join, the new `CountUngradedAnswersForCurrentQuestion` query, the extended `RecordAnswer` INSERT column list, and migration 00011 are exercised by nothing in CI. All four new engine/httpapi tests drive `stubStore`/`stubControlEngine`, so deleting the `NOT EXISTS` clause, inverting `stage IS NULL` to `stage IS NOT NULL`, breaking the `q.position = g.current_question_position` join, or never applying the migration at all would leave the entire suite green. Deferred: this is the project's established and documented testing standard since 2.1 (no real-DB unit tests; the throwaway `cmd/e2escratch` harness is the verification, and it did run clean for this story per the Debug Log) — changing it is a project-wide test-strategy decision, not a story-3.4 fix. Note this is exactly the class of gap that let both of this review's highest-severity findings through to review. Revisit trigger: **Epic 4**, or the first grading/reveal defect that reaches a real game — introduce a Postgres-backed integration test tier (testcontainers or a CI service container) covering the guarded UPDATEs and the migration chain. [server/internal/game/engine_test.go, server/internal/httpapi/control_test.go, server/internal/store/queries/]
- Bank-imported `accepted_answers` bypass the only trimming in the system — `ImportPackageQuestions` is a column-for-column `INSERT ... SELECT pq.accepted_answers` that never passes through `validateQuestion`'s `strings.TrimSpace`, and `questions_type_shape` checks cardinality but not content. A seed row with a leading/trailing space in an accepted answer makes that question ungradeable: every participant is marked incorrect, silently, and Reveal still succeeds because the rows *are* graded — just wrong. Pre-existing: `ImportPackageQuestions` predates this story; story 3.4 only makes the consequence visible by being the first code to compare against those values. Related evidence that Exact-only is already lossy against shipped content: seed row `00005_question_packages.sql:53` stores `ק"נ` with an ASCII quote while Hebrew keyboards emit U+05F4 gershayim `״` — that specific class is explicitly Story 3.5's, but the untrimmed-import path is not covered by that deferral. Revisit trigger: **Story 3.5** (natural home — it is already touching accepted-answer normalization) — either `SELECT array(SELECT btrim(x) FROM unnest(pq.accepted_answers) x)` in the import, or a `CHECK` on `package_questions`. [server/internal/store/queries/packages.sql:25-33, server/internal/httpapi/questions.go:141-145]
- Rolling back migration 00011 while the 3.4 binary is still live silently discards 100% of inbound answers — the Down migration drops `stage`/`is_correct`, but the 3.4 binary's `RecordAnswer` names both columns explicitly, so every insert fails with `42703 undefined_column`, which `store.RecordAnswer` does not map; participants get the generic Help reply and their answers are never recorded. Inherent to any additive-column rollback and not a defect in this diff, but worth recording because `Down` is the only tool an operator has for the ungraded-rows problem this review patched, which makes it an attractive and very wrong move under pressure. Revisit trigger: **whenever a rollback runbook is written** — document "roll back the binary first, then the migration" as the required order. [server/migrations/00011_answer_grading.sql:16-20]
- No `web/`, `strings.he.ts`, or `messages_he.go` changes in this story — flagged here at the story's own explicit request (Dev Notes: "Flag this scoping decision in this story's code review for a `deferred-work.md` entry"). Review agrees the scoping is correct, not an oversight: grading is 100% synchronous through 3.4, so `GRADING_INCOMPLETE` is unreachable by a real organizer and there is no state for a "waiting for grading" indicator to ever display. Building disabled/waiting UI now would be speculative work for a condition the code cannot produce. Revisit trigger: **Story 3.6** (the AI stage's ~5s latency is what first makes `GRADING_INCOMPLETE` genuinely reachable by an organizer clicking quickly) — add the disabled/waiting affordance to the control panel's "גלה תשובה" button then. [web/src/features/live/control-page.tsx, server/internal/httpapi/errors.go]
- Residual rolling-deploy window leaves permanently-ungraded answer rows — `store/migrate.go:23-24` documents that "zero-downtime redeploys briefly run two instances, and both boot through this path." In the seconds between the new instance applying 00011 and the old 3.3 instance draining, the old binary keeps inserting with its pre-3.4 column list; `is_correct`/`stage` default to NULL, `answers_grading_shape` is satisfied by `(NULL,NULL)`, the write succeeds, and the participant receives a normal ack. Those rows are created *after* 00011's Up-migration backfill runs, so the backfill does not reach them, and since there is no `UPDATE answers` statement anywhere in the codebase they can never be graded — the question they belong to can never be revealed. User decision (2026-08-05): backfill the certain case (pre-existing 3.3 rows) and accept this narrow window rather than timing around it — a grace-period constant in the SQL guard is a guess about deploy duration that will silently go wrong, and a two-phase NOT NULL migration is not worth the deploy-ordering burden while the gate is still vacuous scaffolding. Reviewer note: tstamp-scoping the gate (`received_at >= goose_db_version.tstamp`) does **not** help here — the overlapping instance's rows postdate the migration timestamp. Revisit trigger: **Story 3.6** — that is when async AI grading makes the gate load-bearing rather than vacuous, so it has to be genuinely correct then; close the window with a two-phase `NOT NULL` migration, or give the organizer a force-reveal escape hatch (which would also cover the "no `UPDATE answers` path exists" dead end). [server/migrations/00011_answer_grading.sql, server/internal/store/migrate.go:23-24, server/internal/store/queries/answers.sql]
