# Deferred Work

Items deferred from reviews and workflows. Revisit when the referenced trigger arrives.

## Deferred from: code review of 1-1-project-scaffold-ci-and-deployed-walking-skeleton (2026-07-13)

- ~~Spacing-scale enforcement ("no values between stops", DESIGN.md)~~ **closed in 1.3 (2026-07-15): disposition "convention + review".** Mechanical enforcement via `--spacing: initial` stays rejected — it breaks shadcn component defaults (e.g. button `h-9`→36px), and the shadcn inheritance contract (DESIGN.md) says component internals keep their defaults. The binding convention, applied throughout 1.3's builder UI: hand-written classes use only on-scale spacing stops (4/8/12/16/24/32/48/64px ↔ Tailwind 1/2/3/4/6/8/12/16) for padding/margin/gap; buttons are always `h-10` (40px), never shadcn's default `h-9`. Enforcement is code-review attention, not tooling.
- Fail-fast config accepts placeholder secrets — `require()` in `server/internal/config/config.go` validates presence only, so dummy WhatsApp/Anthropic values pass in production (the runbook currently instructs exactly that for unconsumed vars). Add strength/placeholder validation when the vars become live: ~~`SESSION_SECRET` at Story 1.2 (session signing)~~ **done in 1.2** (≥32 chars + `change-me*` rejected, boot fails fast); WhatsApp secrets remain for Story 2.1 (webhook signature verification).

## Deferred from: code review of 1-3-create-a-game-and-author-questions (2026-07-15)

- Concurrent `CreateQuestion` can assign duplicate `position` values — `CreateQuestion` computes `position = COALESCE(MAX(position),0)+1` inside a single `INSERT ... SELECT`, but under READ COMMITTED two concurrent creates for the same game can both read the same MAX before either commits, and the migration deliberately omits `UNIQUE (game_id, position)` (the story chose transactional reorder over fighting a deferred constraint). Result: two questions briefly share a position, order falling to the `created_at` tiebreaker. Self-healing — the next `ReorderQuestions` rewrites positions to a clean 1..N. Low severity for a single-organizer builder. Revisit trigger: if multi-tab / concurrent authoring or an ordering-sensitive consumer of `position` appears, add a serialization guard (advisory lock or `SELECT ... FOR UPDATE` on the game) or a `UNIQUE (game_id, position) DEFERRABLE` constraint with retry.

## Deferred from: code review of 1-4-configure-scoring-rules (2026-07-16)

- Scoring save error handling / unconditional card render — `ScoringEditor`'s mutation has no `onError`, so any non-2xx collapses into the single generic `strings.scoring.errorServer` ("try again in a moment"), and the card renders regardless of `game.state`. If the game were non-draft (409 `GAME_NOT_EDITABLE`) or deleted (404 `GAME_NOT_FOUND`), the user would see an un-recoverable retry loop with nothing invalidating the query to reflect reality. **Not reachable today**: every game is `draft` until game-start (story 2.3), and no game-delete exists — 404/409 cannot occur yet. Revisit trigger: when story 2.3 lets a game leave `draft`, add `onError` handling that distinguishes 409/404 (surface a "reload"/"not editable" state and invalidate `['games', gameId]`), and gate or disable the card when `game.state !== 'draft'`.
- Draft-only scoring guard is check-then-write (TOCTOU) — `UpdateGameScoring`'s `UPDATE games … WHERE id = $1 AND organizer_id = $2` omits `state = 'draft'`, so a draft→lobby transition landing between the `requireDraftGame` read and the write would still persist scoring to a running game. ~~Pre-existing pattern, not introduced here: every existing question mutation (`UpdateQuestion`/`DeleteQuestion`/`UpdateQuestionPosition`) relies on the same `requireDraftGame` check-then-write with no SQL `state` predicate~~ **question-mutation half closed in 3.1's code review (2026-08-04)**: `CreateQuestion`/`UpdateQuestion`/`DeleteQuestion`/`UpdateQuestionPosition` (and the `ReorderQuestions` transaction's up-front check) now all guard on `state = 'draft'` in the `WHERE` clause. `UpdateGameScoring` itself was out of that patch's scope and remains unguarded. Revisit trigger: the next time `UpdateGameScoring` is touched — add the same `AND state = 'draft'` guard, mapping a lost race to `ErrNotFound`/404, matching the pattern now established for questions. [server/internal/store/games.go, server/internal/store/queries/games.sql]

## Deferred from: code review of 1-5-question-bank-package-import (2026-07-16)

- Package import inherits the two already-tracked concurrency postures above — extend the same fix to it when 2.3 lands: (1) **position race** — `ImportPackageQuestions` appends with `COALESCE(MAX(position),0)+row_number()` and no txn / no `UNIQUE (game_id, position)`, so two overlapping imports (or an import racing a `CreateQuestion`) both read the same MAX and duplicate positions (self-healing via the next reorder); same class as the 1.3 `CreateQuestion` item. (2) **draft TOCTOU** — the `INSERT … SELECT` WHERE scopes only on `g.id`/`g.organizer_id` (no `state = 'draft'`), so a draft→lobby transition or a delete between `requireDraftGame` and the write is not observed (append to a non-draft game, or a bogus 201 with empty items); same class as the 1.4 draft-only guard item. Revisit trigger unchanged: at game-start (2.3), decide the posture for ALL draft-only mutations together (import included) — `AND state = 'draft'` in the WHERE and/or a row lock, plus the `UNIQUE (game_id, position)` / serialization guard for positions.
- Import-dialog UX polish — one shared `importPackage.isPending` disables every package card's import button (no per-card affordance showing which import is in flight), and the dialog stays dismissable mid-import (the mutation still resolves → `invalidateQueries` + `onClose` after unmount). Harmless today (one import at a time; React Query settle-after-unmount is a no-op). Revisit if the bank grows to many packages or import latency becomes user-visible: add per-card pending state and block dismiss while a mutation is in flight. [web/src/features/builder/package-import-dialog.tsx]
- Hebrew dual-form count — `questionBank.questionsCount` renders `${count} שאלות` for every n≠1, so n=2 reads "2 שאלות" rather than the grammatical dual "שתי שאלות" (and n=0 could read "אין שאלות"). Pure i18n polish, no path impact. Revisit if Hebrew grammatical number becomes a broader UI concern. [web/src/lib/strings.he.ts]

## Deferred from: code review of story-2.1 (2026-07-20)

- Token-bucket burst can momentarily exceed 80 msg/sec — `rate.NewLimiter(70,70)` (burst=70) can pass ~140 sends in a straddling 1-second window; AC-4's "under 80 msg/sec" governs the sustained rate (70/s) and the limiter config is exactly what the spec instructed. Non-issue at pilot scale (~80-message bursts drain in ~1.2s). Revisit trigger: if outbound throughput requirements grow, or a strict instantaneous ceiling is ever mandated, lower the burst (e.g. `burst = 1`) or reserve headroom for shared inbound traffic. [server/internal/wa/dispatch.go:72]
- ~~No empty recipient/body guard in SendText/Enqueue~~ **closed in 2.2 (2026-08-02, Task 3).** `Dispatcher.Enqueue` now drops an empty `to` or `body` with a WARN before the message reaches the queue, so it can no longer burn 3 attempts, the 1s/2s backoff and 3 rate tokens. The guard sits at `Enqueue` only, not inside `Client.SendText`: the dispatcher is the sole caller of `SendText`, and duplicating the check there would add a second place to keep in sync for no reachable case.
- Retry backoff sleeps inside the worker goroutine — under a Meta outage all 8 workers can park in `contextSleep` (1s then 2s) simultaneously, collapsing queue-drain throughput exactly when the queue is filling, contributing to the queue-full drops the design treats as pathology-only. Pilot non-issue. Revisit trigger: if resilience under sustained Meta degradation becomes a requirement, move retries off the worker (dedicated retry queue / delayed re-enqueue) so backoff never blocks draining. [server/internal/wa/dispatch.go:149]

## Deferred from: code review of story-2.1 (2026-08-02, second round)

- Token-bucket burst can pass ~140 sends across a one-second boundary — re-raised independently by two review layers; already deferred in the 2026-07-20 round and the reasoning stands (AC-4 governs the sustained rate of 70/s; the limiter config is exactly what the spec instructed). Same revisit trigger as the original entry above. The one new sub-point — `dispatch.go`'s comment asserting a hard ceiling the limiter does not provide — was patched as a documentation fix, not deferred. [server/internal/wa/dispatch.go:82]
- ~~Dispatcher shutdown ordering: `Enqueue` after `Run` returns black-holes messages with zero observability~~ **closed in 2.2 (2026-08-02, Task 4).** The dispatcher now runs on its own `context.Background()`-derived context, and the shutdown sequence is: `srv.Shutdown` (all handlers return, so everything they queued is in the queue) → `dispatcher.Drain(5s)` → cancel the dispatcher context → join `Run` with a 2s grace so its unsent-message summary WARN is logged before exit. Verified by a negative control in the 2.2 E2E: with the pre-fix wiring a 40-message burst arriving during shutdown delivered 14 and logged only 10 as dropped (16 vanished silently); with the fix all 40 are delivered. Original text below for the record.<br>Original: SIGTERM cancels the ctx shared by the dispatcher and the HTTP server — SIGTERM cancels the ctx shared by the dispatcher and the HTTP server; `Run`'s workers exit and its drain-and-count snapshot completes, but `srv.Shutdown` keeps serving in-flight webhook requests for up to 15s. From 2.2 those requests call `Enqueue`, which writes into a 1000-slot buffer no goroutine will read again: no drop WARN (the queue is not full), no `dropped_count` (already snapshotted), no error to the caller (fire-and-forget by design). The drain loop's `default` arm is additionally racy against a concurrent `Enqueue`. Unreachable in 2.1 — no production `Enqueue` caller exists. Revisit trigger: **2.2's first `Enqueue` caller** — give the dispatcher its own cancel sequenced after `srv.Shutdown`, await `Run` before process exit, and make post-shutdown `Enqueue` log a drop rather than buffering into the void. [server/cmd/server/main.go:83, server/internal/wa/dispatch.go:99-134]
- The rate limiter has zero executable coverage — every dispatcher test enqueues at most 3 messages, far inside a burst of 70, so `limiter.Wait` never blocks on any exercised path; deleting the call or raising the rate to 100,000 leaves the suite green. AC-4's only quantitative requirement is the only untested one. Deferred rather than patched because a deterministic limiter test requires injecting its clock — which story 2.1 explicitly locked as "overkill" and which the testing standards ("deterministic tests only, no `time.Sleep` assertions") reinforce; reopening it is a spec decision, not a patch. Revisit trigger: if `dispatchRateLimit` is ever changed, or throughput becomes a measured NFR. [server/internal/wa/dispatch_test.go]
- ~~Two different clocks for one "authoritative receipt time" — `InboundMessage.ReceivedAt` uses the Go process clock and is declared authoritative per FR-7, but the ledger row written in the same function stamps `received_at` from the Postgres transaction clock (`DEFAULT now()`), and the Go value is never passed to the INSERT. Two timestamps for one event, differing by network latency, transaction start offset and host clock skew. Dormant: `ReceivedAt` is write-only today (set, handed to a stub, asserted on only via `!IsZero()`). Revisit trigger: **story 3.3**, where receipt-time ordering becomes load-bearing for speed bonuses — decide there which clock wins and make both sites use it.~~ **closed in 3.3 (2026-08-04).** `wa.InboundMessage.ReceivedAt` (the Go webhook-ingestion clock) is now threaded through `Registrar.RecordAnswer` → `store.RecordAnswerParams.ReceivedAt` → `answers.received_at` — the Go clock wins for the persisted receipt time. The unrelated `wa_inbound_messages` dedupe ledger's own `received_at` (migration 00006) is untouched — it was never the receipt time this item was about; the load-bearing one (speed-bonus ordering, story 3.7) is `answers.received_at`, which now has exactly one clock. [server/internal/wa/webhook.go:297, server/internal/game/answers.go, server/migrations/00010_answers.sql]
- No freshness window: a captured signed webhook is replayable forever — a signed POST never expires, the dedupe ledger is the only replay defense, and the code deliberately steps around that ledger whenever the DB errors (the locked "process anyway" posture). During development the endpoint sits behind a public, unauthenticated `trycloudflare.com` URL. `messages[].timestamp` is not parsed at all, so no freshness window can be added later without re-modelling the message struct. Deferred: pilot threat model, and the ledger covers the non-degraded case. Revisit trigger: any production exposure beyond the pilot, or if the fail-open dedupe posture changes. [server/internal/wa/webhook.go]
- Permanent send errors are retried 3x and over-length bodies have no guard — an expired/revoked access token (401), a recipient outside the test-number allow-list (403/131030), or a body over WhatsApp's 4096-character text limit all burn the full 3 attempts plus 1s/2s backoff, tripling rate-token consumption and WARN volume at exactly the wrong moment (e.g. a token expiring mid-game). `SendText` returns an untyped `fmt.Errorf` with the status formatted into the string, so no `isPermanent(err)` classification is possible without introducing a typed error. Still open after 2.2: Task 3 closed only the empty-argument half of the 2026-07-20 item (a locally detectable unsendable message); classifying a *remote* permanent rejection needs a typed error from `SendText`, which no 2.2 task called for. Revisit trigger re-pointed (the original "2.2's first `Enqueue` caller" has now passed): **when an access token expires or a recipient falls outside the allow-list during a real run**, or at the first story that sends bodies whose length is not bounded by a template — introduce a typed error and skip retries for 4xx classes. [server/internal/wa/dispatch.go:147-174, server/internal/wa/client.go:113-123]
- `WaMessageIDDigest` is an unsalted SHA-256 truncated to 64 bits — 64 bits puts a birthday collision around 4 billion messages, at which point two distinct messages log under one identifier and log correlation silently produces the wrong answer. Separately, because the wamid's structure is now documented in the same file, anyone holding both the logs and a candidate phone list can confirm membership by recomputation — a weaker property than "log-safe digest" implies. Neither matters at pilot volume; both are free to fix by keeping the full 64-hex digest instead of truncating. Revisit trigger: if log-derived identifiers ever leave the operator's trust boundary. [server/internal/wa/redact.go:37-42]

## Deferred from: code review of 2-2-universal-reply-the-chat-never-goes-silent (2026-08-02)

- Zero in-flight grace between `Drain` returning and dispatcher-context cancellation — `Drain` polls `len(d.queue)`, which cannot see the up-to-8 messages workers have already dequeued; the very next line (`stopDispatcher()`) cancels the context that `SendText` and the retry backoff derive from, so a message mid-HTTP-flight or parked in a 1s/2s backoff is aborted even when most of the 5s drain budget is unused (a single transient Meta failure moves a message from the queue, which Drain watches, into backoff, which it cannot see). Not silent: every abort path logs a "send abandoned at shutdown" WARN, and `Run`'s doc records this as accepted pilot posture ("a redeploy mid-game already interrupts the room"). Revisit trigger: **Epic 3**, when replies carry answers/results and an aborted send costs a Participant real game state — have `Drain` wait on queue-empty AND an in-flight counter (or WaitGroup) within its ctx budget. [server/cmd/server/main.go:148-150, server/internal/wa/dispatch.go:174-187]
- Router logs "universal reply queued" INFO even when `Enqueue` dropped the message on a full queue — `Enqueue` returns nothing, so `Handle` cannot know; the log stream then asserts AC-2 was met for a message that actually got silence, though the adjacent "outbound queue full, message dropped" WARN records the truth one line earlier. A truthful fix means `Enqueue` returning accepted/dropped, which changes the spec-pinned `Replier` interface. Revisit trigger: **story 3.3** (acknowledgments), the first caller that genuinely needs delivery feedback — widen the interface there. [server/internal/wa/inbound.go:104-108]
- No per-sender damping on the universal reply → mutual auto-responder loop — an inbound from an auto-responder (WhatsApp Business away-message, another bot) triggers our Help reply, which triggers its auto-reply, unbounded: nothing rate-limits per sender, deduplicates repeats, or detects repetition, and reaction add+remove toggles on our own messages (distinct wamids — dedupe never intervenes) each earn a fresh Help reply. Burns the shared 70/s budget and Meta quality rating. Deferred (user decision, 2026-08-02): Meta test-mode prevents the loop today — only verified recipients receive our replies, so an arbitrary bot cannot sustain it — and a damping policy (per-sender cooldown, TTL map) is a design decision deserving its own story. Revisit trigger: **before the platform number leaves Meta test-mode** or is opened to a non-allow-listed audience. [server/internal/wa/inbound.go:92-108]
- `dispatcherDrainTimeout` (5s) mathematically cannot empty the queue it guards — at 70/s sustained + 70 burst, 5 seconds drains ≤ ~420 messages of the 1000-slot capacity, so a SIGTERM landing on a deep queue discards the remainder (recorded only by `Run`'s `dropped_count` WARN). Spec-endorsed sizing ("5s drain + short join is fine") and pilot depth never approaches 420. Revisit trigger: beyond-pilot scale, or when `RAILWAY_DEPLOYMENT_DRAINING_SECONDS` gets sized on the service — budget the HTTP drain, dispatcher drain, and platform window together. [server/cmd/server/main.go:31]

## Deferred from: code review of 2-4-join-the-game-with-one-whatsapp-message (2026-08-03)

- Unbounded user-controlled strings reach replies, the DB, and the dashboard — no length/content guard on the echoed JOIN code, `שם:` display names, or WhatsApp profile names. A `JOIN <~4000-char token>` makes `invalidCodeMessage` exceed WhatsApp's 4096-char text-body limit, so Meta rejects the send and the sender gets silence (breaks FR-2's no-silence guarantee, burns 3 retries + rate tokens per 2.1's dispatch posture). A `שם:` body up to 4096 chars — including interior newlines and invisible bidi/zero-width characters (`ltr()` isolates but does not strip them) — becomes a stored display name broadcast to every dashboard snapshot. Deferred (user decision, 2026-08-03): Meta test-mode's allow-list already bounds who can send arbitrary strings today; bundle the fix (length caps on the parsed code/name, and likely stripping bidi/zero-width control characters) with the pre-existing per-sender-damping trigger from 2.2 rather than solving it in isolation now. Revisit trigger: **before the platform number leaves Meta test-mode** or opens to a non-allow-listed audience (same trigger as the 2.2 auto-responder-loop and this story's code-enumeration-oracle deferrals — solve all three together). [server/internal/wa/inbound.go:61-82, server/internal/game/participants.go:84-92]
- ~~Join's state-check → INSERT is check-then-write (TOCTOU)~~ **closed in 3.1's code review (2026-08-04).** `CreateParticipant`'s INSERT now sources from `games g WHERE g.state = ANY(allowed_states)`, re-reading state at write time instead of trusting the engine's earlier `GetGameByJoinCode` read; `joinLobby` passes `{'lobby'}`. A lost race (state changed between read and write) now surfaces as `store.ErrNotFound` from the same zero-rows path as a genuine phone conflict — `wa/inbound.go`'s existing default branch degrades any unrecognized `Join` error to the generic Help reply, so no new error type or outcome was needed. Original text below for the record.<br>Original: `game.Engine.Join` reads the game state via `GetGameByJoinCode`, then `CreateParticipant`'s `INSERT ... ON CONFLICT (game_id, phone) DO NOTHING` carried no `state = 'lobby'` predicate, so a game leaving `lobby` between the read and the INSERT would still register a player into a started game. The ON CONFLICT closed only the duplicate-phone race (AC-4), not the state race. [server/internal/game/participants.go, server/internal/store/queries/participants.sql, server/internal/store/participants.go]
- Concurrent joins can broadcast lobby snapshots out of order — each webhook request independently does INSERT → `buildSnapshot` (ListParticipants) → `Broadcast`; interleaving A-insert, B-insert, B-list(=2), B-broadcast, A-list-captured-1-earlier, A-broadcast leaves the dashboard showing 1 while 2 exist, until the next join/snapshot/reconnect self-heals. Distinct from the documented snapshot-*failure* skip (that path is honest; this one is reordering). Pilot-scale non-issue (joins trickle in; self-heals within one event). Revisit trigger: if lobby counts become load-bearing (Epic 4 audience display) — add a monotonic snapshot version/sequence the hub or client can use to drop stale frames. [server/internal/game/participants.go:96-111, server/internal/ws/hub.go]
  - **Checked at 4.1's code review (2026-08-09): NOT triggered — but 4.2 will trigger it.** The shell renders no counter; every state maps to one text placeholder. Story 4.2 (the lobby stage, "the room fills the screen") is the story that makes a lobby count load-bearing on a projector, and must resolve this entry before it ships. Trigger unchanged, but read it now as "story 4.2".
  - **TRIGGERED and MEASURED by story 4.2 (2026-08-11). Outcome: mitigated on this one surface; the general fix is re-deferred.** The reordering is **real, frequent, and now demonstrated on the wire** — not a theoretical race. A throwaway `cmd/e2escratch` harness opened a `role=display` socket, fired 20 concurrent signed JOIN webhooks from 20 distinct phones, and recorded every `participantCount` in arrival order across four runs. **Three of four runs delivered frames that counted backwards**, and the final value was correct in all four: run 1 `[4 5 14 14 14 16 15 14 16 16 17 17 21 17 21 16 21 21 21 22]` — 4 of 20 frames lower than one already delivered (the visible sequence includes `17 → 21 → 17` and `21 → 16`); run 2 `[20 20 20 20 20 21 20 22 21 21 21 21 21 21 20 21 22 22 21 22]` — 10 of 20, including a `22 → 21` after the true total had already been shown; run 3 — 2 of 17; run 4 — 0 of 20. (Runs also deliver **fewer frames than joins**: 17 for 22 joins in run 3. That is the hub's 8-slot per-connection drop, a separate documented behaviour, and it is why a self-healing "the next join fixes it" argument is weaker than it looks — the next join's frame can be the one that gets dropped.)
    - **What story 4.2 does about it:** `web/src/features/display/lobby-stage.tsx` holds a **high-water participant count**, so the projector can never regress to a stale lower number. This is sound rather than a lie *here and only here*: in `lobby` the count is monotonically non-decreasing by construction — nothing deletes a participant row, and a spectator can only be created past `lobby` (see the 2.5 roster entry). It is a **symptom-level guard**, recorded as such. A Vitest regression test pins it (`web/src/features/display/lobby-stage.test.tsx`, "never counts backwards when a stale lower count arrives"), demonstrated red against a build with the guard removed before being made green.
    - **Its two limits, stated explicitly.** (1) It does **not** help the dashboard's own lobby counter in `features/lobby/lobby-page.tsx`, which still renders `participantCount` raw and can still show the backwards count the runs above measured. (2) It would be **wrong** to copy into any later stage where a snapshot field can legitimately decrease — 4.5's leaderboard positions genuinely move both ways, and freezing a maximum there would be a real bug.
    - **What it does not touch:** the underlying `seq`-at-`Broadcast()`-time ordering defect is untouched, and it is the same defect as the 3.1 `seq` entry below, whose fix is a `ws`/`control.go` redesign. **These two entries must be read together and fixed together**; note also that `games.updated_at` is no longer a valid commit-ordered source for that fix (see the `updated_at` entry under 4.1's code review).
    - **New trigger, replacing "story 4.2":** *the first snapshot field rendered on the Audience Display that can legitimately decrease* — 4.3's answered count cannot; **4.5's leaderboard can**, and is the likely one — **or the general `seq` fix landing**, whichever comes first. The measurement above is the evidence to reuse; do not re-derive it.
    - **Checked by story 4.3 (2026-08-11): NOT re-triggered, and stated in writing so a later reader does not mistake 4.3's guard for a new, unjustified pattern.** The new trigger above is *"the first snapshot field rendered on the Audience Display that can legitimately decrease"*, and 4.3's answered count is not it: within one open Question the count is **monotonically non-decreasing by construction** — `answers` is append-once under `UNIQUE (question_id, participant_id)` and nothing deletes an answer row. 4.3 nevertheless applies the **same high-water guard** in `question-stage.tsx`'s `AnsweredCount`, for the same measured reason: answers arrive in exactly the concurrent-writer burst shape the runs above measured, so the identical backwards-count is reachable on this field. Two differences from 4.2's are deliberate. (1) The hold is **keyed on `currentQuestion.id`** and resets when it changes — today the shell's `${gameId}:${state}` key means two `question_open` stages are never adjacent, but that is the *shell's* invariant, not this stage's, and question 2 inheriting question 1's count would be a silent lie in front of a room. (2) A Vitest case pins **both** halves (`question-stage.test.tsx`: "never counts the answered total backwards", demonstrated red first, and "resets the answered count when the question id changes"). The limit 4.2 recorded still stands and now has a second reader: **do not copy this into 4.5's leaderboard**, where positions genuinely move both ways. Trigger unchanged — 4.5 remains the likely one. [web/src/features/display/question-stage.tsx, web/src/features/display/question-stage.test.tsx]
  - **Checked by story 4.4 (2026-08-12): NOT re-triggered, and the reason here is STRONGER than at 4.3 — plus this stage deliberately adds no guard of its own.** The trigger is *"the first snapshot field rendered on the Audience Display that can legitimately decrease"*. 4.4 renders three new numbers — `reveal.optionCounts[i]`, `reveal.correctCount`, and the percentages derived from them — and **none of them can decrease, for a reason 4.3's field did not have**. 4.3's answered count is monotonic *within an open question* because `answers` is append-once under `UNIQUE (question_id, participant_id)` and nothing deletes a row; 4.4's counts have that **plus** the fact that by `revealed` the cutoff has already passed (`CloseCurrentQuestion` sets `answer_cutoff_at = LEAST(answer_cutoff_at, now())`, measured at 4.3 as a 0.4s pull-back), so **no new rows can arrive at all**. The set these counts aggregate is closed before the first frame that carries them exists. **Recorded explicitly: this stage adds NO high-water guard**, and that is deliberate rather than an oversight. 4.2's own note says copying the guard where a value can legitimately move both ways would be a real bug, and the reason it is unnecessary here is stronger than at 4.3 — a guard would be pure ceremony over a frozen set. What 4.4 *does* inherit is the shell's `answeredFloor` (`display-page.tsx`, added at 4.3's review), and derived requirement 8 turns that into the reason the distribution uses **its own denominator**: the floor deliberately raises `answeredCount` above the frame's value, so dividing the bars by it would make every bar silently understate. The bars divide by `sum(optionCounts)`. A Vitest case pins exactly that, driven with `answeredCount: 25` against bars summing to 10, and it was demonstrated red by swapping the denominator (`Unable to find an element with the text: 2 · 20%`). Trigger unchanged — 4.5's leaderboard remains the likely one. [web/src/features/display/reveal-stage.tsx, web/src/features/display/reveal-stage.test.tsx]
  - **TRIGGERED by story 4.5 (2026-08-12). Decided in writing: NO high-water guard is applied, and that is the decision — re-deferred with the 3.1 `seq` entry.** The trigger fired exactly as written: the leaderboard is the first Audience Display field that can *legitimately* decrease, and it does — a score can only rise, but a **rank and a slot move both ways**, which is the entire point of the stage. Copying 4.2's monotonic guard here would freeze the board at each row's best-ever position and permanently break the reshuffle 4.5 exists to show; 4.2's own note predicted exactly that (*"into any later stage where a snapshot field can legitimately decrease… would be a real bug"*). **What makes the disposition honest, recorded so nobody re-derives it:** the concurrent-writer burst that produced 4.2's measurement (20 simultaneous inbound joins, each its own webhook request racing `buildSnapshot`→`Broadcast`) **has no analogue at `leaderboard`**. The answer cutoff has passed, no inbound WhatsApp message writes anything at this state, and the only writers are the Organizer's own serialized control clicks plus the display-settings toggle. The reachable race is therefore the ordinary press-then-tick window the 3.1 `seq` entry already owns, whose fix is a `ws`/`control.go` redesign — not a per-field guard. 4.2's measurement is **not** re-derived here and does not need to be. **New trigger, replacing "4.5's leaderboard":** *the `seq` redesign itself, or the first report of a leaderboard visibly jumping backwards in front of a room.* [web/src/features/display/leaderboard-stage.tsx, web/src/features/display/use-leaderboard-memory.ts]
- JOIN replies form a code-enumeration oracle with no rate limiting — invalid code / pre-lobby / started produce three distinguishable replies (spec-canonical copy, so the distinction itself is mandated), letting anyone probe the short human-friendly code keyspace over WhatsApp. Pre-existing webhook posture: no per-sender damping since 2.1/2.2 (see the 2.2 auto-responder-loop deferral). Revisit trigger: same as that item — **before the platform number leaves Meta test-mode** or opens to a non-allow-listed audience; a per-sender cooldown covers both. [server/internal/wa/inbound.go:181-232]

## Deferred from: code review of 2-5-late-join-becomes-spectator (2026-08-03)

- A lobby-joined Player who re-sends JOIN mid-game is told they are a spectator — `joinSpectator` ignores the existing row's `role`: a `role='player'` row refetched via `ON CONFLICT (game_id, phone) DO NOTHING` still yields `Outcome: JoinSpectator` and the "נרשמת כצופה — התוצאות יגיעו לכאן בסוף המשחק" reply, falsely telling an active mid-game Player they were demoted to spectator (no data corrupted — the conflict path never overwrites `role`). Spec-anticipated: story 2.5's Dev Notes explicitly chose not to special-case it and asked triage to log it here. *(Re-checked in 3.1's code review, 2026-08-04: genuinely reachable now — 3.1 shipped the control actions that move a game past `lobby` — but this story's patch only guarded CreateParticipant's insert against a state race, it did not touch this role-blind branch, so the item stands unchanged.)* Revisit trigger: **the next story touching the Join/Rename reply flow** — decide whether a role-aware reply is warranted (e.g. `if !created && p.Role == RolePlayer` → a player-flavored reply, or surface `p.Role` in `JoinResult` for the router to branch on). [server/internal/game/participants.go, server/internal/wa/inbound.go]
- ~~Spectator flavor of the state-read→INSERT TOCTOU~~ **closed in 3.1's code review (2026-08-04)**, same fix as the 2.4 Join-TOCTOU closure above: `CreateParticipant`'s guarded INSERT now re-checks state at write time; `joinSpectator` passes the live-state set (`question_open`/`question_closed`/`revealed`/`leaderboard`), so a `leaderboard`→`finished` transition landing in the race window is observed (surfaces as `store.ErrNotFound`, degraded to the generic Help reply) instead of silently registering a spectator into a finished game. Original text below for the record.<br>Original: a `leaderboard`→`finished` transition between `GetGameByJoinCode` and `CreateParticipant` registers a spectator into a finished game (the combination AC 2 forbids), after its results dispatch has already fired, so the notice's "results will arrive" promise is silently never fulfilled. [server/internal/game/participants.go, server/internal/store/queries/participants.sql]
- New spectator row hijacks the cross-game `שם:` rename from the sender's live player row — `UpdateParticipantNameByPhone` picks the phone's most-recently-joined row across all games, role-blind; a Player in game A who JOINs already-started game B gains a newer spectator row in B, so a subsequent `שם:` renames B's spectator row while A's live surfaces keep the stale name and the reply claims "עודכן ✓". The 2.4 rename-scoping decision predates mid-game JOINs creating rows at all. *(Re-checked in 3.1's code review, 2026-08-04: genuinely reachable now, but `UpdateParticipantNameByPhone` and its query were not touched by 3.1's patch — the item stands unchanged.)* Revisit trigger: **the next story touching the rename/`UpdateParticipantNameByPhone` flow** — revisit the rename scoping (prefer the most recent `role='player'` row, or scope by role). [server/internal/game/participants.go, server/internal/store/queries/participants.sql]
- Spectator rows silently inflate the role-blind snapshot roster/count — `buildSnapshot` uses role-unfiltered `ListParticipants` and `ParticipantSummary` has no role field, so any live-state snapshot served on a WS (re)connect counts spectators indistinguishably from players. No live-state consumer exists today (story 2.5's Dev Notes document this deliberately). Revisit trigger *(re-pointed by 3.1's code review, 2026-08-04: 3.1's control panel renders only the current question + CTAs, no roster — the trigger's assumption did not materialize)*: **the first surface rendering a live roster past lobby** — plausibly Epic 4's audience display, possibly never — decides: filter `role='player'` in `buildSnapshot` or add `role` to `ParticipantSummary`. [server/internal/game/engine.go, server/internal/game/snapshot.go, server/internal/ws/handler.go]
  - **Checked by story 4.4 (2026-08-12): NOT triggered.** The reveal stage renders **no roster at all** — epic AC-3 forbids any per-Participant datum on the shared screen, and `reveal-stage.tsx` never reads `snapshot.participants`, `snapshot.participantCount` or `snapshot.leaderboard`. Two tests assert it rather than trusting the reading: a Vitest case renders the stage against a snapshot whose participants carry distinctive names (`ZZZ-PARTICIPANT-NAME`, `+972-50-999-9999`) and asserts neither appears in the container's text, and the browser pass does the same against the real DOM with real seeded participants. Independently of the roster: this stage's counts are derived from `answers`, and **a spectator never answers**, so an inflated roster could not reach these numbers even if one were rendered. Trigger unchanged. [web/src/features/display/reveal-stage.tsx]
  - **Checked by story 4.1 (2026-08-09): NOT triggered.** The display shell renders no roster at all — every state maps to one text placeholder — and 4.1 added nothing to `Snapshot` but `displaySettings`. 4.2's lobby counter is lobby-only, where spectator rows cannot exist by definition (a spectator is created by a late join, i.e. past lobby), so 4.2 does not fire it either. Trigger unchanged.
  - **Re-checked by story 4.2 itself (2026-08-11): still NOT triggered, and 4.1's prediction held.** Stated in writing so 4.3 does not have to re-derive it. `LobbyStage` renders `participantCount` and nothing else off the roster — it never reads `snapshot.participants`, and it is mounted for exactly one state (`lobby`), where a spectator row cannot exist. The lobby-only reasoning is now load-bearing for a second reason: it is also what makes 4.2's high-water guard sound (see the 2.4 entry above), so if this entry's premise is ever falsified — a spectator, or any other row, becoming creatable during `lobby` — **both** entries have to be revisited together. Trigger unchanged.
  - **Re-checked by story 4.3 (2026-08-11): still NOT triggered.** The question stage renders no roster: it reads `currentQuestion` (`id`, `position`, `type`, `text`, `options`, `timeLimitSeconds`, `answerCutoffAt`, `answeredCount`), `questionCount` and `state`, and never touches `snapshot.participants` or `participantCount`. Note the one thing that *has* changed since 4.2's check: this stage **is** mounted past `lobby`, where spectator rows can exist — so the lobby-only reasoning that protects 4.2 does not apply here. It does not need to. `answeredCount` comes from `CountAnswersByQuestion`, which counts answer rows, and a spectator never answers; the roster's role-blindness cannot reach this number. The trigger remains *the first surface rendering a live roster past lobby*, and 4.5's leaderboard is the next candidate — it renders `leaderboard`, built from scores rather than the roster, so it may well not fire it either. Trigger unchanged.
  - **Checked by story 4.5 (2026-08-12): NOT triggered — and 4.3's prediction held exactly. This is the closest call of the three 4.5 checked, so the reason is stated rather than asserted.** 4.5 *is* "a surface rendering a live roster past lobby" in the loose sense — it is the first stage to put participant names on a projector mid-game. It still does not fire this entry, because the entry is about **`ListParticipants`, which is role-blind**, and the leaderboard does not come from it: `GetLeaderboard` (`queries/answers.sql`) filters `role = 'player'` **in SQL**, so a Spectator can never appear on the board no matter how the display renders it. The stage additionally **never reads `snapshot.participants`** at all — asserted by a Vitest case, not merely intended (derived requirement 14). So the mixed Player/Spectator roster this entry is about is not rendered anywhere on the Audience Display, at any state, and 4.5 changes nothing about that. Trigger unchanged. [server/internal/store/queries/answers.sql (GetLeaderboard), web/src/features/display/leaderboard-stage.tsx]
  - **Checked by story 4.6 (2026-08-14): NOT triggered, for exactly the reason 4.5 was not.** The winner takeover reads **only** `snapshot.leaderboard`, whose source `GetLeaderboard` filters `role = 'player'` **in SQL**, so a Spectator can never reach this surface no matter how it renders — and this stage never reads `snapshot.participants` or `participantCount` at all, asserted by a Vitest case rather than trusted (`winner-stage.test.tsx`, "renders nothing from snapshot.participants"). Worth one line even though the reasoning is 4.5's verbatim, because this is the **last** Audience Display stage Epic 4 produces: with 4.6 shipped, no stage on the display renders `ListParticipants`' role-blind roster at any state, so the epic closes without firing this entry once. Trigger unchanged — it now needs a surface outside the Audience Display entirely, or a new one added to it. [web/src/features/display/winner-stage.tsx]

## Deferred from: code review of 3-1-run-the-game-live-control-state-machine (2026-08-04)

- Control mutations discard the REST response snapshot; the panel's advance depends on a single WS frame — every control action is `api<void>`, throwing away the snapshot the backend deliberately returns, and the hub drops a full/slow connection's frame (8-slot queue) with no redelivery. A dropped frame leaves the organizer's panel on stale state (a re-press produces a spurious 409); a dropped `finished` frame is terminal until a manual reconnect, since finished never re-broadcasts. Deferred: this is the established single-source-of-truth WS posture from 2.3 (open-lobby behaves identically), the drop window is small at pilot scale, and applying the REST snapshot into the module-level socket store (or refetch-on-settle) is an architecture change deserving its own decision. Revisit trigger: **the first real-world report of a stuck control panel**, or **Epic 4** (audience displays raise the cost of a stale terminal state) — apply the returned snapshot into the socket store, or add a snapshot version enabling safe refetch. [web/src/features/live/control-page.tsx:55-59, web/src/lib/use-game-socket.ts, server/internal/ws/hub.go:172-178]
  - **Re-triaged at 4.1's code review (2026-08-09): TRIGGERED, re-deferred, and now worse than when it was written.** Story 4.1's dev pass missed this entry entirely (it enumerated three Epic-4 entries and there were ten). The Epic-4 half of the trigger has fired, and 4.1 adds a case the original text did not anticipate: `display-controls.tsx`'s reduce-motion mutation is `api<unknown>` and discards the same returned snapshot, but it drives a **controlled checkbox** — so a dropped frame does not merely leave a panel un-advanced, it leaves a control silently displaying the opposite of what is persisted, with no error and no self-heal until the next click or reconnect. Re-deferred because the fix is the same single architectural change for all seven mutations and does not belong in a shell story. See the dedicated entry under 4.1's code review below. Trigger unchanged.
  - **Explicitly STILL OPEN after story 3.11 (2026-08-10) — said out loud because a later reader will assume otherwise.** 3.11 fixed the 🔴 dead-control-panel entry under 4.1's review, which lives two entries away from this one, is about the same component, and produces a superficially similar "the panel is stuck" report. It did **not** touch this one: all seven mutations are still `api<void>`/`api<unknown>` and still discard the snapshot the backend returns, and 3.11's scope boundaries forbade changing that. **The two are distinguishable by symptom, and that is the useful test:** 3.11's defect issued **no request at all** (nothing in the Network tab, no banner, no console message); *this* defect issues the request, gets a 200 or a 409, and leaves the panel on stale state — or, on the reduce-animations checkbox, silently displaying the opposite of what is persisted. If a stuck panel is reported with a request in flight, it is this entry, not the one 3.11 closed. Trigger unchanged.
  - **Re-triaged at story 4.5 (2026-08-12): TRIGGERED again, re-deferred, and the count restated plainly.** 4.5 adds an **eighth** broadcasting write path (`handleShowLeaderboard`) — seven in `control.go` plus `display.go`'s `handleUpdateDisplaySettings`, which the 4.1 entry above already numbers seventh — and **no new `api<void>` call site at all**: the "טבלת התוצאות" secondary CTA fires through the *existing* `action` mutation, so `control-page.tsx` still has exactly two, and the CTA inherits 3.11's single-flight guard and the existing banner with no new state. *(Both counts were originally written as "seventh" and "an eighth `api<void>` call site", reproduced unchecked from this story's own Task 12 text; corrected at the code review of 2026-08-12, which counted them.)* The fix remains one architectural change covering all of them (have the mutations consume their 200 response snapshot instead of discarding it) and does not belong in a stage story. **The one thing that is genuinely new, and the reason this is worth a line rather than a tally:** the Leaderboard is the first state where a dropped frame can strand *the room's screen* while the organizer's panel has already advanced. Every prior instance stranded the panel, which one organizer sees; this one strands a projector, which everybody sees. Trigger unchanged. [server/internal/httpapi/control.go (handleShowLeaderboard), web/src/features/live/control-page.tsx]
- Broadcast `seq` is assigned at `Broadcast()`-call time, not DB-commit order — two concurrent control-panel actions (e.g. two organizer tabs) that commit their guarded UPDATEs in order A→B can have their goroutines call `hub.Broadcast` in order B→A (descheduling between the engine's write and the handler's broadcast); the *older* snapshot then receives the *higher* seq, and the client's stale-drop guard (`use-game-socket.ts`, `seq < lastSeq`) accepts it as newer — every viewer regresses to a stale state and sticks there, since a `finished` game never broadcasts again to self-correct. The WS *connect* path already guards this exact class of race (`seqBeforeBuild`/`seqAfterRegister` in `handler.go`); the five REST control handlers added by this story have no equivalent — `Broadcast` assigns `seq` under its own lock, independent of when the underlying transaction actually committed. Same family as 2.4's "concurrent joins can broadcast lobby snapshots out of order" (deferred 2026-08-03), now landing on terminal states where self-healing doesn't exist. User decision (2026-08-04): defer rather than patch now — the race requires two organizer sessions racing the same game, effectively unreachable at pilot scale (one organizer, one tab, per game). ~~Revisit trigger: **before Epic 4** (audience displays make a stuck stale state visible to a whole room, not just one organizer tab)~~ or **the first report of a control panel stuck on a stale state** — fix by deriving `seq` from something commit-ordered (e.g. `games.updated_at`/a DB sequence returned by the guarded UPDATE and threaded through to `Broadcast`) rather than assignment-order, or serialize broadcast-after-write per game. [server/internal/ws/hub.go:153-161, server/internal/httpapi/control.go, server/internal/ws/handler.go]
  - ~~**Re-triaged by story 4.1 (2026-08-09): recommendation is re-defer.** … story 4.1 adds no new writer — the display is strictly a reader, so the number of racing writers is unchanged by Epic 4 … the race still needs two organizer sessions acting on one game concurrently.~~ **Both premises were wrong; corrected at 4.1's code review below.**
  - **Re-deferred at 4.1's code review (2026-08-09, Avraham's decision), with the reasoning corrected.** The dev's re-triage above argued the race still needs two organizer sessions because "4.1 adds no new writer". Review falsified both halves: `handleUpdateDisplaySettings` **is** a new broadcasting writer (the seventh), and `DisplayControls` renders *inside* the live control panel next to the transition CTAs, with neither disabling the other — so **one organizer in one tab is now enough**. Press "next question", tick "הפחת אנימציות" a moment later, and two goroutines race `hub.Broadcast`; the older snapshot can take the higher `seq`, the client's `seq < lastSeq` guard accepts it, and the room regresses to a stale stage with no self-heal (a re-press answers 409; on `finished` it is permanent). **Still deferred**, on the honest grounds rather than the wrong ones: the window is the few milliseconds between two commits, it takes a deliberate press-then-tick inside that window, and the fix is a `ws`/`control.go` redesign that belongs in its own story rather than riding on a shell. 4.1's code review did add the in-flight guard on the checkbox itself (`display-controls.tsx`'s `submittingRef`), which removes the *self*-race of a double-clicked toggle but not the cross-race with a transition. **New revisit trigger: a second organizer surface (a co-host, a second tab as a supported workflow), the first report of a stuck stale state, or Epic 4 shipping to a real room — whichever comes first.** When it is implemented, see the `updated_at` entry under 4.1's code review below: `games.updated_at` is no longer a valid commit-ordered source. [server/internal/httpapi/display.go, web/src/features/live/control-page.tsx]
  - **Explicitly STILL OPEN after story 3.11 (2026-08-10).** 3.11 was frontend-only and changed **zero `.go` files** — nothing in `server/internal/ws/**` or `server/internal/httpapi/control.go` was touched, `seq` is still assigned at `Broadcast()`-call time rather than commit order, and the client's `seq < lastSeq` guard in `use-game-socket.ts` is unchanged. Recorded because 3.11 sits adjacent to this entry in two ways that invite the wrong conclusion: it is about the same control panel, and it too was reachable by one organizer in one tab. **The symptoms differ:** this defect makes the room *regress to a stale stage and stick there* after a real state change; 3.11's made the CTA *issue no request at all*, with the panel and the display both frozen on the correct-but-not-advanced stage. Trigger unchanged.

## Deferred from: code review of 3-2-question-delivery-to-every-phone (2026-08-04)

- `PlayerRecipients` is unscoped by `organizerID` on `ControlEngine` — authorization-by-convention, relying on the caller having already validated ownership via the preceding state transition. Mirrors the pre-existing `buildSnapshot`/`ListParticipants` pattern; not a new risk surface introduced by this diff, but the pattern is now used twice. Revisit if `PlayerRecipients` or a sibling unscoped method ever gains a direct HTTP-reachable call site. [server/internal/httpapi/control.go, server/internal/game/engine.go]
- Question text and MCQ options are not bidi-isolated, unlike every digit token in the same templates — organizer-authored Latin/mixed content (e.g. "C++", English question text) can render direction-scrambled in RTL. Deliberate, documented tradeoff per this story's Dev Notes ("free-form sentence content never has [isolation]"), not an oversight. Revisit if organizers author non-Hebrew question/option content in practice. [server/internal/wa/messages_he.go]
- The displayed time limit doesn't account for dispatch/delivery latency — a slow burst under retry/backoff could show "20 seconds" with meaningfully less time actually remaining once answer-cutoff enforcement lands. Inherent to the pre-existing async `Dispatcher` model, unchanged by this story per its own AC-2 note. Revisit at story 3.3 (answer-cutoff enforcement) if delivery-lag complaints surface. [server/internal/wa/messages_he.go, server/internal/wa/dispatch.go]
- `dispatchQuestionOpened`'s recipient-lookup-failure WARN uses the package-level `slog` instead of an injected/testable logger — matches every other `slog.Info`/`slog.Warn` call already in `control.go`; not a new departure introduced by this diff. [server/internal/httpapi/control.go]

## Deferred from: code review of 2-3-open-the-lobby-and-watch-it-live (2026-08-03)

- `role=display` authenticates with a full organizer session — whatever machine drives the public display must carry the organizer's `wc_session` cookie, which authorizes `POST /open-lobby` and every game mutation; after authentication the role parameter gates nothing (it is only logged). A projector/shared screen holding write-capable credentials is a real exposure the role query param appears to address but does not. No display UI consumes `role=display` yet — the WS protocol accepts it so the wire contract is ready. ~~Revisit trigger: **Epic 4** (audience display shell, story 4.1) — design display auth there (e.g. a scoped read-only display token or signed display URL) instead of reusing the organizer session.~~ [server/internal/ws/handler.go:44-65]
  - **Re-triaged by story 4.1 (2026-08-09): re-deferred, trigger re-pointed.** The trigger fired and was examined. Building a scoped display token in 4.1 would have been the story overruling its own spec: epic AC-1 and `architecture.md#Authentication-Security` both mandate the session-cookie approach in as many words ("same-origin route opened from an authenticated dashboard session — inherits the session cookie; no pairing flow"), and EXPERIENCE.md's Interaction Primitives (A12) rule out a pairing flow explicitly. What is genuinely true and worth recording: in every pilot scenario (Flow 1) the projector machine **is the organizer's own laptop**, so the credential never leaves their hands, and story 4.1 verified in a real browser that an unauthenticated display window self-heals to `/login` rather than lingering. The exposure only becomes real on a machine the organizer does not control. **New revisit trigger: the first deployment where the display runs on a machine the organizer does not physically control** (a venue PC, a shared/kiosk screen, a remote viewer) — *not* a story number. See also the logout entry below, which fires under the identical condition.
- Live WS connections survive logout and session expiry — the `/ws` handshake authenticates once; the read/write loops never re-validate, and neither the hub nor the auth service can terminate connections for a revoked session, so an already-open socket keeps receiving snapshots after `POST /auth/logout` or session-row expiry until the tab closes. Logout does not actually cut off live game data on a shared machine. Pilot posture accepts it (snapshots are low-sensitivity lobby/game state; sessions are long-lived). ~~Revisit trigger: **when the display-auth design lands (Epic 4)**~~ or if session revocation becomes a security requirement — index hub connections by organizer/session and tear them down on logout, or periodically re-authenticate long-lived connections. [server/internal/ws/handler.go:117-121]
  - **Re-triaged by story 4.1 (2026-08-09): re-deferred, merged with the display-auth entry above.** Same outcome and same reasoning: the condition that makes this matter is a display on a machine the organizer does not control, which is exactly the entry above's new trigger. **These two must be fixed together or not at all** — terminating live sockets on logout is only meaningful once the credential can outlive the organizer's own machine. Story 4.1 confirmed the adjacent behavior is already correct: a *new* display window opened after logout is rejected at the handshake (401 before the upgrade) and redirects to `/login`; it is only an *already-open* socket that survives. **Trigger: identical to the entry above.**

## Deferred from: code review of 3-3-answer-intake-with-immediate-acknowledgment (2026-08-04)

- `handleTextOrAnswer`'s unexpected-error path logs the raw `err.Error()` at WARN — pre-existing pattern, not introduced by this story (`handleJoin`/`handleRename`'s default-error paths already do the same). An unclassified store/DB error could in principle embed parameter values, in tension with the rest of `wa` package's phone-digest-only logging posture. Revisit trigger: if/when error classification is generalized across the router (a typed-error effort touching all three default-error paths at once, not just this story's new call site). [server/internal/wa/inbound.go:221, server/internal/wa/inbound.go:294, server/internal/wa/inbound.go:318]

## Deferred from: code review of 3-4-grading-pipeline-mcq-and-exact-match (2026-08-05)

- Every SQL artifact this story introduces has zero automated coverage — the rewritten `RevealCurrentQuestion` join, the new `CountUngradedAnswersForCurrentQuestion` query, the extended `RecordAnswer` INSERT column list, and migration 00011 are exercised by nothing in CI. All four new engine/httpapi tests drive `stubStore`/`stubControlEngine`, so deleting the `NOT EXISTS` clause, inverting `stage IS NULL` to `stage IS NOT NULL`, breaking the `q.position = g.current_question_position` join, or never applying the migration at all would leave the entire suite green. Deferred: this is the project's established and documented testing standard since 2.1 (no real-DB unit tests; the throwaway `cmd/e2escratch` harness is the verification, and it did run clean for this story per the Debug Log) — changing it is a project-wide test-strategy decision, not a story-3.4 fix. Note this is exactly the class of gap that let both of this review's highest-severity findings through to review. Revisit trigger: **Epic 4**, or the first grading/reveal defect that reaches a real game — introduce a Postgres-backed integration test tier (testcontainers or a CI service container) covering the guarded UPDATEs and the migration chain. [server/internal/game/engine_test.go, server/internal/httpapi/control_test.go, server/internal/store/queries/]
  - **Re-triaged at 4.1's code review (2026-08-09, Avraham's decision): TRIGGERED, re-deferred, trigger re-pointed off "Epic 4".** This is the head of a four-entry family (3.4 here, 3.7 and 3.10 below, plus 00014's NULL path) that all point at the same missing tier and all named Epic 4. Epic 4 has now started and the gap grew: story 4.1's `UpdateGameDisplaySettings` is one more piece of hand-written SQL whose `AND organizer_id = $2` cross-tenant guard can be deleted with the whole Go suite staying green, and whose column the only real exercise (`cmd/e2escratch`) verified once before being deleted per convention. Re-deferred because building the tier is its own story and Epic 4 is mid-flight. **New trigger for the whole family, replacing "Epic 4": the first scoring, grading, results or settings defect that reaches a real game, or the first `sqlc`/migration change made by someone who did not write the original query.** Numbering an epic was the mistake — the epic arrived and nothing about the risk actually changed.
  - **Re-triaged at story 4.4 (2026-08-12): TRIGGERED on the second clause, re-deferred with the family, and the gap is stated plainly rather than gestured at.** 4.4 adds **two hand-written queries** to `queries/answers.sql` — `ListAnswerCountsByResponse` (`GROUP BY a.response`) and `CountCorrectAnswersByQuestion` (`count(*) FILTER (WHERE a.is_correct)`) — written by someone who did not write the originals, which is exactly the second half of the family trigger above. **What IS covered:** six new `engine_test.go` cases pin the Go side hard — the state gate (`question_open` and `question_closed` both assert `Reveal == nil` **and** that neither store method was called, demonstrated red against a build with the gate removed), the index-alignment and zero-fill of `OptionCounts`, the dropping of out-of-range responses (`"0"`, `"5"`, `"ג"`), the free-text branch, and error propagation from either read. **What is NOT covered, precisely:** those six tests drive `stubStore`, so **deleting `GROUP BY a.response`, inverting `FILTER (WHERE a.is_correct)` to `FILTER (WHERE NOT a.is_correct)`, or pointing either query at the wrong column would leave the entire Go suite green.** The only thing that has ever executed this SQL is 4.4's throwaway `cmd/e2escratch` harness, which ran clean three times (measured MCQ split `[3 4 2 3]` matching the intended split element for element, `sum(optionCounts) == answeredCount == 12`, `correctCount == 4`, free-text `correctCount == 3` against 5 answers) **and has since been deleted per the 2.1–4.3 convention.** That is the family's whole thesis, demonstrated once more: the strongest evidence this story produced about its own SQL now exists only as prose in a story file. **This story does not build the tier**, for the reason 4.1's re-triage already gave — it is its own story, Epic 4 is mid-flight, and 4.4 is a stage story with a deliberately narrow backend scope (a payload type, two reads, one branch). Family trigger unchanged. [server/internal/store/queries/answers.sql, server/internal/game/engine_test.go]
  - **Re-triaged at story 4.5 (2026-08-12): TRIGGERED, and on a strictly sharper case than 4.4's. Re-deferred with the family; the gap is stated plainly.** 4.5 adds **one new query** (`ShowLeaderboard`) and — the sharp part — **modifies two existing guarded writes**: `OpenNextQuestion`'s guard widens from `state = 'revealed'` to `state IN ('revealed','leaderboard')`, and `FinishGame`'s `state IN (…)` list gains `'leaderboard'`. This is the family trigger's *"the first `sqlc`/migration change made by someone who did not write the original query"* case, and widening a `WHERE state IN (…)` **in the wrong direction is a silent correctness bug**: it would let a transition fire from a state it must not, e.g. skipping a live question. **What the tests do and do not cover, precisely:** the Go tests drive `stubStore`, so they pin the **engine's** guards (`ShowLeaderboard` rejects **six** states without writing — `draft`, `lobby`, `question_open`, `question_closed`, `leaderboard`, `finished`, asserting the call counter as well as the error; `NextQuestion` accepts `revealed`/`leaderboard` and rejects the other five; `StopGame` accepts four) but they **never execute the SQL text**. The widened `WHERE` clauses themselves are exercised only by the local Go E2E harness in Task 13, which runs against a real Postgres and is **deleted after use** — so it leaves no standing regression coverage, which is the whole substance of this family. 4.5 does not build the tier for the same reason 4.4 did not: a real-DB test tier is an infrastructure decision (CI Postgres service, fixture lifecycle, migration seeding) that a stage story should not make unilaterally. Trigger unchanged. [server/internal/store/queries/games.sql, server/internal/game/engine_test.go]
- Bank-imported `accepted_answers` bypass the only trimming in the system — `ImportPackageQuestions` is a column-for-column `INSERT ... SELECT pq.accepted_answers` that never passes through `validateQuestion`'s `strings.TrimSpace`, and `questions_type_shape` checks cardinality but not content. A seed row with a leading/trailing space in an accepted answer makes that question ungradeable: every participant is marked incorrect, silently, and Reveal still succeeds because the rows *are* graded — just wrong. Pre-existing: `ImportPackageQuestions` predates this story; story 3.4 only makes the consequence visible by being the first code to compare against those values. Related evidence that Exact-only is already lossy against shipped content: seed row `00005_question_packages.sql:53` stores `ק"נ` with an ASCII quote while Hebrew keyboards emit U+05F4 gershayim `״` — that specific class is explicitly Story 3.5's, but the untrimmed-import path is not covered by that deferral. Revisit trigger: **Story 3.5** (natural home — it is already touching accepted-answer normalization) — either `SELECT array(SELECT btrim(x) FROM unnest(pq.accepted_answers) x)` in the import, or a `CHECK` on `package_questions`. [server/internal/store/queries/packages.sql:25-33, server/internal/httpapi/questions.go:141-145] **[REVISITED at story 3.5 code review, 2026-08-05 — partially closed.]** The gershayim class is now closed: `grading.Normalize` deletes U+0022 and U+05F4 alike (both are category Po and both are treated as intra-word marks), so `ק"נ` and the keyboard spelling normalize to the same token and match at distance 0 through the Fuzzy stage. The untrimmed-import class is *mitigated but not fixed at source*: `Normalize` trims via `strings.Fields`, so Fuzzy now rescues an accepted answer with stray whitespace that Exact still misses — the `ImportPackageQuestions` query itself is unchanged and a leading/trailing space still defeats the Exact stage. Remaining work is the `btrim` on import or the `CHECK` on `package_questions`, unchanged from the original entry. **[NOTE added at story 4.4, 2026-08-12 — the remaining half is now VISIBLE, which is new information about an unchanged defect.]** Until 4.4, an untrimmed accepted answer was invisible: it lived in the DB and cost a participant an Exact match they should have had, silently. 4.4's reveal stage is **the first code anywhere that renders an accepted answer to a room** — `reveal.acceptedAnswer` is `accepted_answers[1]` verbatim, painted at 64px inside the `stage-answer-card`. So a leading or trailing space in a bank-imported answer now shows up as a **visibly misaligned card on a projector**, in front of an audience, with no trimming anywhere on the path (the Go side does no trimming, and the card's `<span>` renders the string as given). Two things follow. (1) The defect's *cost* went up without its *cause* changing — this is a note on the existing entry, not a new entry, exactly as this file's convention requires. (2) There is now a **cheap detection channel** that did not exist before: a misaligned reveal card is a symptom an Organizer will notice and report, where a silently-wrong grade was not. Remaining work and revisit trigger both unchanged (`btrim` on import, or a `CHECK` on `package_questions`). [web/src/features/display/reveal-stage.tsx, server/internal/store/queries/packages.sql:25-33]
- Rolling back migration 00011 while the 3.4 binary is still live silently discards 100% of inbound answers — the Down migration drops `stage`/`is_correct`, but the 3.4 binary's `RecordAnswer` names both columns explicitly, so every insert fails with `42703 undefined_column`, which `store.RecordAnswer` does not map; participants get the generic Help reply and their answers are never recorded. Inherent to any additive-column rollback and not a defect in this diff, but worth recording because `Down` is the only tool an operator has for the ungraded-rows problem this review patched, which makes it an attractive and very wrong move under pressure. Revisit trigger: **whenever a rollback runbook is written** — document "roll back the binary first, then the migration" as the required order. [server/migrations/00011_answer_grading.sql:16-20]
- ~~No `web/`, `strings.he.ts`, or `messages_he.go` changes in this story~~ **closed in 3.6 (2026-08-06).** `control-page.tsx`'s error banner now distinguishes a `GRADING_INCOMPLETE` (409) response from a generic conflict — a new `strings.he.ts` string (`live.gradingIncomplete`, "עדיין בודקים חלק מהתשובות. נסו שוב בעוד רגע.") replaces the generic `actionConflict` copy when `action.error.code === 'GRADING_INCOMPLETE'`. The Reveal ("גלה תשובה") button itself stays enabled, deliberately, per 3.1's documented focus-retention decision (see the comment directly above the `<Button>` in `control-page.tsx`) — the affordance this entry asked for is message-copy distinguishing "still grading" from "something else changed," not a disabled/waiting state; a disabled button was considered and rejected again here for the same reason 3.1 rejected it. `messages_he.go` (participant-facing WhatsApp copy) remains untouched — only the Organizer-facing dashboard gained new copy. Original text below for the record.<br>Original: flagged here at the story's own explicit request (Dev Notes: "Flag this scoping decision in this story's code review for a `deferred-work.md` entry"). Review agrees the scoping is correct, not an oversight: grading is 100% synchronous through 3.4, so `GRADING_INCOMPLETE` is unreachable by a real organizer and there is no state for a "waiting for grading" indicator to ever display. Building disabled/waiting UI now would be speculative work for a condition the code cannot produce. Revisit trigger: **Story 3.6** (the AI stage's ~5s latency is what first makes `GRADING_INCOMPLETE` genuinely reachable by an organizer clicking quickly) — add the disabled/waiting affordance to the control panel's "גלה תשובה" button then. [web/src/features/live/control-page.tsx, server/internal/httpapi/errors.go]
- ~~Residual rolling-deploy window leaves permanently-ungraded answer rows~~ **closed in 3.6 (2026-08-06, revised the same day by 3.6's code review).** `FailCloseOrphanedAnswers` (queries/answers.sql, `store.FailCloseOrphanedAnswers`) fail-closes every `stage IS NULL` row older than `game.OrphanAnswerAge` (5 minutes) to `is_correct=false, stage='fuzzy'`, logging a WARN when it recovers any. This is the "force-reveal escape hatch" this entry anticipated, applied generally rather than rolling-deploy-specific: it covers any pending-AI row abandoned by a crashed or replaced process, which 3.6's async design makes a routine occurrence rather than an edge case. **The as-delivered version of this sweep did not actually close the entry** — it was unscoped (`WHERE stage IS NULL`, no age filter) and ran only once at boot, which is wrong in both directions for the two-instance topology this entry is entirely about: the booting instance would fail-close rows the *outgoing* instance was still legitimately grading for an open question, and it would run *before* that instance died, so the rows it existed to catch were created after the only sweep it would ever perform. 3.6's code review caught this; the age filter plus a one-minute ticker plus a shutdown drain of in-flight grades (`game.Engine.WaitForGrading`) are what close it. Revisit trigger: if instances are ever run at a scale where a full-table `stage IS NULL` scan every minute matters, index it or scope the sweep by game. Original text below for the record.<br>Original: `store/migrate.go:23-24` documents that "zero-downtime redeploys briefly run two instances, and both boot through this path." In the seconds between the new instance applying 00011 and the old 3.3 instance draining, the old binary keeps inserting with its pre-3.4 column list; `is_correct`/`stage` default to NULL, `answers_grading_shape` is satisfied by `(NULL,NULL)`, the write succeeds, and the participant receives a normal ack. Those rows are created *after* 00011's Up-migration backfill runs, so the backfill does not reach them, and since there is no `UPDATE answers` statement anywhere in the codebase they can never be graded — the question they belong to can never be revealed. User decision (2026-08-05): backfill the certain case (pre-existing 3.3 rows) and accept this narrow window rather than timing around it — a grace-period constant in the SQL guard is a guess about deploy duration that will silently go wrong, and a two-phase NOT NULL migration is not worth the deploy-ordering burden while the gate is still vacuous scaffolding. Reviewer note: tstamp-scoping the gate (`received_at >= goose_db_version.tstamp`) does **not** help here — the overlapping instance's rows postdate the migration timestamp. Revisit trigger: **Story 3.6** — that is when async AI grading makes the gate load-bearing rather than vacuous, so it has to be genuinely correct then; close the window with a two-phase `NOT NULL` migration, or give the organizer a force-reveal escape hatch (which would also cover the "no `UPDATE answers` path exists" dead end). [server/migrations/00011_answer_grading.sql, server/internal/store/migrate.go:23-24, server/internal/store/queries/answers.sql]

## Deferred from: code review of 3-5-hebrew-fuzzy-matching (2026-08-05)

- `answers.stage` cannot distinguish "the fuzzy stage accepted this" from "the fuzzy stage was consulted and declined" — story 3.4 established "graded means the currently-implemented pipeline ran to completion", and 3.5's AC-3 deliberately extends that so a total miss records `stage='fuzzy', is_correct=false`. The consequence is that NFR-9's audit trail, the exact mechanism the threshold's own `[ASSUMPTION]` comment names as how a mis-calibration would surface, cannot answer "show me what fuzzy accepted" — `WHERE stage='fuzzy'` returns both outcomes, separable only by `is_correct`, which is the thing under dispute. Compounded across the migration boundary: rows backfilled by `00011_answer_grading.sql:27-34` label a free-text miss `'exact'` while post-3.5 rows label the identical outcome `'fuzzy'`, and no migration backfills them. Story 3.6 relabels every free-text miss again as `'ai'`, making the history three-way inconsistent. Deferred: the behavior is explicitly specified by AC-3 and called out in Dev Notes as intentional, so changing it is a pipeline-design decision, not a 3.5 fix. Revisit trigger: **Story 3.6** — that is when the third relabelling lands and when an async AI verdict makes a separate "stage outcome" representation (e.g. a `matched_stage` distinct from `last_stage_run`) genuinely worth its cost. [server/internal/game/answers.go:171-174, server/migrations/00011_answer_grading.sql:27-34]
- Nothing persists the evidence behind a fuzzy verdict — `GradeFuzzy` returns a bare `bool`, so which accepted alternative matched, at what edit distance, against what threshold, and what the two normalized forms were are all discarded. When a participant disputes a grade, the row holds only `stage='fuzzy'`, `is_correct`, and the raw response; reproducing the verdict means re-running `Normalize` by hand over characters that are invisible in the DB, in logs, and in any organizer view — and the surprising verdicts are exactly the ones driven by those invisible characters. The threshold comment names "Organizer-reported wrong grades (NFR-9)" as the tuning mechanism while the story persists none of the data tuning would need. Deferred: NFR-8's logging restraint and the story's own "do not add per-answer logging" guardrail make this a deliberate scope boundary, and no AC asks for it. Revisit trigger: **the first real organizer grade dispute**, or Story 3.6 (an AI verdict has strictly more to explain than a distance, so the two want one design). [server/internal/grading/fuzzy.go:74-83]
- Hebrew Presentation Forms (U+FB1D-FB4F, e.g. U+FB2E ALEF WITH PATAH, U+FB2A SHIN WITH SHIN DOT) and the Yiddish digraphs U+05F0-U+05F2 survive `Normalize` as distinct runes — it handles exactly two classes, U+0591-U+05C7 and the five final letters. A precomposed form never equals the base letter a phone keyboard emits, so each one costs a full edit against a response no participant could type differently. Notable because CI's own copy-centralization gate explicitly greps the FB1D-FB4F range on the stated grounds that copy "pasted from a PDF" arrives in that form — the codebase already knows this happens, and the seed pack's religious-text subject matter is exactly the content organizers paste. Deferred: the clean fix is Unicode compatibility normalization (NFKC), which means adding `golang.org/x/text` and thereby re-opening the story's "pure Go, no external dependency" constraint — a dependency decision, not a mechanical fix. Revisit trigger: **the first organizer-authored question whose accepted answer nobody can match**, or whenever `golang.org/x/text` enters `go.mod` for another reason. [server/internal/grading/normalize.go:31-46, .github/workflows/ci.yml:47-49]
- `parseMCQOption`'s doc comment claims "any extra content ... fails to parse — FR-5 specifies a bare letter or digit", but the digit path is `strconv.Atoi`, which accepts `"+2"`, `"02"`, and `" 2"`-after-trim as option 2 while `"2."` correctly earns the format hint. Pre-existing since story 3.3 — the Atoi path is unchanged by 3.5 — but this story rewrote the surrounding comment and thereby re-affirmed a strictness the code has never had. Low impact (a participant sending `"+2"` is plausibly intending option 2 anyway, so the lenient parse is arguably the kinder behavior); the defect is the comment, not necessarily the code. Revisit trigger: **any story that next touches MCQ parsing** — decide whether to tighten the parse or correct the comment, and do it in one place. [server/internal/game/answers.go:61-70]

## Deferred from: code review of 3-6-ai-semantic-validation-fail-closed (2026-08-06)

- Migration 00013's Down block is unrunnable the moment one `'ai'` verdict exists — `ADD CONSTRAINT ... CHECK` validates existing rows, so from the first free-text answer graded by the AI stage, `goose down` past 00013 fails with 23514 and rollback is blocked without hand-written SQL (`UPDATE answers SET stage='fuzzy' WHERE stage='ai'` first, which is itself a silent grade rewrite). Deferred as pre-existing: this is the identical shape already recorded for migration 00012 in 3.5's review round, and the 3.4 entry above ("Rolling back migration 00011 while the 3.4 binary is still live...") already establishes that `Down` is a trap for this table generally. Revisit trigger unchanged from those entries: **whenever a rollback runbook is written** — document the required order (roll back the binary first, then the data relabel, then the migration) once, covering 00011/00012/00013 together. [server/migrations/00013_answer_grading_ai_stage.sql:12-13]
- The control panel's error banner multiplexes two mutations, so a stale error can mislabel an unrelated failure — the banner renders when `action.isError || stop.isError` but picks its message from `action.error` first (`gradingIncomplete`, then `anyConflict`). An organizer who gets a 409 `GRADING_INCOMPLETE` from Reveal and then hits a 500 from Stop is shown "עדיין בודקים חלק מהתשובות. נסו שוב בעוד רגע." while the real server error is never surfaced. Deferred as pre-existing in shape: `anyConflict` already had the same one-banner-for-two-mutations precedence problem before this story, which only added a third branch in front of it (note the naive fix of gating on `action.isError` is a no-op — React Query only holds `error` while `status === 'error'`). Self-healing in practice: the reset effect on `snapshot.state` clears both mutations as soon as the live state advances. Revisit trigger: **the first organizer report of a confusing error banner**, or any story that adds a fourth error branch here — at that point give each mutation its own banner or track which one failed last. [web/src/features/live/control-page.tsx:60-66, 147-155]
  - **A note, not a re-triage, from story 4.5 (2026-08-12): NOT moved, and said out loud so a reviewer does not read 4.5's new CTA as triggering it.** 4.5 adds a second *path* to the **same `action` mutation** (`fire('show-leaderboard')` beside the primary's `fire(primary.path)`) — deliberately, so it inherits the single-flight guard and the existing banner without new state. That is not a third mutation and not a fourth error branch: the banner still multiplexes exactly `action` and `stop`, and the ambiguity this entry describes is unchanged in both kind and degree. (4.5's story text attributes this item to 3.1's review; it is in fact from **3.6's**. Recorded here so the next reader is not sent hunting.) Trigger unchanged.

## Deferred from: code review of 3-7-scoring-with-speed-bonuses (2026-08-06)

- Answers landing in the close→reveal race window get `points_awarded = NULL` forever, and the player silently loses their points. `RecordAnswer`'s guard is a plain `SELECT` inside an `INSERT ... SELECT`; under READ COMMITTED that statement's snapshot is taken at statement start and takes no lock on the `games` row, so an insert whose SELECT observed `question_open` can still commit after `CloseCurrentQuestion` has committed. If it commits after `Reveal`'s `ListAnswersForScoring` snapshot, the row is simply not in `points`, the `UpdateAnswerPoints` loop never touches it, and it keeps `points_awarded = NULL` — which per migration 00014 means "not yet revealed". `GetLeaderboard`'s `points_awarded IS NOT NULL` predicate then drops it permanently, with no error, no log, and no reconciliation path. Scoped down by two existing guards: a pending-AI row (`stage IS NULL`) is caught instead by the in-transaction `NOT EXISTS` in `RevealCurrentQuestion`, which fails the reveal loudly rather than mis-scoring; only a synchronously-graded (mcq/exact/fuzzy) row hits the silent path. Deferred rather than patched because the clean fix — reading the answer set *inside* the reveal transaction — is a design change, not a mechanical one: `store` cannot call `game.AwardPoints` without inverting the dependency direction the architecture fixes ("game imports store, never the reverse"), so closing it needs either a callback threaded through the store method or the scoring computation moved behind a `store`-owned interface. The window itself is one INSERT statement wide and is the same race class already recorded for 3.4 ("Rolling deploy window leaves permanently-ungraded answer rows") and 3.6. Revisit trigger: **the first reported "my correct answer scored 0"**, or whenever the reveal path is next restructured. [server/internal/game/engine.go:288-296, server/internal/store/games.go:190-202, server/internal/store/queries/answers.sql (RecordAnswer)]
  - **Checked by story 4.5 (2026-08-12): NOT triggered by mechanism, but the CONSEQUENCE changed and that is worth recording once.** 4.5 does not touch `RecordAnswer`, its guard, or the close→reveal window, and adds no evidence about how often the race fires. What it changes is who notices: this story renders `GetLeaderboard`'s output **on a projector**, so an answer stuck at `points_awarded = NULL` is now a visibly missing score in a public ranking rather than a quiet discrepancy in one participant's WhatsApp thread. Severity of the *outcome* rose; likelihood, mechanism and evidence are all unchanged. Re-deferred without re-deriving anything. Trigger unchanged.
- The degraded `emptySnapshot` broadcasts an empty leaderboard that is indistinguishable from a legitimate "everyone scored 0". When `buildSnapshot` fails after `RevealCurrentQuestionAndAwardPoints` has already committed (transient pool error, 5s build timeout), `snapshotAfterCommit` falls back to `emptySnapshot`, which this story gives `Leaderboard: []LeaderboardEntry{}` — and `handleReveal` broadcasts that to every dashboard and returns it as the 200 body. The pre-existing degrade fields fail *visibly* (empty roster, `questionCount: 0`, `currentQuestion: null`); an empty leaderboard is the one field whose degraded value is a plausible real value, so it degrades silently into wrong data at the exact moment the room is watching the reveal. There is no periodic refresh — the comment at `engine.go:427-428` says the true state is picked up by "the next real read (a reconnect, or a future broadcast)", which for a reveal means the organizer's next action. Deferred because the two constraints conflict and neither is this story's to overturn: the story's Task 4 explicitly mandates the non-nil empty slice ("same 'never null on the wire' discipline as `Participants`"), and the TS mirror types `leaderboard` as non-nullable, so making the degraded value distinguishable (null, or an explicit `degraded: true` flag) is a wire-contract change spanning Go, TS, and Epic 4's consumers. Revisit trigger: **Epic 4** (`leaderboard-stage.tsx` is the first surface that actually renders this, and is where "no data" vs "all zeros" first becomes visible to a room). [server/internal/game/engine.go:436-445, server/internal/game/engine.go:508-518, server/internal/httpapi/control.go:170]
  - **Checked by story 4.1 (2026-08-09): NOT triggered — the trigger is `leaderboard-stage.tsx`, which is story 4.5.** The 4.1 shell renders no leaderboard. 4.1 does add one line to `emptySnapshot` (`DisplaySettings` carried from the row, so a degraded snapshot cannot silently flip the room's motion setting), which is purely additive and does not touch the leaderboard field or this entry's reasoning. **Story 4.5 must resolve this before rendering a leaderboard to a room.**
  - **Checked by story 4.4 (2026-08-12): NOT triggered — still `leaderboard-stage.tsx`, still story 4.5.** The reveal stage renders no leaderboard. Worth recording because 4.4 is the first story to *render* an `emptySnapshot`-shaped degraded frame at `revealed` and therefore looks adjacent to this entry: `snapshotAfterCommit` degrades a post-commit `buildSnapshot` failure to `emptySnapshot`, which carries `State = "revealed"` with `CurrentQuestion = nil`, and `handleReveal` broadcasts exactly that. **4.4's derived requirement 9 handles that frame honestly rather than plausibly, which is the opposite of this entry's complaint** — `currentQuestion === null` renders `strings.display.waiting` (visibly degraded, not a plausible-looking wrong value), and `currentQuestion` present with `reveal === null` renders the options **undecorated** rather than guessing an answer. Both are pinned by Vitest cases. So 4.4 is a data point *for* this entry's framing (a degraded value must be distinguishable from a real one) without touching the leaderboard field it is about. A Go test also states the adjacent property directly (`TestEmptySnapshotCarriesNoReveal`): `emptySnapshot`'s `CurrentQuestion` is nil, so it carries no reveal by construction — no field was added to guarantee that. Trigger unchanged. [web/src/features/display/reveal-stage.tsx, server/internal/game/engine_test.go]
  - **TRIGGERED by story 4.5 (2026-08-12) — this entry named `leaderboard-stage.tsx` as its trigger and that file now exists. RESOLVED for the display half; the wire ambiguity is re-deferred.** The entry (and 4.1's follow-up, *"Story 4.5 must resolve this before rendering a leaderboard to a room"*) assumed the fix had to be a **wire-contract change** — a `null`, or a `degraded: true` flag, spanning Go, TS and all of Epic 4. **It did not.** The discriminator was already on the wire as a *correlation across existing fields*: `emptySnapshot` sets `Leaderboard: []` **together with** `QuestionCount: 0` and `CurrentQuestion: nil`, while a legitimate `leaderboard` frame always carries `questionCount ≥ 1` and a non-nil `currentQuestion` — because the state is only reachable from `revealed`, which requires an open question. So `leaderboard-stage.tsx` renders `strings.display.waiting` when `questionCount === 0 || currentQuestion === null`, and the genuinely-empty board (`leaderboard.length === 0` on an otherwise-healthy frame) gets its own honest copy, `display.leaderboard.noPlayers` — two different true sentences, both Vitest-pinned, plus two further cases pinning that **either** half of the correlation alone is enough. *(As first written only one of the two disjuncts was pinned — both existing cases drove the `currentQuestion === null` half, so deleting `questionCount === 0` from the guard left the whole suite green. The code review of 2026-08-12 added the missing case on both surfaces and moved the predicate itself into a single shared `isDegradedFrame` in `stage-props.ts`, because the stage's refusal to render such a frame and the memory hook's refusal to capture one had already drifted apart — the hook tested only the currentQuestion half.)* The memory hook refuses to capture a baseline from such a frame for the same reason. **What is NOT closed:** `Snapshot.Leaderboard` is still an empty non-nil slice in both the degraded and the genuinely-empty case, so the field **read alone still cannot tell them apart** — every future consumer inherits the trap and has to re-derive the correlation. **New trigger, replacing "`leaderboard-stage.tsx`":** *the next consumer of `Snapshot.Leaderboard` outside `leaderboard-stage.tsx`* — story **4.6's winner takeover reads the same field** and is the likely one. [web/src/features/display/leaderboard-stage.tsx, web/src/features/display/use-leaderboard-memory.ts, server/internal/game/engine.go (emptySnapshot)]
  - **TRIGGERED by story 4.6 (2026-08-14) — the predicted second consumer arrived, and it needed a DIFFERENT discriminator. Resolved for this consumer; the wire ambiguity is re-deferred a second time.** 4.5's re-pointed trigger was *"the next consumer of `Snapshot.Leaderboard` outside `leaderboard-stage.tsx`"*, naming 4.6's winner takeover as the likely one. It is. **The important finding is that 4.5's correlation does not carry over, and copying it would have failed silently in every game:** `isDegradedFrame` is `questionCount === 0 || currentQuestion === null`, and at `finished` the second disjunct is **unconditionally true on every real frame** — `games.sql`'s `FinishGame` resets `current_question_position` to 0 on every transition into the state (deliberately, per its own comment: a stale position would make `buildSnapshot` keep reporting a `currentQuestion` for a game that already ended), and `buildSnapshot` only populates `current` when the position is `> 0`. The shared helper would therefore have shown `display.waiting` on every finished game forever and never rendered a winner. `leaderboard` is not exposed to this because nothing moves the position on the way into it. **What `winner-stage.tsx` does instead:** `snapshot.questionCount === 0` **alone**, written locally rather than imported, with a comment citing this reasoning so nobody "simplifies" it back to the shared helper. `QuestionCount` is never reset (`ListQuestionsByGame`'s length regardless of state), and a game cannot start without at least one question, so it alone separates `emptySnapshot`'s degraded fallback from a real finished frame. **Demonstrated, not reasoned:** swapping `isDegradedFrame` in for the local check was run before the guard was written and took **9 of the 11 cases in `winner-stage.test.tsx` red at once**; the only two that stayed green were the degraded-frame case itself (which is why it cannot stand alone as coverage) and the privacy case, which asserts absence. The contrasting case — a healthy finished frame with `currentQuestion: null` and `questionCount: 5` renders the winner — is the one that actually pins the requirement, and it is in the suite. **So `Snapshot.Leaderboard`'s degraded ambiguity now has TWO state-specific discriminators in the frontend, which is exactly the cost this entry predicted every consumer would inherit** — and the second one is not a copy of the first, which is a stronger form of the same complaint than 4.5 could make. The field read alone still cannot tell a degraded board from a genuinely empty one. **New trigger, replacing 4.5's:** *the next consumer of `Snapshot.Leaderboard` outside `leaderboard-stage.tsx` and `winner-stage.tsx`.* There is no third consumer anywhere in Epic 4 — `finished` is the last stage — so this is likely **dormant** going forward unless a future epic adds a surface reading the board, at which point that story must re-derive the discriminator for **its own** state rather than copying either existing one. [web/src/features/display/winner-stage.tsx, web/src/features/display/winner-stage.test.tsx, server/internal/store/queries/games.sql (FinishGame)]
- No automated coverage for any SQL this story introduces, nor for the transaction itself. The riskiest code in the diff — a hand-rolled `Begin`/`defer Rollback`/N x `UPDATE`/`Commit`, plus three new statements including an aggregate over a LEFT JOIN — sits behind a `stubStore` method that cannot fail halfway. Nothing verifies that a mid-loop `UpdateAnswerPoints` failure leaves the game in `question_closed` (the entire atomicity claim in the method's own doc comment), that a `Commit` failure is reported, that `GetLeaderboard`'s LEFT JOIN really yields a 0-score row for a participant with no answers, or that putting `points_awarded IS NOT NULL` in the ON clause rather than the WHERE clause is what preserves those rows. Deleting the `role = 'player'` filter, inverting the `IS NOT NULL`, or never applying migration 00014 at all would leave the entire suite green. Deferred as the project's established and documented standard since 2.1 — identical in shape to the 3.4 entry above, and verified for this story only by the throwaway `cmd/e2escratch` harness, which did run clean per the Debug Log and has since been deleted. **Re-triaged at 4.1's code review (2026-08-09): TRIGGERED, re-deferred with the 3.4 entry above — see it for the family-wide new trigger.** Revisit trigger unchanged from the 3.4 entry: ~~**Epic 4**~~**, or the first scoring/reveal defect that reaches a real game** — introduce a Postgres-backed integration tier (testcontainers or a CI service container). Note this story's four mutation-proven test gaps are the same class of blind spot, and three of them are in pure Go code that a stub-based suite *could* have caught. [server/internal/store/games.go:180-206, server/internal/store/queries/answers.sql:177-211, server/internal/game/engine_test.go]
- Clock skew between instances defeats `received_at` ordering, and `seq` only breaks *exact* ties. `received_at` is `time.Now().UTC()` from whichever Go process handled the webhook — correct and deliberate (FR-7's authoritative receipt time, resolving 2.1's two-clocks item), but not monotonic across processes. `store/migrate.go:23-24` documents two concurrent instances as a routine operating mode during zero-downtime redeploys. The comparator falls through to `Seq` only when `ReceivedAt.Equal(...)` is exactly true, so a few milliseconds of inter-instance skew — or a backwards NTP step on a single instance — produces a strictly-ordered-but-wrong sequence in which the tie-break never engages, and the wrong participant receives `SpeedBonusFirst`. Migration 00010 scoped `seq` narrowly to "when two answers share a received_at at timestamptz resolution"; skew is a different failure than resolution, and story 3.7 is `seq`'s first consumer. Deferred because every fix is a real design choice with its own cost (Postgres `clock_timestamp()` at insert, trading FR-7's ingestion-clock semantics for a single DB clock; a monotonic sequence issued per game; or accepting it and documenting that bonus order is only as good as instance clock sync). Revisit trigger: **the first disputed speed bonus**, or whenever the deployment moves to more than one long-lived instance by design rather than transiently. [server/internal/game/scoring.go:37-42, server/internal/wa/webhook.go:297, server/migrations/00010_answers.sql:18-20]

## Deferred from: code review of 3-8-personal-results-after-reveal (2026-08-06)

- `ListAnswerResultsForQuestion`'s wrapper reads `pgtype.Bool`/`pgtype.Int4` without checking `.Valid` — `r.IsCorrect.Bool` and `r.PointsAwarded.Int32` both materialize Go zero values from a SQL NULL, so a NULL `is_correct` is indistinguishable from a genuine "wrong" and a NULL `points_awarded` from a genuine 0. The consequence is an outbound WhatsApp message, which is unrecoverable once sent. Deferred as pre-existing posture rather than a 3.8 defect: the sibling `ListAnswersForScoring` reads the same two columns the same way and documents why (Reveal's `CountUngradedAnswersForCurrentQuestion == 0` precondition plus the same-transaction points write), and the single documented case where the "IS NOT NULL means revealed" invariant does not hold — rows predating migration 00014 on an already-revealed question — was explicitly accepted by Avraham in the 3.7 code review on the grounds that no deployed game has passed a reveal, making the affected set empty in practice. Adding `.Valid` checks to one query and not its sibling would create a worse inconsistency than the gap itself. Revisit trigger: **whenever the 00014 acceptance stops holding** (i.e. the first deployed game that passes a reveal on pre-migration rows), or whenever a real-DB integration tier lands and can actually exercise the NULL path. [server/internal/store/answers.go:228, server/migrations/00014_answer_points.sql]
- Displayed base points are re-read from live game config rather than derived from the persisted award, and the `r.Points < base` case has no branch. `base = g.PointsPerCorrect; if r.Points > base { bonus = r.Points - base }` means a correct answer whose stored `points_awarded` is *less* than the configured value silently advertises the config number — a figure the leaderboard on the very next line of the same message never credited. Deferred because it is genuinely unreachable today and the review verified why rather than assuming: `AwardPoints` is the only writer and always emits `PointsPerCorrect + bonus` for a correct row, and `handleUpdateScoring` is `requireDraftGame`-guarded so scoring config cannot drift mid-game. (Note this specifically disproves the reviewing layer's initial claim that a mid-game scoring endpoint made it live.) The residual smell is architectural — the message displays a number it did not read from the row it is reporting on. Revisit trigger: **the moment scoring config becomes editable after `draft`**, or any change that lets `points_awarded` be written by something other than `AwardPoints`; at that point derive the split from the stored row or assert `base + bonus == r.Points` and fail loudly. [server/internal/game/results.go:133-139, server/internal/httpapi/games.go:261]
- Dispatch error-path tests signal before the code under test has made its decision, so they cannot fail. The stub sends on its `done` channel from *inside* the engine call, before returning the error; the test receives that signal and immediately asserts `len(dispatcher.Calls()) == 0` — sampling at a moment when the dispatch goroutine has not yet reached its `if err != nil` guard at all. Deleting or inverting the guard would leave the test green on essentially every run. Deferred as pre-existing rather than a 3.8 defect: `TestRevealDispatchSkippedOnResultsError` is a faithful copy of story 3.2's `TestStartGameDispatchSkippedOnRecipientsError`, which carries the identical flaw; the correct fix is to wait on a signal that orders *after* the decision (the dispatcher's own `done`, asserted absent within a bounded window) and it should be applied to both tests in one pass rather than diverging them. Also unfixed in the same file: neither of `dispatchAnswerRevealed`'s two guards has a test that can fail — no case drives `/reveal` with `State != "revealed"`, and none with `CurrentQuestion == nil`, which is precisely the input the decision-needed finding above shows to be reachable. Revisit trigger: **whenever either dispatch path is next touched**, or alongside the decision-needed snapshot-degradation fix, which needs the nil-`CurrentQuestion` test anyway. [server/internal/httpapi/control_test.go]
- Post-response dispatch goroutines are untracked by graceful shutdown, so a redeploy landing mid-dispatch silently drops every message they were about to enqueue. `srv.Shutdown` waits only for handlers; `go dispatchAnswerRevealed(...)` is spawned *after* the response is written and is joined by nothing. `drainAndStop()` then drains a queue that does not yet hold the result messages, `stopDispatcher()` cancels the workers, and `run` returns — so `main.go`'s own assertion that "every handler has returned, so the queue now holds everything they enqueued; this drain is the complete one" is false. Deferred as pre-existing: this has been true of `dispatchQuestionOpened` since story 3.2, and story 3.8 only adds a second instance of the same shape. Materially more likely than it looks for this project specifically, since Railway redeploys are a routine part of the workflow and the window is the full 5s dispatch budget. The fix is mechanical but cross-cutting — track post-response dispatch goroutines in a `sync.WaitGroup` and join it before `drainAndStop()` — and should cover both dispatch paths at once. Revisit trigger: **the first redeploy that visibly loses a live game's messages**, or whenever graceful shutdown is next revisited. [server/internal/httpapi/control.go, server/cmd/server/main.go:293-295, server/internal/wa/dispatch.go:127-131]
- The reveal-result burst and the next-question burst have no ordering guarantee, so a participant can receive "שאלה N+1..." before "לא נכון הפעם. התשובה: ...". Two independent detached goroutines with no sequencing: `dispatchAnswerRevealed` makes four sequential DB round trips before its first `Enqueue` while `dispatchQuestionOpened` makes one, so the later-triggered burst can win — and even after enqueue, `dispatchWorkerCount = 8` drains concurrently with no per-recipient ordering. Deferred because the underlying property is pre-existing (the dispatcher has never guaranteed ordering) and every fix is a real design choice: sequence per game, sequence per recipient, or resolve results synchronously enough to bound the gap. Product impact is real but bounded — the organizer's own click rhythm is normally seconds apart, well beyond the window. Revisit trigger: **Epic 4** (when the Audience Display makes reveal→next pacing tighter and more scripted), or the first report of results arriving out of order. [server/internal/httpapi/control.go, server/internal/wa/dispatch.go:19, 144-150]
  - **Checked at 4.1's code review (2026-08-09): NOT triggered — story 4.4 triggers it.** The trigger is about the Audience Display making reveal→next pacing "tighter and more scripted". The 4.1 shell has no reveal stage: `revealed` maps to the same text placeholder as every other state, so nothing about the organizer's rhythm changes yet. Story 4.4 (the reveal stage, "the answer marked") is the one that puts a scripted beat on the projector and makes an out-of-order WhatsApp burst visible against it. Trigger unchanged, but read it now as "story 4.4".
  - **TRIGGERED by story 4.4 (2026-08-12). Decided in writing: RE-DEFERRED, and the trigger is re-pointed off story numbers.** The trigger fired exactly as 4.1 predicted — the reveal now has a visible scripted beat on the projector, so the window in which a Participant can receive "שאלה N+1…" before "לא נכון הפעם. התשובה: …" is now a window in which the room is *watching the answer being marked*. **What actually changed, stated precisely:** the *consequence* got worse, the *mechanism* did not. Before 4.4 an out-of-order burst was confusing in a private chat; now it can contradict a shared screen, which is a stronger claim on a participant's attention than a message alone. **Why re-deferred anyway, and this is a decision rather than an inheritance.** (1) The window is bounded by the Organizer's own click rhythm, and 4.4 does not shorten it — the reveal stage is *static*, with no auto-advance and no timer; the room looks at the marked answer until a human presses "השאלה הבאה". Nothing in 4.4 makes the two bursts closer together than they already were. (2) The fix remains a dispatcher-ordering design choice (sequence per game, per recipient, or resolve results synchronously enough to bound the gap) — the same three options 3.8 recorded, none of which 4.4's scope or evidence narrows down. (3) 4.4's E2E measured nothing new about the ordering: its four scenarios are about the reveal *payload*, and the dispatcher never ran (`WHATSAPP_API_BASE_URL` pointed at a dead address, `recipient_count` aside). So this story adds a stronger reason to fix it and **zero new evidence about it** — re-deferring on unchanged evidence is the honest call, and pretending otherwise would be worse. **New trigger, replacing "story 4.4": the first report of a participant seeing the next question before their own result, OR any change that shortens the reveal→next gap (an auto-advance timer, a "reveal and continue" control, or a scripted stage sequence).** An auto-advance is the realistic one — it would collapse the Organizer's click rhythm, which is currently the only thing bounding this, and it is exactly the kind of feature a polished reveal stage invites.

## Deferred from: code review of 3-10-post-game-summary-on-the-dashboard (2026-08-09)

- The new `ListQuestionResponseStats` aggregate has zero repo-resident coverage, and the only thing that ever exercised it has been deleted. Every one of the nine handler tests stubs the query out wholesale, so the `LEFT JOIN` that makes a never-answered question appear, the `FILTER (WHERE a.is_correct)` NULL exclusion, the `GROUP BY q.id` functional dependency and the `g.organizer_id` scoping are all unverified by anything that will run again. `TestGameResultsQuestionStatsAreOrganizerScoped` is the sharpest illustration: it asserts the handler forwards a string, and would pass unchanged if the SQL dropped `AND g.organizer_id = $2` entirely — its own comment describes a property it cannot observe. Deferred as the project's documented standard since 2.1 and identical in shape to the 3.4 and 3.7 entries above; the throwaway `cmd/e2escratch` harness did run 33/33 clean per the Debug Log and has since been deleted per convention. Review re-derived the query's correctness statically instead and it holds. **Re-triaged at 4.1's code review (2026-08-09): TRIGGERED, re-deferred with the 3.4 entry above — see it for the family-wide new trigger.** Revisit trigger unchanged from the 3.7 entry: ~~**Epic 4**~~**, or the first results/scoring defect that reaches a real game** — introduce a Postgres-backed integration tier. [server/internal/store/queries/answers.sql:295-329, server/internal/httpapi/results_test.go]
- A question the game never reached and a question every player ignored render identically as `0 מתוך N` / `0%`, and the distinction is not merely unrendered but unrecoverable. `StopGame` is legal from `question_open`/`question_closed`/`revealed`, so an organizer who stops a 10-question game after Q3 gets seven rows reporting a false engagement collapse as fact — on the surface whose entire purpose is reviewing how the event went. `FinishGame` resets `current_question_position` to 0 (deliberately — a stale position would leak a `currentQuestion` into finished snapshots), so after the game ends nothing in Postgres records how far it got. Deferred because the story decides this explicitly and forbids all three obvious fixes: reconstructing progress from answer rows is a guess, a `reached` column is a migration this story forbids, and not resetting the position breaks story 3.1's snapshot fix. Recorded because two independent review layers flagged it as misleading without prompting, which is evidence the design decision has a real cost. Revisit trigger: **the first organizer who reads their own summary as a failure**, or whenever a migration lands on `games` for another reason — a `last_reached_position` column is one field. [server/internal/store/queries/games.sql:124-130, web/src/features/results/results-summary.tsx:154-178]
- The summary is a one-shot read that never refreshes, on a surface the code knows can be stale. The query has no `refetchInterval` and `finished` is terminal so nothing re-broadcasts; the SQL's own comment concedes that a game stopped from `question_open` "can absolutely leave some" ungraded rows in story 3.6's async AI path. Those answers count as answered but not correct, and `GetLeaderboard`'s `points_awarded IS NOT NULL` omits their points from scores — so an organizer who stops mid-question and looks immediately sees scores and a `correctCount` that the AI grader silently corrects seconds later behind their back. Substantially mitigated in practice by TanStack's defaults, which this project does not override for this: `staleTime` 0 means a remount refetches and `refetchOnWindowFocus` stays true, so switching tabs away and back repairs it. Deferred because the honest fix is a cue rather than a poll — the page cannot know grading is pending without a new field. Revisit trigger: **whenever the payload next gains a field**, or the first report of results that changed on their own. [web/src/features/results/results-summary.tsx:36-41, server/internal/store/queries/answers.sql:305-312]
- The games-list card is now the only state dispatch on that surface, and both of its branches lose something. A finished game's card leads to the results summary, which is correct and specified — but nothing in the UI then links to `/games/{id}`, so an organizer who wants to reread last week's questions, copy one, or check its scoring config has no route in but a hand-typed URL. The else-branch is worse and pre-existing: a game abandoned in `question_open`/`question_closed`/`revealed` will never become `finished` on its own, so it never gets the results link either, and it routes to an editor where every mutation 409s `GAME_NOT_EDITABLE` — those games belong at `/games/{id}/lobby`, where they can actually be finished. The new `finishedBadge` sharpens the asymmetry by giving `finished` a visible state cue while the live states still read as drafts. Deferred as pre-existing in shape and out of this story's scope. Revisit trigger: **Epic 4**, or any story that next touches the games list. [web/src/features/builder/games-list-page.tsx:58, web/src/features/builder/game-editor-page.tsx:137]
  - **Checked at 4.1's code review (2026-08-09): NOT triggered.** Naming "Epic 4" here was imprecise — nothing about an audience display touches the games list, and 4.1's scope boundaries forbid `games-list-page.tsx` outright (verified: no diff there). None of 4.2–4.6 touches it either. **Trigger narrowed to its second half: any story that next touches the games list** — drop the epic reference so a later Epic 4 story does not have to re-derive this.
- The `.tsx` Hebrew-comment gap was found, correctly judged out of scope, and then never written down. The Debug Log calls it a "Candidate for `deferred-work.md`" and `deferred-work.md` does not appear in the File List. Two pre-existing violations remain untracked (`scoring-editor.tsx:44` from story 1.4, `control-page.tsx:39,49` from story 3.1 — both verified present on `origin/main`, so neither is 3.10's regression), and CI greps only `*.go`, so nothing will catch the third one. The architecture's copy-centralization rule binds `.tsx` regardless of whether CI enforces it. Revisit trigger: **whenever the CI workflow is next edited** — extend the existing Hebrew gate to `--include='*.tsx'` with the two known files exempted, or fix them and add the gate clean. [.github/workflows/ci.yml:35-64, web/src/features/builder/scoring-editor.tsx:44, web/src/features/live/control-page.tsx:39,49]
- sqlc copies `queries/*.sql` comment blocks verbatim into `gen/*.sql.go`, which the Hebrew copy-centralization gate scans as a non-test `.go` file. This story's 20-line SQL comment now sits inside `gen/answers.sql.go` and passes only because it is English. Any Hebrew ever written into a `queries/*.sql` comment — a perfectly natural thing to do in a file CI does not scan — will turn the build red in a *generated* file the author never edited, pointing at a line they did not write. The gate cannot distinguish a comment from a literal, which is the same trap that cost story 3.8 a follow-up commit (`65fafe9`). Revisit trigger: **the first time this actually fires**, or whenever the gate is next edited — either exempt `internal/store/gen/` (safe: it is generated and contains no hand-authored copy) or note the rule in the SQL files' own header. [server/internal/store/gen/answers.sql.go:406-429, .github/workflows/ci.yml:49]
- `writeStoreError(w, err, "GAME_NOT_FOUND")` on the two post-game reads passes an error code those calls cannot produce and that would be wrong if they could. Both are `:many` queries and the store's own doc states a `:many` with no rows is not `ErrNoRows`, so the 404 branch is unreachable; the tests correctly pin 503 `DB_UNAVAILABLE` for generic failures. The trap is latent rather than live: if a future refactor makes either read a `:one`, the handler would answer 404 `GAME_NOT_FOUND` for a game it proved four lines earlier exists and is owned, and the frontend's 404 branch would render "game not found" and bounce the organizer to the games list. Deferred rather than patched because there is no obviously better code to pass and the churn is not worth it while the branch is dead. Revisit trigger: **whenever either read becomes a `:one`**, or a new error code is added to `errors.go`. [server/internal/httpapi/results.go:91, 96]
- Two small wire/render mismatches, both spec-prescribed and both harmless today. `questionStatsPayload.Type` ships on the wire and is rendered nowhere — a field nobody displays is a field nobody notices breaking; and because it is scanned as a raw DB `string` but typed as the closed union `QuestionType` on the TS side, a value outside the union would be a silent type lie (currently impossible: `questions_type_shape`'s CHECK constrains it to `mcq`/`free_text`). Separately, since `questions` deliberately declines `UNIQUE (game_id, position)` and `CreateQuestion`'s `max(position)+1` has no concurrency backstop, two rows sharing a position render as two rows both labelled with the same number — only one of which was ever asked, so the other compounds the never-reached-question entry above. Labelling by ordinal index rather than the raw column would fix the display half. Revisit trigger: **whenever the results payload next changes**, or if a duplicate position is ever observed in a real game. [server/internal/httpapi/results.go:18, web/src/features/results/results-summary.tsx:156-161, server/migrations/00003_games_questions.sql:19-21]
- The post-game results copy ships `[ASSUMPTION]`-marked and unconfirmed. EXPERIENCE.md's Host-microcopy table specifies this surface's *content* ("Final Leaderboard + per-question response rates on screen. No export") but no rows for its wording, so the entire `strings.results` block plus `gamesList.finishedBadge` was authored to that table's rules rather than transcribed from it — direct, terse, no apology, no emoji. Deferred at Avraham's call in the 3.10 code review: he will confirm or replace the wording after seeing the surface in a real browser, which is the same session as the outstanding manual pass, and it does not block merge. Two strings carry extra weight: `correctColumn: 'צדקו'` is the visible half of the `correctCount` judgment call that review accepted, so rejecting the column is a copy decision here rather than a scope reversal; and `results.title` ('סיכום המשחק') is now an eyebrow above the game's own name rather than the page's only identifier, following the review patch that renders `title`. Revisit trigger: **the manual browser pass**, or the first organizer-facing demo. [web/src/lib/strings.he.ts:166-186, web/src/lib/strings.he.ts:39]

## Deferred from: code review of 4-1-audience-display-shell-the-screen-that-follows-the-game (2026-08-09)

- 🔴 **PRE-EXISTING AND SEVERE — the live control panel goes dead after exactly one action, until the page is reloaded.** Found in 4.1's code-review browser pass and **reproduced against `HEAD` with none of story 4.1's changes present** (`control-page.tsx` checked out from `a4523ea`), so it is on `main`, not 4.1's doing. Mechanism: `fire()`'s only early return is `if (submittingRef.current) return`, and the only thing that ever clears that ref is the `onSettled` callback passed to `action.mutate(...)`. The server **broadcasts the new snapshot before writing the HTTP response** (deliberate, documented in `handleOpenLobby` and `display.go`), so the WS frame normally wins the race: `snapshot.state` changes → the reset effect at `control-page.tsx:111-114` calls `action.reset()` → React Query detaches the in-flight mutation and **never calls its `onSettled`** → `submittingRef` stays `true` forever → every later click on the primary CTA is silently swallowed. No request, no error banner, no console message. Observed sequence: `start` (LobbyPage's own ref, unaffected) → `close-question` (works, poisons ControlPage's ref) → `reveal` **dead**; after `location.reload()` the identical click returns `reveal:200` and advances. **Real-world impact: an organizer closes a question and the panel stops responding mid-game**, with the audience display frozen on the same stage — precisely the "stuck control panel" failure the 3.1 entries anticipate, but reachable without any race between two writers. Not fixed in 4.1: `fire()`, the ref guard and the reset effect are all explicitly off-limits to this story (its Dev Notes list them as protected 3.1/3.10 review decisions), and the fix is a real choice — clear the ref in the reset effect too, key the reset off the mutation instead of `snapshot.state`, or stop calling `reset()` on a pending mutation. **Revisit trigger: immediately — this should be the next story, ahead of 4.2.** It is the highest-severity item in this file. [web/src/features/live/control-page.tsx:80-88, web/src/features/live/control-page.tsx:105-114]
  - **CLOSED in story 3.11 (2026-08-10).** Fixed by a fourth approach the entry did not list, found by reading `@tanstack/query-core`'s `mutationObserver.js` rather than reasoning from the API surface: **release the lock from the mutation's own promise.** The new shared hook `web/src/lib/use-single-flight.ts` calls `mutation.mutateAsync(v).catch(noop).finally(release)` instead of `mutation.mutate(v, { onSettled: release })`. `mutateAsync` is `observer.mutate` unwrapped — it returns `Mutation.execute()`'s promise, and `removeObserver` does not cancel `execute()` — so the release is unconditional and **nothing outside the hook can prevent it**. The three candidates this entry proposed were each considered and rejected, with the reasoning recorded in the story's Dev Notes → *Three fixes were on the table* (do not re-derive it): **(a) also clear the ref in the reset effect** — works today, but couples banner hygiene to the submit lock, covers only the one `reset()` that currently exists, and changes the guard's semantics (the lock would drop while the request is still in flight); **(b) key the reset off the mutation instead of `snapshot.state`** — does not touch the lock at all and breaks the banner's purpose, which is to expire when the *live state* moves on; **(c) skip `reset()` while pending** — the honourable runner-up and a genuine root-cause fix, rejected because it is still a rule every future `reset()` call site has to remember. The fix is a shared `lib` hook rather than three local patches because the pattern existed verbatim in three files with three copies of the same comment, and that duplication is precisely why the bug shipped twice (see the new entry directly below); `web/src/lib/use-space-action.ts` set the precedent, and the architecture's Web boundary rule ("shared logic is promoted to `lib`") requires it. A **Vitest regression test now guards it** — `web/src/lib/use-single-flight.test.tsx`, the project's first frontend test, whose first case resets the mutation mid-flight and asserts the next press still issues a request. It was demonstrated red against the pre-fix shape (`expected "vi.fn()" to be called 2 times, but got 1 times`) before being made green, and it runs in CI. `reset()` keeps its exact previous semantics, so 3.1's finding #146 is untouched. [web/src/lib/use-single-flight.ts, web/src/lib/use-single-flight.test.tsx]
- **Found and CLOSED in the same change (story 3.11, 2026-08-10): `display-controls.tsx` carried the identical defect and nobody had recorded it.** The 🔴 entry above documents `control-page.tsx` only, but story 4.1's `display-controls.tsx` shipped the same `submittingRef` + per-call `onSettled` shape (its own `submittingRef`/`submit` at lines 54–63) alongside its own `[gameState]` reset effect (lines 72–78) — so the reduce-animations checkbox strands exactly the same way: tick "הפחת אנימציות", let a game-state change land before the PUT settles, and the checkbox is dead for the rest of the game. Worse in kind than the CTA, because `checked={reducedMotion}` reads off the snapshot with no local echo, so a dead toggle reads to the Organizer as "this control does nothing" rather than as a stuck panel. Both instances were created by the same review pass (3.1's findings #143 and #146, then copied forward into 4.1), and neither review noticed. Recorded even though it ships closed, because it is the evidence for the story's derived requirement #9 — the duplicated pattern is what let one review-created bug become two, and it is why the fix is one `lib` hook rather than three patches. Fixed by the same `useSingleFlight` adoption; covered by the same test, which exercises the hook every call site now shares. [web/src/features/live/display-controls.tsx:54-63]
- **`lobby-page.tsx` was converted too, although it was never broken** (story 3.11, 2026-08-10, no defect — recorded so a reader of the diff does not go looking for one). It held the third copy of the pattern but has no reset effect, so its `onSettled` always fired and its lock never stranded. It adopts `useSingleFlight` so the third copy cannot drift into the fourth instance, and so one test covers every call site. The only behavioural note is a compile-level one: the hook's parameter is `void` there, and under `strictFunctionTypes` a `(v: void) => void` is not assignable to `onClick`'s `(e: MouseEvent) => void`, so line 131 became `onClick={() => fireStart()}`. `useSpaceAction(fireStart, …)` compiles unchanged. [web/src/features/lobby/lobby-page.tsx:42-48]
- The reconnect band overlays the top of the stage content box rather than reserving space for it. 4.1's code review moved the band off the outer edge and inside the projector safe margin (verified in a real browser: `band.y=48px`, `margin=48px`, so it can no longer be the one element a projector's overscan clips). What the patch did **not** change is that it is still `position: absolute` over the stage: measured geometry is `bandBottom=124px` against `stageTop=48px`, so it covers the first ~76px of the content box at 1920×1080. Harmless for 4.1, whose only stage is a vertically-centred one-line placeholder — verified nothing is clipped and no scrollbar appears — but a full-height stage will lose its top band-height of pixels with `overflow: hidden` preventing any recovery. Deferred rather than patched because reserving space means deciding whether the stage shrinks or the band floats, which is a layout decision the first real stage should make with content in hand. Revisit trigger: **story 4.2** — the first stage with content near the top edge. [web/src/features/display/display-page.tsx:151-153]
  - **Trigger fired and CHECKED by story 4.2 (2026-08-11): re-deferred, because the lobby stage does not have content near the top edge after all.** Measured in a real browser during a real outage (the Go server was killed, not `setOffline`, so the socket genuinely closed): at 1920×1080 the band's box is **48–124px** and the lobby stage's content box starts at **190px** — **66px of clearance, nothing covered**. The reason is structural rather than lucky: the shell's stage wrapper is `justify-center`, and this stage's stack is ~700px tall in a 984px content box, so it is centred well clear of the top. A stage that fills its box vertically would still lose its top ~76px, so the entry stands. **Trigger re-pointed off a story number: the first Audience Display stage whose content actually reaches the top of the content box** — plausibly 4.5's leaderboard, which is a list that grows downward from the top; verify with the same measurement rather than by eye.
  - **Related and also checked (story 4.2): the band's own legibility on this stage's green ground.** 4.2's Dev Notes predicted the band would be near-invisible as a *box*, and it is: measured `bg = rgb(20, 83, 45)` (green-900) sitting on `rgb(22, 101, 52)` (green-800), ~1.4:1 apart, while the band's white text stays ~10:1 and is perfectly legible. **This is a pass with a note, not a defect**, and per 4.2's Dev Notes the fix (a border, or a token that adapts) is the *shell's* decision, not a stage story's. 4.6's winner takeover lands on green-800 too and is the second data point. Revisit trigger: **whenever the band is next touched, or if a real room reports missing it.**
  - **Trigger fired at story 4.3 (2026-08-11) — the question stage IS the full-height stage this entry was waiting for, and it is a PASS WITH A NOTE. Re-deferred.** 4.2's re-pointed trigger was *"the first Audience Display stage whose content actually reaches the top of the content box"*; the split-hero is full-bleed and its band starts at y=0, so this is that stage. Measured in a real browser at 1920×1080 during a **real** outage (the Go server was killed with SIGKILL, not `ctx.setOffline`, and `ws.on('close')` was asserted to have fired before anything else was trusted):

    | element | box | covered by the band (48–124px)? |
    |---|---|---|
    | progress label | 23–71px | **yes** |
    | timer ring | 79–299px | **yes, its top arc only** |
    | timer numeral | 141–237px | no — 17px of clearance |
    | question `<h1>` | 445–533px | no — 321px of clearance |

    That is exactly the *"covers only the progress label and/or the ring's top arc"* branch of story 4.3's decision rule, so per that rule it is **recorded and re-deferred rather than escalated**. Neither of the two elements the rule treats as must-fix (the numeral, the question text) is touched, and the room can still read the countdown and the question during a reconnect. What the room *does* lose is "שאלה 3 מתוך 10" and the ring's top arc — cosmetic during an outage that is itself being announced. Note the geometry is now **known to be tight rather than lucky**: the numeral clears the band by 17px, and any future change that raises the hero stack (a taller band, a larger progress label, a ring pushed upward) eats that margin directly. **Trigger, unchanged in substance but now with a measured baseline: the first stage whose content sits within ~20px of y=124px, or any change to the band's height or offset.** The fix, when taken, is still the shell's (reserve space, or move the band), not a stage's.
  - **Re-measured at story 4.4 (2026-08-12) — the numbers REPRODUCE 4.3's exactly, on a structurally different body. Re-deferred.** 4.3's trigger was *"the first stage whose content sits within ~20px of y=124px, or any change to the band's height or offset"*; 4.4 changes neither the band nor the offset, but it is the second full-bleed split-hero stage and its **body is structurally different** — three-child distribution rows instead of single option pills — so the measurement is a genuine second data point rather than a re-run. Measured in a real browser at 1920×1080 during a **real** outage (the Go server was killed via the orchestrator, not `ctx.setOffline`, and `ws.on('close')` was asserted `true` before anything else was trusted): band box **48–124px** (height 76px), timer numeral **141–237px** (**17px of clearance**), question `<h1>` **445–533px** (321px of clearance). **Identical to 4.3's figures to the pixel**, which is the expected result and is now confirmed rather than assumed — the band and the hero stack are the same components at the same 35%, and the body's differences are all below y=445. Neither must-fix element is covered, so per 4.3's own decision rule this is **recorded and re-deferred, not escalated to Avraham**. The 17px numeral clearance is now a **twice-measured** tight margin: any change raising the hero stack eats it directly. **Trigger unchanged** — the first stage whose content sits within ~20px of y=124px, or any change to the band's height or offset. Also verified in the same outage, and worth recording because it is what the entry is ultimately about: the reveal stage **holds its marks and bars unchanged underneath the band** (identical pill fills and identical inline bar widths before, during and after), and re-renders with **no interaction** once the server is back, at which point the band disappears. [web/src/features/display/reveal-stage.tsx]
  - **Second data point for the legibility note (story 4.3, 2026-08-11), as 4.2 asked for.** The band lands on green-800 again here: measured `rgb(20, 83, 45)` (green-900) on `rgb(22, 101, 52)` (green-800) — the same ~1.4:1 box 4.2 measured — with white text at ~10:1. **Identical result on a second, structurally different stage, so this is now a reproduced property of the band rather than a one-stage observation.** Still a pass with a note, still the shell's decision, still not a stage story's to fix. 4.6's winner takeover would be a third data point and is no longer needed to establish the pattern.

- The display-settings mutation discards the 200 response snapshot, so a dropped WS frame leaves a *controlled checkbox* permanently showing the opposite of what is persisted. This is the standing 3.1 entry ("Control mutations discard the REST response snapshot") reaching a surface where it is worse than for a CTA: `api<unknown>(...)` throws away the full updated snapshot the handler deliberately returns, `hub.Broadcast` drops on a full 8-slot queue with no redelivery, and `checked={reducedMotion}` has no local state to fall back on — so the PUT succeeds with no error, the frame is lost, and the toggle silently lies about the room's actual setting until the organizer clicks it again or the socket reconnects. A CTA that fails to advance is at least visible as a stuck panel; a checkbox that reports the wrong persisted value is not. Deferred as pre-existing in cause: the fix is the 3.1 entry's fix (apply the returned snapshot into the socket store), not a local patch here. Revisit trigger: **unchanged from the 3.1 entry** — the first real-world report of a stuck control panel, now also covering "the reduce-animations toggle shows the wrong state". [web/src/features/live/display-controls.tsx:28-33, web/src/lib/use-game-socket.ts, server/internal/ws/hub.go]
- `games.updated_at` is now advanced by a write that changes no state, which poisons the remedy the broadcast-`seq` entry itself proposes. That entry's recommended fix is "deriving `seq` from something commit-ordered (e.g. `games.updated_at`)"; after story 4.1, `UpdateGameDisplaySettings` bumps `updated_at` on a purely cosmetic toggle with no accompanying state change, so whoever implements that fix will find the column is no longer a proxy for "state changed" — a settings toggle would issue a higher `seq` than a concurrently-committing real transition, reintroducing exactly the regression the fix was meant to close. Two entries in this file now assume two different meanings for the same column. Recorded rather than patched because removing the `updated_at` bump would make this query the only mutation in `games.sql` that does not touch it, which is its own inconsistency. Revisit trigger: **whenever the `seq` ordering fix is implemented** — pick a source that is genuinely commit-ordered and state-scoped (a DB sequence returned by the guarded UPDATE), not `updated_at`. [server/internal/store/queries/games.sql:38, server/internal/ws/hub.go:153-161]
- Nothing exercises the `StageProps` wiring that five later stories depend on. `StagePlaceholder` ignores both props and is the value for all seven entries in `stageByState`, so `<Stage snapshot={rendered} reducedMotion={reducedMotion} />` could be changed to pass the nullable `snapshot`, or to pass `prefersReducedMotion` alone (silently dropping the room-level half of AC-5's OR), and nothing would notice — not the type checker, not a test, not a running placeholder — until story 4.3 renders the first real stage. The `Record<GameState, …>` exhaustiveness guard protects the map's *keys*; nothing protects its *inputs*. Deferred as pre-existing: no frontend test framework exists (the documented standard since 1.2) and story 4.1 is explicitly forbidden from adding one. Revisit trigger: **story 4.2** — the first story to replace a switcher entry with a stage that actually reads its props is the first that can observe the wiring, and is the natural place to decide whether this contract warrants the project's first frontend test. [web/src/features/display/display-page.tsx:139, web/src/features/display/stage-placeholder.tsx:20]
  - **Half of this trigger was consumed by story 3.11 (2026-08-10): the framework decision has already been made, elsewhere.** Vitest exists in the repository as of 3.11 — co-located per architecture § Structure Patterns, one `test` block in `web/vite.config.ts`, a `test` script, and a CI step in the existing `frontend` job — so "decide whether this contract warrants the project's first frontend test" is no longer a question anyone has to answer. What remains is unchanged in substance: 3.11 tests a `lib` hook, not the display, so the `StageProps` contract itself is still exercised by nothing and could still be rewired silently. The remaining work is *writing* a test, not *choosing* a framework, and it is now cheap. **New trigger: story 4.3** — still the first stage with genuinely unit-testable logic (4.2's lobby stage renders a count off the snapshot; 4.3 owns the authoritative timer).
  - **CLOSED for the `snapshot` half in story 4.2 (2026-08-11); the `reducedMotion` half stays open.** Story 4.2 was *written* to re-defer this to 4.3 on the premise that no test framework existed and that "`LobbyStage` is pure presentation with no logic a unit test could assert". **The first half of that premise had already expired** (3.11 landed Vitest — see the note directly above, which the story predates), and the second half was **wrong**: the high-water guard 4.2 adds *is* logic, and it is the mitigation for a live defect. The test was therefore written rather than deferred — a developer call on the grounds that the story's premise had expired, and one that never engaged the scope boundary it appears to cross: the boundary forbids adding a test *framework*, and no framework was added (`web/package.json` is unmodified; Vitest, `@testing-library/react` and `jsdom` all arrived with 3.11). *(Wording corrected at 4.2's code review, 2026-08-11: this sub-bullet originally attributed the call to Avraham as established fact. Nothing in the repository corroborated that, and this file is a permanent shared record — the reasoning above stands on its own without the attribution.)* `web/src/features/display/lobby-stage.test.tsx` now renders the real `LobbyStage` through the real `StageProps` shape and asserts the join code, the phone number and the count actually come off the snapshot — so passing a nullable snapshot or the wrong object now fails a test instead of waiting for a browser. **The `reducedMotion` half is still exercised by nothing**: this stage deliberately has no animation (its AC-7), so the room-level half of 4.1's AC-5 OR could still be silently dropped and no test would notice. **Trigger for the remainder: story 4.3** — its timer ring is the first stage motion, and the first thing that can observe `reducedMotion` being wrong. [web/src/features/display/lobby-stage.test.tsx]
  - **CLOSED in full by story 4.3 (2026-08-11).** `TimerRing` reads `reducedMotion` as a **JS prop** and uses it to decide whether the depleting ring gets its `stage-timer-sweep` class, so the room-level half of 4.1's OR is now load-bearing at runtime. Two tests pin it from both sides: `timer-ring.test.tsx` ("renders a static ring under reduced motion while the numeral still counts" / "sweeps when motion is allowed and the question is open") asserts the class is absent when the prop is `true` and present when it is `false`, and `question-stage.test.tsx` ("passes reducedMotion through to the ring") asserts the prop actually traverses `StageProps` from the shell to the ring. Silently dropping `displaySettings?.reducedMotion` from the shell's OR now fails a test rather than waiting for a browser. **The CSS `data-reduced-motion` channel was available and deliberately not used here**, and the reason should stop a later reader from "simplifying" it back and reopening this entry: a CSS-only kill is invisible to jsdom, so it cannot be asserted, and asserting it is the entire point of this entry. The shell keeps both channels — `.stage-fade` still uses the CSS one, because the **shell** owns that animation and no component reads it. [web/src/features/display/timer-ring.tsx, web/src/features/display/timer-ring.test.tsx, web/src/features/display/question-stage.test.tsx]

## Deferred from: code review of 3-11-the-control-panel-that-never-goes-dead (2026-08-11)

- `useSingleFlight`'s returned callback changes identity on every render, churning `useSpaceAction`'s global keydown listener. `useCallback(..., [mutation])` never stabilizes, because `useMutation` returns a fresh object each render — so `fireStart` is a new function every render and `use-space-action.ts`'s `[onFire, enabled]` effect removes and re-adds its `window` keydown listener on every render, including one per inbound WebSocket frame. **Not a correctness bug** (the teardown and re-add are synchronous within a single React commit, so no keypress can land in the gap) and **not introduced by 3.11** — the pre-fix `useCallback(..., [action])` / `[startGame]` had identical churn. Deferred rather than patched because story 3.11's Task 1 explicitly considered and declined it (*"That is unchanged from today's `useCallback(..., [action])` and is not this story's problem — do not 'fix' it with a latest-ref"*), and that prohibition stands. Recorded because the review found a fix the story did not consider, which honours the prohibition: **depend on `[mutation.mutateAsync]` rather than `[mutation]`**. `MutationObserver.bindMethods()` binds `mutate` once in the constructor and `#updateResult()` passes that identical reference into every result object, so `mutation.mutateAsync` is stable for the observer's lifetime — a stable callback for a one-token change, with no ref and no rule to remember. Revisit trigger: **whenever a surface needs a genuinely stable callback identity** (a `useEffect` dependency, a memoized child, or a third `useSpaceAction`-style consumer), or opportunistically alongside the next change to this hook. [web/src/lib/use-single-flight.ts:36,53, web/src/lib/use-space-action.ts:26]
- `make test` and `make build` assume `web/node_modules` is already populated. Story 3.11 extended the `test` target with `cd web && npm test`, which fails at the vitest binary on a fresh clone because nothing runs `npm ci`/`npm install` first. CI is unaffected — it installs in its own step (`.github/workflows/ci.yml`, `npm ci` before `lint`/`typecheck`/`vitest`/`build`). Deferred as pre-existing convention rather than a new gap: `make build`'s `cd web && npm run build` already had the identical shape before this story, so adding an install prerequisite is a Makefile-wide decision (which target owns it, and whether it should run on every invocation) rather than a fix belonging to 3.11. Revisit trigger: **the first time a fresh clone or a new contributor hits it**, or whenever the Makefile's targets are next revised. [Makefile:23-25]

## Deferred from: code review of 4-2-lobby-stage-the-room-fills-the-screen (2026-08-11)

- `StageProps` lives in `display-page.tsx`, so the display module graph contains a real import cycle held apart by a single `type` keyword. `display-page.tsx` value-imports `LobbyStage`, and `lobby-stage.tsx` imports `StageProps` back from `display-page.tsx`. The `import type` is correct and erased at compile time, so there is no runtime cycle today — but the defence is one keyword that no type checker enforces, and stories 4.3–4.6 will each add another stage file repeating the same shape. A developer writing `import { StageProps }` instead of `import type { StageProps }` reintroduces a genuine circular dependency whose symptom is an undefined component at module-init time, i.e. a blank projector. Deferred rather than patched because moving the type is a shell-contract change and story 4.2's Task 3 explicitly mandated the type-only import form. Fix when taken: move `StageProps` to its own module (or to `@/lib/types`, already imported by both) so the cycle cannot exist at all. Revisit trigger: **story 4.3** — the second stage file is the point where the pattern stops being a one-off and starts being a convention four more files will copy. [web/src/features/display/display-page.tsx:14-24, web/src/features/display/lobby-stage.tsx:7]
  - **CLOSED by story 4.3 (2026-08-11), before the second stage file was written.** `StageProps` now lives in its own module, `web/src/features/display/stage-props.ts`, holding the interface and its doc comment and nothing else. The three existing importers — `stage-placeholder.tsx`, `lobby-stage.tsx` and `display-page.tsx` (which now imports the type rather than declaring it) — all read `import type { StageProps } from './stage-props'`, and `display-page.tsx` **re-exports nothing**: a `export type { StageProps } from './stage-props'` left behind would have preserved the very import path whose value form recreates the trap. The cycle cannot exist at all now, so the `import type` keyword is no longer load-bearing and the **"runtime cycle" comment above each import was deleted rather than left in place** — it documented a hazard that no longer exists, and a comment describing an impossible failure mode is worse than never having written it. Not `@/lib/types`, whose header is "Wire types mirroring the Go payloads": `StageProps` is a component contract, not a wire type, and keeping it in `features/display/` keeps the display's contract co-located. [web/src/features/display/stage-props.ts]
- Nothing on the Audience Display has heading semantics — every text element on every stage is a `<p>`, including the join code, which is the single most important string on the surface. `font-heading` and the `Heading 800` ramp step name a *font weight*, not an element, so the rendered document has no structure at all: a screen-reader user gets no heading to navigate to and no outline. Deferred as pre-existing and shell-level: 4.1 established the pattern (`stageMessageClass` is applied to `<p>`), it is not introduced or worsened by 4.2, and deciding whether the display gets an `<h1>` — and whether it belongs to the shell or to each stage — is a contract decision that binds 4.3–4.6 too. Note the surface is deliberately output-only with nothing focusable, which weakens but does not remove the case. Revisit trigger: **whenever the shell's `StageProps` contract is next revised** (see the entry above — the two are the same file and should be taken together), or the first time the display is evaluated against the Accessibility Floor with a real screen reader. [web/src/features/display/display-page.tsx:57, web/src/features/display/lobby-stage.tsx:95,114,119]
  - **Trigger fired at story 4.3 (2026-08-11) — PARTIALLY closed, and re-deferred for the rest.** 4.3 revised the shell's `StageProps` contract (it moved to its own module — see the entry above), which is this entry's stated trigger. **What changed:** the question stage's question text is now an `<h1>`, the first heading element anywhere on the Audience Display. It was safe to take unilaterally for three reasons worth recording: the question is unambiguously that stage's heading, Tailwind's preflight resets `h1` `font-size` and `font-weight` to `inherit` so there is **zero visual change**, and exactly one stage is mounted at a time so there is never a second `<h1>` on the page. **What did NOT change, and is the substance of this entry:** the shell-level decision. Does every stage get an `<h1>`, and does it belong to the shell or to each stage? The lobby stage's join code — this entry's own example of "the single most important string on the surface" — is still a `<p>`, and 4.4–4.6 are unconstrained. One stage having a heading arguably makes the inconsistency *worse* than none having one, which is an argument for taking the shell decision soon rather than stage by stage. **Trigger re-pointed off the `StageProps` hook (it has now fired and cannot fire again): the next stage story that renders a dominant text element** — 4.4's answer card or 4.6's winner name — **or the first real screen-reader evaluation of the display**, whichever comes first. [web/src/features/display/question-stage.tsx]
  - **Trigger fired at story 4.4 (2026-08-12) — the answer card is that dominant text element. Re-deferred, with a decision recorded about the card itself.** **What 4.4 did:** the reveal stage's question is an `<h1>`, identical in element, class list and position to the question stage's — because it *is* the same element in the same place, and the whole point of derived requirement 7 is that it does not move between `question_closed` and `revealed` (verified in the browser: `boundingBox` identical to the pixel, `{x:48, y:444.84, w:1824, h:88.31}` at both states). No new decision was needed for it. **What 4.4 deliberately did NOT do: the free-text answer card is not a heading, and that is a considered call rather than an omission.** The card is the *answer* — it is content, the thing the question was asking for — and the question sitting above it is already this stage's heading. Marking the answer as a second `<h1>` would give the page two competing top-level headings; marking it `<h2>` would imply a document outline the display does not have (one stage, one screen, no navigation). It is a `<div>` with a `sr-only` accessible name on its ✓ (`display.reveal.correctOptionLabel`), which is what UX-DR14 actually asks for on this element. **The substance of this entry is still open and is still shell-level:** the lobby stage's join code — this entry's own example of "the single most important string on the surface" — is **still a `<p>`**, and 4.5's leaderboard is unconstrained. Two of four built stages now have an `<h1>` and two have nothing, which is arguably a worse inconsistency than none having one, and it is now the *second* story to say so. **Trigger re-pointed again: 4.6's winner name (the last dominant text element Epic 4 will produce), or the first real screen-reader evaluation of the display, whichever comes first.** If 4.6 also decides stage-locally, the shell decision should be taken as its own item rather than deferred a fourth time. [web/src/features/display/reveal-stage.tsx]
  - **Checked by story 4.5 (2026-08-12): NOT triggered — 4.4's re-pointed trigger (4.6's winner name, or the first real screen-reader evaluation) is unchanged. Recorded because 4.5 moved the surface anyway.** The leaderboard stage adds an **`<ol>`**, which is the first real *structural* semantics on any Audience Display stage: the room's ranking is an ordered list in the *markup* rather than a pile of paragraphs. **Do not read that as a delivered accessibility win** — the review of 2026-08-12 checked it and the exposed semantics are weaker than this sentence first claimed: Tailwind preflight sets `ol { list-style: none }` and every row is `display: flex`, and under either of those WebKit/VoiceOver stops exposing list semantics at all. Immaterial to this story's ACs (the display is output-only with nothing focusable and names no AT target), but it means the `<ol>` is honest structure for a future fix to build on, not evidence the entry is partly closed. It deliberately adds **no heading**, and the reason is a budget rather than an oversight — ten rows at `--stage-leaderboard-row` leave ~264px at 1080p with the list centred in it, DESIGN.md and EXPERIENCE.md specify no title for this stage, and the shell's assertive announcer already says "טבלת התוצאות" on entry, so a visible `<h1>` would be a third naming of the same thing. **Do not read the `<ol>` as partial progress on this entry**: it does not give the join code, the question text or the winner name heading semantics, which is what the entry is actually about. Trigger unchanged. [web/src/features/display/leaderboard-stage.tsx]
  - **TRIGGERED and CLOSED by story 4.6 (2026-08-14) — decided stage-locally for the third time, and that IS the decision. Not re-deferred.** 4.4's re-pointed trigger was *"4.6's winner name (the last dominant text element Epic 4 will produce), or the first real screen-reader evaluation of the display, whichever comes first"*, with its own warning that a fourth deferral should not happen. The winner name is now an **`<h1>`**, on the same reasoning 4.3 used for the question and 4.4 for the reveal's shared question: it is unambiguously this stage's one dominant text element, Tailwind preflight resets `h1`'s `font-size`/`font-weight` to `inherit` so there is zero visual change, and exactly one stage is mounted at a time so there is never a second `<h1>` on the page. A Vitest case asserts the **element**, not just the text (`winner-stage.test.tsx`, "renders the winner name as an `<h1>`"), plus that it is the only heading in the container. **The accepted final shape, stated so nobody reopens this looking for the missing half:** three of the five built stages carry an `<h1>` (the question stage's question, the reveal stage's shared question, and this winner name); the lobby stage's join code and the leaderboard's rows remain plain elements. That asymmetry is **the shape Epic 4 ships with**, not an open item — the join code is a `<p>` because no story after this one exists to change it, and the leaderboard deliberately has no heading at all for a documented vertical-budget reason. **Closed rather than re-deferred because there is nothing left to defer to:** the entry's substance was always "take the shell decision instead of deciding per stage", and Epic 4 has now produced its last stage, so the shell decision has been taken by exhaustion — every stage decided locally, and the result is recorded here. If a real screen-reader evaluation of the display ever happens and finds this shape wanting, it is a **fresh entry with its own evidence**, not a reopening of this one. [web/src/features/display/winner-stage.tsx, web/src/features/display/winner-stage.test.tsx]
- The giant phone number has no horizontal fit guard, and its headroom is glyph-count-bound rather than viewport-bound. `--stage-lobby-code` is a `vw` value inside a `vw`-padded container, so the ratio of glyph width to available width is **constant at every viewport size**: the number fits at roughly 19 glyphs and fails at every resolution above that, not just on small screens. `+972 50-000-0000` is 17. A `WHATSAPP_DISPLAY_NUMBER` with more segments either wraps at one of its spaces (adding a second 172.8px line, which then feeds the vertical budget the `min(9vw, 16vh)` guard was added for) or, if it has no spaces to break at, is clipped mid-digit by `.stage-root`'s `overflow-hidden` with no ellipsis and no scrollbar — on the one token the whole stage exists to have transcribed. Deferred rather than patched at 4.2's code review for two reasons: the threshold is a property of a **configuration value that is visible the moment it is set**, not a silent runtime failure during an event, and the obvious one-line remedy (`whitespace-nowrap`) merely converts a wrap into a clip, i.e. swaps one failure mode for another. A real fix is a `min()`/`clamp()` that also considers the token's own length, which is a ramp decision rather than a stage patch. The vertical half of the same family **was** fixed in 4.2 (`--stage-lobby-code: min(9vw, 16vh)`). Revisit trigger: **whenever `WHATSAPP_DISPLAY_NUMBER` is changed to a longer or differently-formatted value**, or the first time a stage needs a token whose length is not known at config time. [web/src/index.css:219, web/src/features/display/lobby-stage.tsx:112-121]

## Deferred from: code review of 4-3-question-stage-with-the-authoritative-timer (2026-08-11)

- Extreme question and option text on the question stage is clipped silently, with no clamp, ellipsis or type scaling. The `<h1>` (`question-stage.tsx:105-107`) has no line clamp and its container has no `overflow-hidden` of its own, so a very long question grows past the body region in both directions and is cut by `.stage-root`'s `overflow-hidden` — losing the first and last lines with no indication. Each option row (`question-stage.tsx:125-127`) is `basis-[var(--stage-option-min)] shrink overflow-hidden` with `flex-grow: 0`, so text past roughly two lines at 40px is hard-cut mid-word with no ellipsis. **Not a speculative concern, but bounded**: story 4.3's browser pass measured both realistic worst cases (a two-line question with four long wrapping options) at 1920×1080 *and* 1280×720 and found nothing clipped — the rows shrank 64px → 58px exactly as designed. What is unmeasured is the authoring ceiling: 500 characters for a question and 200 per option (`server/internal/httpapi/questions.go:122`), which is far beyond two lines. Deferred rather than patched for the same reason as the `--stage-lobby-code` entry above, which is the same family of defect: the real remedy is a ramp/clamp decision (a `clamp()` on `--stage-heading`, or a line-clamp with a documented truncation policy), not a stage patch, and the threshold is a property of a value the Organizer sees while authoring rather than a silent runtime failure during an event. Revisit trigger: **the first Organizer-authored question or option that visibly clips on a real projector**, or whenever the projection ramp is next revised — take it together with the `--stage-lobby-code` entry, since both are fit guards on the same ramp. [web/src/features/display/question-stage.tsx:105-107,125-127, web/src/index.css:229-238]
- The Audience Display's countdown trusts the projector's clock **offset**, not just its monotonicity, and the two-way clamp masks the number without correcting the skew. `TimerRing` computes `Date.parse(answerCutoffAt) - Date.now()` and clamps to `[0, totalSeconds*1000]`. A clock running **ahead** of the server therefore floors at 0 from the very first frame: verified in a probe with the deadline 10s past by client reckoning while the server still accepted answers for 20s more, the display rendered numeral `0`, `stroke=var(--color-gold)`, `stroke-width=14`, the ≤5s region pre-filled, and no pending timer — the room is told time is up, in gold, for the whole question, while every answer still counts. The mirror direction (clock **behind**) was a live freeze bug and **was patched** in 4.3's review; this entry is the half that a clamp cannot fix. Note also that the ≤5s gold — rationed by UX-DR2 to two moments in the whole product — is spent by any degraded or skewed deadline, since `urgent = !closed && remainingSeconds <= 5` is true at 0. Deferred at Avraham's call at 4.3's code review: the remedy is to derive a server-clock offset (stamp the snapshot with the server's time and subtract once at receipt, then drive every deadline off `serverNow()` rather than `Date.now()`), which means a new `Snapshot` field and therefore a `.go` change — squarely outside 4.3's "frontend only, zero `.go` files change" boundary. The code as written **conforms** to derived req. 6; the gap is in the requirement's model, which is why this is a follow-up story rather than a patch. Revisit trigger: **the first report of a projector showing a wrong or stuck countdown**, or whenever a stage other than the question stage needs server time (4.4's reveal and 4.6's winner both plausibly do) — take it together with the `seq` ordering entries above, since a snapshot-stamped server time would serve both. [web/src/features/display/timer-ring.tsx:47-50, server/internal/game/snapshot.go:13-14]
  - **Checked by story 4.6 (2026-08-14): the parenthetical guess about this story is WRONG, and it is worth saying plainly so a later reader does not go hunting for a dependency that was never real.** 4.3's trigger reads *"whenever a stage other than the question stage needs server time (4.4's reveal and 4.6's winner both plausibly do)"*. **4.6's winner does not, in any form.** The winner takeover renders no countdown, no deadline, no elapsed figure and no timer of any kind: its entire data surface is `snapshot.questionCount` and `snapshot.leaderboard`, neither of which is time-relative, and it calls neither `Date.now()` nor `Date.parse()` anywhere. Its one animation is a CSS loop with a fixed duration and a fixed negative delay per piece — wall-clock-independent by construction, and driven by the compositor rather than by any clock this code reads. There is nothing on this stage a server-time offset would change. Trigger unchanged in substance; **the "4.6's winner" half of it is now closed by inspection**, leaving 4.4's reveal and any future time-bearing stage. [web/src/features/display/winner-stage.tsx]

## Deferred from: code review of 4-4-reveal-stage-the-answer-marked (2026-08-12)

- `SetDisplaySettings` at `revealed` now inherits two new non-degrading failure points. `SetDisplaySettings` is deliberately the **one** `buildSnapshot` caller that propagates a build failure instead of degrading — its own doc comment argues the case at length (degrading would blank the roster, the current question and the leaderboard for the whole room, reachable from a cosmetic toggle, on a finished game that never re-broadcasts to self-correct). Story 4.4 adds `CountCorrectAnswersByQuestion` and `ListAnswerCountsByResponse` inside `buildSnapshot`'s `g.State == StateRevealed` branch, so an Organizer toggling "הפחת אנימציות" while the game sits at `revealed` now has two more reads that can turn a committed boolean write into a returned error with no broadcast: the setting is persisted, the room's projector keeps animating, and the dashboard says the toggle failed. Deferred rather than patched because it **widens an existing, deliberately-chosen surface rather than introducing a new class of failure** — the same path already fails on `ListParticipants`, `ListQuestionsByGame`, `CountAnswersByQuestion` and the leaderboard reads, and the documented remedy (the Organizer retries, which is harmless because the write already committed) is unchanged. `TestSnapshotRevealReadErrorPropagates` only exercises the `Snapshot()` entry point, not this one. Revisit trigger: **the first report of a display-settings toggle that reports failure while the setting visibly took effect**, or whenever `SetDisplaySettings`' degrade-vs-propagate call is next revisited — take it together with the `snapshotAfterCommit` reasoning, since the two are the same decision seen from opposite sides. [server/internal/game/engine.go:238-244, server/internal/game/engine.go:565-575]
- `reveal.correctCount` is read on the MCQ path and rendered nowhere, and the MCQ payload now carries two independent answers to the same question. `buildQuestionReveal` calls `CountCorrectAnswersByQuestion` before the type branch, "for both question types" — but the frontend reads `correctCount` in exactly one place, the **free-text** counts line. On every MCQ reveal the value is computed, serialized and dropped: one DB round trip on a state the story's own comment budgets at "two extra round trips". More interesting than the cost: on MCQ the wire carries `correctCount` (derived from `answers.is_correct`, i.e. **grading**) alongside `optionCounts[correctOption-1]` (derived from `answers.response`, i.e. **what was typed**). These must agree, nothing asserts that they do, and if grading ever diverges from the response value the payload is self-contradictory with no detection anywhere. Deferred rather than patched because **the read is spec-mandated** — story 4.4's Task 3 says "correct count from `CountCorrectAnswersByQuestion` for **both** types" and its scope boundary explicitly forbids folding reads to save a round trip on a path that is not hot — so removing it is a spec change, not a patch. Revisit trigger: **the first story that renders `correctCount` on the MCQ reveal** (4.5's leaderboard is the likely place a per-question correct count would surface), or any grading change that could make `is_correct` and `response` disagree — at which point the cheap version of the check is a one-line invariant log in `buildQuestionReveal` comparing the two. [server/internal/game/engine.go:55-59, web/src/features/display/reveal-stage.tsx:176]
- A response that fails to parse is dropped with **zero** observability. `n, err := strconv.Atoi(r.Response); if err != nil || n < 1 || n > len(counts) { continue }` — the error is discarded with no log line, no metric and no counter. The drop itself is the **correct** behaviour and the code's comment argues it well (silently attributing a row to the wrong bar would be worse), but the observable symptom of it ever firing is bars that quietly sum to less than `answeredCount`, on a projector, with nothing in the logs to say why. Unreachable today — `game/answers.go` is the only writer of `answers.response` and it writes `strconv.Itoa` of a 1–4 parse — which is exactly why a `slog` line here would cost nothing and would be the only evidence that the "unreachable" assumption broke. Deferred rather than patched to keep this story's `engine.go` diff to the three things it scoped. Revisit trigger: **the first time a distribution is reported as not adding up**, or whenever a second writer of `answers.response` is introduced (a bulk import, a manual grade correction, or a second answer-intake channel). [server/internal/game/engine.go:81-85]
- `.stage-bar-grow`'s `both` fill mode holds a `transform` on the fill element indefinitely. The rule is `animation: stage-bar-grow 400ms ease-out both` over a keyframe block with only a `from`. `both` includes forwards fill, so after 400ms the element stays under an animation-applied `transform` value — and animation-applied values outrank **every** author declaration regardless of `@layer`, which is the one thing the layering directly above it cannot protect against. The comment beside the rule worries about exactly this hazard class for `animation-*` utilities and solves it with `@layer`; the hazard that survives is that any later `transform` utility on the bar fill is silently dead. No consumer sets a `transform` on the fill today, and `both` is what story 4.4's Task 5 specified verbatim, so this is latent rather than broken. Note also that the equivalent choice on `.stage-timer-sweep` is `forwards`, deliberately, for a different reason (it must hold the depleted arc) — so the two are not inconsistent, but a future reader comparing them should know why. Revisit trigger: **the first stage or state that needs to `transform` a distribution bar** (4.5's leaderboard reshuffle is the plausible one — it animates rows that look like these), or any change to `@keyframes stage-bar-grow` that adds a `to`. [web/src/index.css:290-321, web/src/index.css:349-353]
  - **Checked by story 4.5 (2026-08-12): the predicted trigger fired, and the answer is that the two rules are deliberately different.** 4.5's reshuffle does animate rows that look like these, and it uses `backwards` rather than `both` — precisely so it does *not* pin a transform after settling. The `index.css` comment beside `.stage-row-reshuffle` states the reasoning and warns against "making them consistent" by copying the older rule. So this entry is **not** closed and its subject is unchanged (`.stage-bar-grow` still fills `both`), but the newest animation in the file is now the counter-example rather than another instance. Trigger unchanged. [web/src/index.css (.stage-row-reshuffle)]

## Deferred from: code review of 4-5-leaderboard-stage-the-room-reshuffles (2026-08-12)

- Two rows arriving from below the visible ten stack on one start slot, and rows leaving the ten are not rendered at all — so the 300ms hold is not the board the room just saw. `Math.min(previous.index, maxRows)` clamps **every** below-the-fold arrival to start slot 10, so with more than ten players and two or more of them climbing into the top ten in one question, both rows paint on top of each other for the full duration of the delay. Simultaneously the slots vacated by rows that dropped out sit empty, because a departing row has no entry in `snapshot.leaderboard.slice(0, maxRows)` and is never rendered. There is vertical room for both to be visible (720px of rows inside an 888px content box at 1920×1080). This matters only because the `index.css` comment sells the delay as load-bearing — *"the board arrives holding the order the room last saw, the shell's 300ms cross-fade finishes, and only THEN does it reshuffle"* — and in this shape it holds a board the room did not see. **Deferred at Avraham's call at this code review (2026-08-12), reason recorded: cosmetic, it needs more than ten players *and* two simultaneous below-the-fold climbers in one question, and both candidate fixes (staggering clamped start slots to 10, 11, …; or rendering exiting rows for the animation's duration) add machinery to a stage that derived requirement 11 deliberately built without any measurement or exit tracking.** Same family as the story's other accepted cost — a reloaded display shows no ▲ on the first Leaderboard it sees — and should be taken together with it. Revisit trigger: **the first game with a room large enough for two rows to enter the top ten at once**, or whenever exit/enter animation is added to any display stage (4.6's winner takeover is the next candidate). [web/src/features/display/leaderboard-stage.tsx:99-101, web/src/index.css (.stage-row-reshuffle)]
  - **Checked by story 4.6 (2026-08-14): the named candidate did NOT fire it.** The second half of the trigger names this story as the next candidate for exit/enter animation, and the winner takeover adds an animation — but not that kind. Its 22 confetti pieces are a **fixed decorative set that loops in place**: the array is a module-level constant, identical on every render, so no piece ever enters, leaves or reorders. There is no set derived from server data on this stage that could grow or shrink the way `snapshot.leaderboard.slice(0, maxRows)` does, and therefore no row-departure case to build and no start-slot collision to stagger. The stage's *content* set (up to three winner names) is static for the entire life of the mount — `finished` is terminal and nothing re-broadcasts a different winner — so it has no enter/exit case either. Trigger unchanged in substance, minus its named candidate: it now needs **a display stage that animates a server-derived collection whose membership changes**, of which the leaderboard remains the only instance in the product. [web/src/features/display/winner-stage.tsx, web/src/index.css (.stage-confetti-piece)]
- A degraded post-commit frame at `leaderboard` costs the room the entire board, and this is the first state with nothing to re-broadcast it. When `buildSnapshot` fails after `ShowLeaderboard` has committed (pool blip, or the 5s detached-snapshot budget expiring), `snapshotAfterCommit` degrades to `emptySnapshot` and `handleShowLeaderboard` broadcasts that. The *rendering* half is handled well and deliberately — `leaderboard-stage.tsx` shows `display.waiting` rather than a plausible all-zero board, and the memory hook refuses to capture a baseline from it (derived req. 8). What is not handled is recovery: the socket is healthy so no reconnect fires, `SetDisplaySettings` is the only other writer, and the organizer's next action *leaves* the state. Every previous degraded frame was followed by another transition that re-broadcast the same content; **the Leaderboard's content exists only at the Leaderboard**, so the room is shown the waiting copy for the whole pause and never sees that question's board. Deferred as a variant of two entries that already own the mechanism — the 3.1 discarded-REST-snapshot entry (re-triaged by this story, which already records that the Leaderboard is the first state a dropped frame can strand *the room* on) and the 3.7 degraded-`emptySnapshot` entry — and because the fix is the same architectural change both name (consume the 200 response snapshot, or add a periodic/­on-demand refresh), not a stage patch. Revisit trigger: **the first report of an Audience Display stuck on the waiting copy while the organizer's panel shows a live state**, or whenever the discarded-REST-snapshot fix is implemented — take all three together. [server/internal/game/engine.go (snapshotAfterCommit, emptySnapshot), server/internal/httpapi/control.go (handleShowLeaderboard), web/src/features/display/leaderboard-stage.tsx]
- The leaderboard row's three fixed-width numeric columns have no overflow handling. `w-[6ch]` on the score, `w-[3ch]` on the rank and `w-[5ch]` on the mover slot are all `shrink-0` with no `overflow-hidden` and no `truncate`, while the name between them is `min-w-0 flex-1 truncate` and therefore cannot yield. A seven-digit score, a four-digit rank, or a `▲` glyph wider than one `ch` in the resolved font at "▲10" spills into the neighbouring column — the exact column-alignment failure the reserved mover slot exists to prevent. Deferred as unreachable at pilot scale rather than as unreal: the schema's own ceiling is `points_per_correct BETWEEN 0 AND 10000` (migration 00004), so seven digits needs ~100 questions, and a four-digit rank needs a thousand-player room; the story's browser pass measured the score column's digits aligned across rows at both verified resolutions with realistic values. Revisit trigger: **the first game authored with enough questions or players to reach seven-digit scores or four-digit ranks**, or the first report of a leaderboard column visibly overlapping — the cheap remedy is `overflow-hidden` on the three fixed columns, which turns a spill into a clip. [web/src/features/display/leaderboard-stage.tsx:120,130-140,165]
- On the last question the Leaderboard's primary CTA reads "שאלה הבאה ←" and irreversibly ends the game. `nextPosition <= snapshot.questionCount` is false on the last question, so the label falls back to `strings.live.nextQuestionCta`; pressing it POSTs `next-question`, which `Engine.NextQuestion` forks into `FinishGame` — `state = finished`, `current_question_position = 0`, and the final-results WhatsApp dispatch to every participant. The branch has the information to say "סיים משחק" and does not use it. Deferred as **strictly pre-existing and unchanged by this story**: the identical button at `revealed` has behaved identically since story 3.1, and story 4.5's scope boundary explicitly requires the `revealed` primary to stay as 3.1 resolved it because AC-2 depends on it. What is new is only that 4.5 made the label expression position-aware, which is what makes the improvement cheap when it is taken. Revisit trigger: **the first organizer who ends a game expecting to advance**, or whenever the Host-control-panel copy is next revised — take it together with the `revealed` label, since one fix should cover both. [web/src/features/live/control-page.tsx:70-78, server/internal/game/engine.go (NextQuestion)]
- `live.openQuestionNumberCta` interpolates a digit run into a Hebrew template with no bidi isolation. `` (n: number) => `פתח שאלה ${n}` `` produces a bare number inside an RTL string, while `leaderboard-stage.tsx` states five times over that DESIGN.md makes `<bdi dir="ltr">` mandatory for all-digit runs in RTL context — the rule this story declares mandatory, broken in the same story. It renders acceptably today because the number is trailing and single-token. Deferred rather than patched because the fix is not local: `strings.he.ts` returns strings, not JSX, so isolating the number means composing the CTA in TSX (or emitting explicit U+2068/U+2069 isolate characters), and this is the organizer's dashboard rather than the projector, where the bidi rule was written for a room reading at twelve metres. Note `strings.live.questionProgress` has the same shape and predates this story. Revisit trigger: **the first Hebrew-locale rendering defect reported on a numbered dashboard CTA**, or whenever the dashboard's bidi discipline is brought up to the display's — take both strings together. [web/src/lib/strings.he.ts (live.openQuestionNumberCta), web/src/features/live/control-page.tsx:70-78]

## Deferred from: code review of 4-6-winner-takeover (2026-08-14)

- A score of exactly 1 renders as "1 נקודות" on **both** surfaces — neither the display nor WhatsApp has a Hebrew singular form for a point. `strings.he.ts`'s `display.winner.scoreSuffix` is `` (score: number) => `${score} נקודות` `` and `messages_he.go`'s `winnerFinalMessage`/`finalResultsMessage` interpolate a bare `strconv.Itoa` into the same suffix, so the two agree — wrongly, in the same way. Reachable whenever a game's scoring rules can award exactly one point (`points_per_correct: 1` with speed bonuses at zero is the direct route). Six other strings in `strings.he.ts` do spell out the Hebrew singular — `שאלה אחת`, `משתתף אחד שיחק`, `תשובה נכונה אחת`, `עלייה של מקום אחד` — and `scoreSuffix`'s own comment explicitly invokes `results.playerCountLabel`'s "spelled-out-singular discipline" before declining to apply it, so the gap is documented at the site rather than accidental. **Deferred at Avraham's call at story 4.6's code review (2026-08-14), reason recorded: patching only the display would manufacture a silent divergence between the projector and the WhatsApp message about the same number — exactly the failure the `noWinner` comment beside it says this file exists to prevent — while patching both is a change under `server/`, which story 4.6's scope boundary forbids outright ("a diff anywhere under `server/` means you went off-spec").** The two must be taken together or not at all. Cosmetic, and the room reads it at twelve metres. Revisit trigger: **the first game authored with a one-point-per-correct rule**, or whenever `messages_he.go`'s score templates are next revised — take both surfaces in one change. [web/src/lib/strings.he.ts (display.winner.scoreSuffix), server/internal/wa/messages_he.go (winnerFinalMessage, finalResultsMessage)]
- A degraded post-commit frame at `finished` is permanent, and the winner takeover never appears at all. This is the **same mechanism** as the 4.5 entry directly above (`snapshotAfterCommit` degrading to `emptySnapshot` after the write has committed), taken to its terminal case, and it should be read as one item with that entry and with the 3.1 discarded-REST-snapshot and 3.7 `emptySnapshot` entries all three name. Both routes into the state — `NextQuestion` past the last question (`engine.go:461`) and `StopGame` (`engine.go:495`) — go through `snapshotAfterCommit`, which on failure broadcasts `emptySnapshot` with `QuestionCount: 0`. The *rendering* half is handled correctly and deliberately by story 4.6's derived requirement 5: `winner-stage.tsx` shows `display.waiting` rather than a plausible no-winner card, and the guard that distinguishes the two was the whole difficulty of that story. What is not handled is recovery, and `finished` is worse than `leaderboard` in the one way that matters: **it is terminal.** `'finished'` appears in `queries/games.sql` only as `FinishGame`'s target and never in any transition's `WHERE`, and the only other broadcasters (`RecordAnswer`, `joinLobby`) are gated to `question_open`/`lobby`. There is no next frame — not a later transition, not an organizer action, not a retry. That degraded snapshot is the last one the game will ever broadcast, so the projector holds the waiting copy for the remainder of the event and the room never sees who won. The shell's `retained` does not rescue it either: a degraded frame is a real snapshot, so it replaces the retained one rather than being ignored. The only recovery is a socket drop, since `ws/handler.go:82` reads a fresh `engine.Snapshot` per connection. Deferred rather than patched because story 4.6's scope boundary forbids any change under `server/` ("a diff anywhere under `server/` means you went off-spec") and because the fix is the architectural one all three related entries already name — consume the 200 response's snapshot, or add a periodic/on-demand refresh — not a stage patch. Note the window is narrow (a pool blip or the 5s detached-snapshot budget expiring, in the one moment of the transition) but the blast radius is the emotional peak of the whole product. Revisit trigger: **the first report of an Audience Display stuck on the waiting copy after a game ended**, or whenever the discarded-REST-snapshot fix is implemented — take all four entries together. [server/internal/game/engine.go (snapshotAfterCommit, emptySnapshot, NextQuestion, StopGame), web/src/features/display/winner-stage.tsx:144-150]
