---
baseline_commit: 34ac8a2
---

# Story 2.1: WhatsApp Webhook Intake and Outbound Dispatch

Status: in-progress

## Story

As the platform operator,
I want verified, idempotent WhatsApp message intake and rate-limited outbound sending,
so that the platform can converse reliably with Participants without losing or duplicating messages.

## Acceptance Criteria

1. **Given** Meta's webhook registration handshake, **when** `GET /webhooks/whatsapp` arrives with the verify token, **then** the challenge is answered correctly (endpoint lives outside `/api`, no session auth).
2. **Given** an inbound `POST /webhooks/whatsapp`, **when** the `X-Hub-Signature-256` HMAC is invalid, **then** the request is rejected and logged; valid signatures are processed.
3. **Given** Meta retries a webhook delivery, **when** the same WhatsApp message ID arrives twice, **then** it is processed exactly once (dedupe by message ID).
4. **Given** outbound messages are queued, **when** the dispatcher sends them, **then** a worker pool with a token-bucket limiter stays under 80 msg/sec, failed sends retry up to 3 times with backoff, and final failures are logged WARN without blocking anything.
5. **Given** any log line involving a Participant, **then** the phone number appears as last-4 only (`phone_last4`), per NFR-4, **and** the local dev workflow (tunnel via cloudflared, Meta test number) is documented in the README.

*Note: requires the Meta WhatsApp Business setup kicked off in Epic 1 — a Meta test number suffices for development. Per the Epic 1 retro (action item 1), Avraham and the dev agent walk through the Meta setup together at the start of this story; the webhook verify handshake succeeding against the test number is that action item's success criterion and part of this story's E2E.*

## Tasks / Subtasks

- [ ] Task 0: Meta setup walkthrough — joint session with Avraham (prerequisite for E2E; retro action item 1) — **BLOCKED: requires Avraham directly** (his Facebook account + Meta console access; see Dev Agent Record)
  - [ ] Create the free Business Portfolio + Meta app (type Business) + WhatsApp product with test number (~30 min; personal FB account already aged since 2026-07-13; no registered business entity needed — README section "Meta WhatsApp Business setup" records the plan)
  - [ ] Collect the four values and set them in `server/.env` (local): `WHATSAPP_ACCESS_TOKEN` (API Setup temporary 24h token now; System User long-lived token before the pilot), `WHATSAPP_PHONE_NUMBER_ID` (API Setup "Phone number ID" — the ID, never the display number), `WHATSAPP_APP_SECRET` (App settings → Basic → App secret), `WHATSAPP_VERIFY_TOKEN` (self-chosen random string, e.g. `openssl rand -hex 16`)
  - [ ] Add Avraham's personal WhatsApp number as a verified test recipient (API Setup allows up to 5)
  - [ ] ⚠️ Set the same four values in Railway Variables **before this story merges to main** — Task 1's placeholder validation will otherwise fail-fast the next production boot (mirror of the 1.2 `SESSION_SECRET` precedent; update the README runbook warning accordingly)
- [x] Task 1: Config hardening — WhatsApp placeholder validation (AC: 2; retro action item 2, closes the deferred-work 1.1 item's WhatsApp half)
  - [x] `server/internal/config/config.go`: after presence checks, validate the four `WHATSAPP_*` values — reject `"dummy"` (the documented `.env.example` placeholder) and any `change-me` prefix, with an error naming the variable and pointing at the README Meta runbook. Follow the `validateSessionSecret` shape; collect all offenders in one error (fix-all-in-one-pass pattern). **Do NOT validate `ANTHROPIC_API_KEY`** — it stays dummy until Epic 3 consumes it (story 3.6); note this in a comment so the retro action item's "(and Anthropic)" half has a recorded home.
  - [x] No length/entropy floor on WhatsApp values: token formats vary (temporary vs System User) and over-constraining locks out legitimate values. Placeholder rejection only.
  - [x] `server/.env.example`: replace the `dummy` WhatsApp values + comment with real-value guidance (where each value lives in the Meta console, per Task 0 mapping); keep `ANTHROPIC_API_KEY=dummy` with an "Epic 3" note. Make explicit (here and in the README): **from this story on, local boots reject dummy WhatsApp values** — real test-number values from Task 0 are required for `make dev` (by design; frontend-only work can still use `npm run dev` alone against a running server or mock nothing — the Vite dev server itself boots fine).
  - [x] `config_test.go`: table additions — each `WHATSAPP_*` var as `dummy`/`change-me…` → boot error naming it; all four wrong → one error naming all; valid values pass; `ANTHROPIC_API_KEY=dummy` still boots.
- [x] Task 2: Dedupe ledger — migration 00006 + store wrapper (AC: 3)
  - [x] `server/migrations/00006_wa_inbound_messages.sql` (next number after 00005 — repo numbering stays offset from the architecture's illustrative list): `wa_inbound_messages` with `wa_message_id TEXT PRIMARY KEY` (Meta `wamid.…` opaque string — TEXT, not UUID), `received_at timestamptz NOT NULL DEFAULT now()`. All columns NOT NULL (the 1.2 sqlc all-NOT-NULL/pgtype-free constraint). Down: `DROP TABLE wa_inbound_messages;`. No retention job — pilot volume is thousands of rows per game; unbounded growth is the accepted posture (revisit only if the table ever matters operationally).
  - [x] `server/internal/store/queries/wa_messages.sql`: `-- name: MarkWaMessageProcessed :execrows` → `INSERT INTO wa_inbound_messages (wa_message_id) VALUES ($1) ON CONFLICT DO NOTHING;` — rows-affected 1 = first delivery, 0 = duplicate. One atomic statement; no read-then-write race.
  - [x] `server/internal/store/wa_messages.go`: `MarkWaMessageProcessed(ctx, waMessageID string) (bool, error)` — true on first delivery; pgx errors wrapped, no pgx sentinels escape `store` (established discipline).
  - [x] Run `sqlc generate`, commit `gen/`; verify no pgtype leak.
- [x] Task 3: `wa` package — webhook intake (AC: 1, 2, 3)
  - [x] New `server/internal/wa/webhook.go` — `NewWebhookHandler(appSecret, verifyToken string, dedupe Deduper, inbound InboundHandler) http.Handler` serving both verbs:
    - **GET (Meta registration handshake)**: `hub.mode == "subscribe"` AND `hub.verify_token` matches → 200 with the raw `hub.challenge` string as `text/plain` body; anything else → 403 empty body. Constant-time token compare (`subtle.ConstantTimeCompare`) — cheap and removes the class.
    - **POST (inbound delivery)**: read the raw body with a 256 KiB `http.MaxBytesReader` cap (Meta payloads are small; cap is hostile-input hygiene) → compute HMAC-SHA256 of the **raw bytes** with the app secret → compare against the `X-Hub-Signature-256` header after stripping the `sha256=` prefix, hex-decoding, and using `hmac.Equal` (constant-time). Invalid/missing/malformed signature → 403 + `slog` WARN `"webhook signature rejected"` with reason (never the body). Signature must be verified **before** any JSON parsing — the raw bytes are the signed message.
    - **After a valid signature, always respond 200** — even on unparseable JSON or unknown payload shapes (log WARN, drop). Meta retries non-200 deliveries with growing backoff for days; an authentic-but-unprocessable payload must not retry-loop. 403 is reserved for signature failures, which by definition are not Meta.
    - Parse the standard envelope (`entry[].changes[].value`): for each `messages[]` entry, extract a normalized `InboundMessage{WaMessageID, From, ProfileName, Type, TextBody, ReceivedAt}` — `From` = `messages[].from` (E.164 digits, no `+`), `ProfileName` from the parallel `contacts[]` entry matched by `wa_id` (empty when absent), `Type` = `messages[].type` verbatim (`text`, `image`, `sticker`, …), `TextBody` = `text.body` for type `text` else empty (2.2's Universal Reply handles every class), `ReceivedAt` = server `time.Now().UTC()` (FR-7's authoritative receipt clock is ours, never Meta's `timestamp`).
    - Per message: `dedupe.MarkWaMessageProcessed(wa_message_id)` **before** invoking `inbound` — duplicate → skip with INFO `"duplicate webhook delivery skipped"` + `wa_message_id`; first delivery → `inbound.Handle(ctx, msg)` then INFO `"inbound message accepted"` with `wa_message_id`, `type`, `phone_last4`. Handler runs synchronously inside the request (parse + one INSERT + a stub call = milliseconds; well inside Meta's response window) — async handoff is not needed until a heavier consumer exists.
    - `statuses[]` changes (delivery receipts) and non-`messages` fields: acknowledged 200, silently ignored — no log spam at INFO (a healthy game generates thousands of them).
  - [x] Interfaces in the same file (consumer-defined, the `httpapi.GameStore` precedent): `Deduper` (`MarkWaMessageProcessed(ctx, string) (bool, error)`) and `InboundHandler` (`Handle(ctx context.Context, msg InboundMessage)` — no error return: intake never fails on handler behavior; handlers own their logging). Story 2.2 replaces the wired stub with the real parser; 2.1 wires a no-op logger implementation in `main.go`.
    - Dedupe store failure (DB down): log slog Error? No — WARN + still invoke the handler? **Decision: WARN and process anyway** (availability over strict once-only: a DB blip must not silence the chat — SM-C2/NFR-2 posture; duplicate risk during a blip is accepted and logged). Record `dedupe_degraded: true` on the WARN line.
  - [x] `webhook_test.go` (stdlib `testing` + `httptest`, stub Deduper/InboundHandler grown in-test — the 1.x stub style): GET happy path echoes challenge exactly · GET wrong/missing token or mode → 403 · POST valid signature + single text message → 200, handler receives normalized fields (incl. `ReceivedAt` set, `From`, profile name, body), dedupe called with the wamid · invalid signature (wrong secret, tampered body, missing header, malformed `sha256=` prefix, non-hex) → 403, handler NOT called, dedupe NOT called · duplicate (stub returns false) → 200, handler NOT called · statuses-only payload → 200, no handler call, no dedupe call · unparseable JSON with valid signature → 200 · sticker/image message → handler called with empty `TextBody` and correct `Type` · multi-message batch → handler called per message in order · oversized body → 4xx without handler call · phone appears in NO log output except as last-4 (assert via a captured slog handler).
- [x] Task 4: `wa` package — outbound client + dispatcher (AC: 4, 5)
  - [x] `server/internal/wa/client.go` — minimal Cloud API client: `NewClient(accessToken, phoneNumberID string, opts…)` with `SendText(ctx, to, body string) error`. POST `{baseURL}/{phoneNumberID}/messages` where `baseURL` defaults to `https://graph.facebook.com/v25.0` (Graph API v25.0 — current as of 2026-07, released 2026-02; keep as one overridable constant/option for tests and future bumps). Headers `Authorization: Bearer …`, `Content-Type: application/json`. Body: `{"messaging_product":"whatsapp","recipient_type":"individual","to":"<digits>","type":"text","text":{"preview_url":false,"body":"…"}}`. Per-request timeout ~10s via the ctx; non-2xx → error carrying status + Meta's error body (`error.message`, `error.code`) for the WARN logs — never logged with the full recipient number.
  - [x] `server/internal/wa/redact.go` — `PhoneLast4(msisdn string) string` (last 4 digits, `"****"` when shorter); **every** log attribute involving a phone number is `phone_last4` = this helper. Exported: `httpapi`/`game` will need it in later stories.
  - [x] `server/internal/wa/dispatch.go` — `NewDispatcher(client SenderClient, …)` with `Enqueue(to, body string)` and `Run(ctx)`:
    - Buffered channel queue (capacity ~1,000 — a full 80-participant burst plus acks with headroom); **`Enqueue` never blocks**: queue full → drop + WARN `"outbound queue full, message dropped"` + `phone_last4` (the game loop is never blocked — NFR-2; at pilot scale this fires only under pathology).
    - Worker pool (8 goroutines) all draining the queue through **one shared token-bucket limiter** at 70 msg/sec (`golang.org/x/time/rate`, `rate.NewLimiter(70, 70)`, `limiter.Wait(ctx)` before each send attempt) — the limiter is global across workers. 70, not 80: the AC says "stays **under** 80 msg/sec" and inbound traffic shares the same per-number throughput budget; 12.5% headroom still clears an 80-participant burst in ~1.2s, far inside FR-4's 5s/95% target. One named constant. `SenderClient` is a one-method interface (`SendText`) so tests inject a stub.
    - Per-message retry: up to 3 attempts total, backoff 1s → 2s → 4s (context-aware sleep); retry WARN `"send retry"` with attempt number; final failure WARN `"send failed permanently"` with the Meta error + `phone_last4` — **never ERROR** (NFR-8: degradations are WARN; a healthy run has zero ERRORs) and never propagated to the caller (fire-and-forget by design; FR-4/NFR-2 "failures logged, never block the game loop").
    - Shutdown: `Run(ctx)` returns when ctx is cancelled; in-flight sends finish (their own timeout bounds them), queued-but-unsent messages are dropped with one summary WARN. Accepted pilot posture — a redeploy mid-game already interrupts the room.
  - [x] `client_test.go` (httptest server): URL contains phone-number-ID and `/messages` · bearer header + exact JSON body shape · 2xx → nil · 4xx/5xx with Meta error body → error containing code, and the error string free of the full recipient number.
  - [x] `dispatch_test.go` (deterministic — inject the limiter's clock is overkill; assert behavior, not timing): happy path delivers all enqueued messages to the stub · failing stub → exactly 3 attempts then permanent-failure WARN (captured slog), later messages still delivered · queue-full → drop WARN, `Enqueue` returns immediately · ctx cancel stops workers. Keep backoff injectable (a `sleep func` field) so the retry test runs in microseconds.
- [x] Task 5: Wiring — router + main (AC: 1, 2)
  - [x] `server/internal/httpapi/router.go`: `NewRouter` gains a `webhook http.Handler` parameter, mounted **outside** `/api`: `r.Mount("/webhooks/whatsapp", webhook)` — no session middleware on that branch (HMAC is its auth); nil webhook → branch absent (keeps existing router tests compiling with one added nil arg). `isReservedPath` already reserves `/webhooks` — no change there.
  - [x] `server/cmd/server/main.go`: construct `wa.NewClient` (from cfg) → `wa.NewDispatcher` (goroutine: `go dispatcher.Run(ctx)` on the existing signal-aware ctx) → `wa.NewWebhookHandler(cfg.WhatsAppAppSecret, cfg.WhatsAppVerifyToken, st, stubInbound)` → pass to `NewRouter`. The 2.1 `stubInbound` logs INFO `"inbound message received (no handler yet)"` with `wa_message_id`/`type`/`phone_last4` — story 2.2 replaces it. `st` (store) satisfies `Deduper` directly.
  - [x] `router_test.go`: webhook branch reachable without a session (GET handshake 403 with wrong token — proves mounting + no auth middleware); `/api` behavior unchanged.
- [x] Task 6: README — Meta runbook + local webhook dev workflow (AC: 5)
  - [x] Extend the "Meta WhatsApp Business setup" section from a deferred-decision note into the actual runbook (human steps): Business Portfolio → Meta app (Business type) → WhatsApp product → test number; the four env values and exactly where each lives in the console (Task 0 mapping); adding verified test recipients (max 5); webhook subscription: App Dashboard → WhatsApp → Configuration → Callback URL + Verify Token → "Verify and save" (fires our GET handshake) → subscribe to the **`messages`** webhook field; temporary token expires ~24h — regenerate each dev session, System User token before the pilot.
  - [x] Local webhook dev workflow: `cloudflared tunnel --url http://localhost:8080` (quick tunnel, no account) → use the printed `https://…trycloudflare.com/webhooks/whatsapp` as the callback URL; tunnel targets the **Go server on :8080 directly** (not Vite — the proxy is for browser dev, not Meta); quick-tunnel URL changes per run → re-verify the webhook each session (30 seconds); cloudflared install note for Windows (`winget install Cloudflare.cloudflared`).
  - [x] Update the Railway runbook: ⚠️ set real `WHATSAPP_*` values in Railway Variables **before merging this story** (placeholder validation fails the boot otherwise — same pattern as the 1.2 `SESSION_SECRET` warning, which this replaces as the active warning); production callback URL = the Railway domain + `/webhooks/whatsapp` (point Meta at it when moving from dev tunnel to deployed testing; one app has one callback URL — dev tunnel and production alternate by re-verifying, acceptable for the pilot).
- [ ] Task 7: Quality gates + E2E verification (all ACs)
  - [x] All local gates: `gofmt` clean, `go vet ./...`, `go test ./...` (all existing 67 httpapi + 9 auth + 4 store + config suites stay green — regression guard), `sqlc generate` (empty diff), `eslint`, `tsc -b --noEmit`, `npm run build` (frontend untouched but the gate runs), filter-safety greps (no new asset references — backend-only story).
  - [x] Local API E2E (Python script per the 1.5 pattern — **no Hebrew through Git Bash curl**, and HMAC computation needs a real client anyway; ASCII payloads + `hmac`/`hashlib` stdlib): boot against Docker Postgres (migration 00006 applies on existing AND fresh DB; `goose down`/`up` round-trips) → GET handshake correct token → 200 challenge echo; wrong → 403 → POST valid signature/text message → 200 + INFO accepted + row in `wa_inbound_messages` → same wamid again → 200 + INFO duplicate-skipped, handler count unchanged → tampered body → 403 WARN → statuses payload → 200 silent → sticker-type message → accepted with empty body → boot with `WHATSAPP_ACCESS_TOKEN=dummy` → fail-fast naming the variable → zero ERROR log lines throughout (NFR-8); no full phone number anywhere in captured logs (grep the log output for the test number, expect only last-4).
  - [ ] Live Meta E2E (with Avraham, closes retro action item 1) — **BLOCKED: requires Avraham directly**: cloudflared tunnel up → Meta "Verify and save" succeeds against the running server (AC-1 against real Meta) → send a WhatsApp message from the verified test recipient to the test number → webhook arrives, signature passes, INFO accepted with `phone_last4` → dispatcher smoke: enqueue a text to the verified recipient (one-off `cmd` snippet or temporary test hook — NOT app code; delete after) → message arrives in WhatsApp → Railway variables set → (after merge) production boot green.

## Dev Notes

### What this story is — and is not

The WhatsApp transport layer, both directions, with nothing game-shaped in it: webhook verify handshake, HMAC-verified + deduped intake handing normalized messages to a stub, and a rate-limited fire-and-forget outbound dispatcher. It does **not** build: message parsing/routing (`inbound.go` — 2.2), any outbound copy or `messages_he.go` (2.2 creates it; this story sends no user-facing text from app code), JOIN/registration (2.4), `participants` table (2.3), the `game` package (2.3 — **must still not exist** after this story), WebSocket work (2.3), any frontend change (zero web/ diffs). Zero new npm deps; one new Go dep: `golang.org/x/time` (see design decisions).

### Design decisions locked for this story (rationale recorded — do not relitigate)

- **DB-backed dedupe, not in-memory**: Meta retries non-200/timeout deliveries for days; an in-memory set dies on every redeploy mid-retry-window. One tiny table + `INSERT … ON CONFLICT DO NOTHING` (atomic, race-free under concurrent webhook deliveries) is the SM-4-grade answer. `:execrows` 0/1 distinguishes duplicate from first delivery without a read.
- **`wa_inbound_messages` table name**: `wa_` prefix namespaces the transport tables (the glossary covers game domain nouns, not transport bookkeeping); plural snake_case per convention. `wa_message_id TEXT PRIMARY KEY` — Meta wamids are opaque strings, not UUIDs; TEXT PK is correct and the natural key IS the dedupe key.
- **Dedupe-before-handler, 200-always-after-valid-signature**: at-most-once handler invocation, availability over redelivery. If the handler ever fails after dedupe recorded the wamid, Meta already got its 200 and will not redeliver — accepted: from 2.2 onward every reply is best-effort by FR/NFR design (degrade + log WARN, never block). The alternative (5xx to force Meta redelivery) trades a silent-chat risk for a retry-storm risk and violates the "always answer 200 fast" guidance.
- **Dedupe store failure → WARN + process anyway**: a DB blip must not silence the chat (SM-C2 posture: reply guarantees outrank strict once-only). Logged with `dedupe_degraded: true` so NFR-8 observability holds.
- **Consumer-defined interfaces (`Deduper`, `InboundHandler`, `SenderClient`) in `wa`**: exactly the `httpapi.GameStore` precedent — keeps `wa` free of `store` imports and makes every seam stub-testable. Dependency direction stays law: `wa` imports nothing of `httpapi`/`ws`; `main.go` is the composition root; `NewRouter` takes the webhook as an opaque `http.Handler` (no `httpapi` → `wa` import either).
- **Synchronous handler inside the webhook request**: parse + one INSERT + stub call is milliseconds — async machinery (queues, goroutine fan-out) buys nothing until a heavy consumer exists and would complicate the persist-before-ack chain that story 3.3 builds on this seam. Revisit only if 3.x measurements demand it.
- **`golang.org/x/time/rate` as the token bucket** — the single new Go dependency: first-party Go module, zero transitive deps (`golang.org/x/*` already in go.sum via crypto/sync), and it IS the canonical token-bucket the architecture names. A hand-rolled limiter is more code to review for zero benefit. `rate.NewLimiter(80, 80)` shared across all workers = global ≤80 msg/s regardless of pool size.
- **Fire-and-forget dispatcher (`Enqueue` returns nothing)**: FR-4/NFR-2 — send failures are logged WARN and never propagate; callers (2.2 replies, 3.2 bursts) must never block or branch on delivery. Queue-full drops rather than blocks for the same reason (capacity 1,000 ≫ any pilot burst).
- **Graph API v25.0 pinned as an overridable base-URL constant**: current version (released 2026-02, ~2-year support window); one constant to bump, tests inject httptest URLs through the same knob.
- **403 for signature failures, 200 for everything authentic**: signature failures are by definition not Meta (wrong secret/forgery/tamper) — rejecting hard is correct and cheap; authentic payloads never get a non-200 (retry-storm avoidance), including unparseable ones (log WARN, drop).
- **No `messages_he.go` in this story**: 2.1 sends no user-facing copy from app code (the live-E2E smoke text is test tooling, deleted after). Creating the file empty would just invite a literal to sneak in outside 2.2's canonical-copy task.
- **Anthropic placeholder validation deliberately deferred to Epic 3**: validating an unconsumed secret breaks boot for zero safety gain — the retro action item's WhatsApp half lands here, the Anthropic half lands with story 3.6 (comment in `config.go` records this).

### Previous story intelligence (1.5 + Epic 1 retro — established patterns, follow or rework)

- **Infrastructure stories carry heavy hardening load** (retro insight #1: 1.1's review had 15 findings; 2.1 is the same shape). Budget review attention for: raw-body handling vs chi middleware ordering, HMAC edge cases (missing header, non-hex, prefix variants), goroutine lifecycle/leaks in the dispatcher, shutdown ordering (dispatcher vs http server), log-redaction misses.
- **Store discipline**: `store` is the only pgx importer; wrappers translate pgx errors; params primitive; `writeStoreError` exists for httpapi handlers (not needed by `wa` — the webhook never speaks the JSON error envelope; it speaks Meta's dialect: 200/403 with empty bodies).
- **sqlc mechanics**: schema dir is `migrations/` (sqlc parses migration SQL); uuid→string/timestamptz→time.Time overrides valid **only because every column is NOT NULL** — `wa_inbound_messages` keeps that invariant; v1.31 pinned (`go install …sqlc@v1.31.1`); commit `gen/`; CI diff-gate includes untracked files.
- **Migrations**: goose v3 Provider API, embedded FS — drop `00006_wa_inbound_messages.sql` next to the others, the glob picks it up; verify fresh AND existing dev DB plus down/up round-trip (1.3–1.5 precedent). ASCII-only content this time — no BOM concerns.
- **Test conventions**: stdlib `testing`, co-located `_test.go`, stubs grown in-test (no mock framework). New for this story: capturing slog output in tests — use `slog.New(slog.NewTextHandler(&buf, …))` injected via the constructor (give `NewWebhookHandler`/`NewDispatcher` an optional `*slog.Logger`, defaulting to `slog.Default()`) so redaction and WARN assertions are deterministic.
- **Handler conventions that DO carry over to the webhook**: bound every DB call with `context.WithTimeout(r.Context(), 5*time.Second)` (the dedupe INSERT). **Fact-checked: no panic-recovery middleware exists anywhere in the repo today** (the architecture's "panics recovered by chi middleware" is unimplemented debt from 1.1; `net/http` itself recovers handler panics per-connection so the process never exits). Retrofitting `middleware.Recoverer` globally is NOT this story's scope — instead the webhook handler must be panic-free on hostile input (the Task 3 malformed-payload tests are that guarantee), and a crash between dedupe-insert and the 200 loses that one message to the duplicate-skip on Meta's retry — same accepted at-most-once class as a handler failure (documented above).
- **Windows/toolchain notes (permanent tax, all documented)**: Go 1.26.5 via the `go1.26.5` wrapper (system go is 1.22); `-buildvcs=false` may be needed locally; `make` needs Git Bash; transient `VirtualAlloc errno=1455` resolves on re-run; **Hebrew must never pass through Git Bash `curl -d`** — E2E via Python stdlib (1.5's proven pattern; HMAC needs scripted requests anyway); all new files lowercase; PowerShell writes UTF-16 by default — files via the Write tool only.
- **E2E leftovers**: dev DB has test organizers `e2e-org-a`/`e2e-org-b` — irrelevant here (no session surface in this story), clean up any new rows created.
- **CI note**: no CI change needed — `go test ./...` picks up the new package; the filter-safety grep is untouched by backend code.

### Architecture guardrails (violations = rework)

- **Canonical files** (architecture tree): `wa/client.go`, `wa/webhook.go`, `wa/dispatch.go` — exactly these names. `inbound.go` and `messages_he.go` belong to 2.2; creating them here is a scope violation. `redact.go` is a documented addition to the tree (helper, no canonical home named).
- **Dependency direction is law**: `wa` imports stdlib + `x/time/rate` only (its dependencies arrive as interfaces); `httpapi` does not import `wa`; `main.go` composes. `store` remains the only pgx importer.
- **Route**: `/webhooks/whatsapp` outside `/api`, no session auth, HMAC instead (NFR-7). `isReservedPath` already guards SPA fallback for `/webhooks` — do not duplicate.
- **Logging (NFR-8)**: slog JSON, canonical keys — `wa_message_id`, `phone_last4`, `event` context; INFO = lifecycle (accepted, duplicate-skipped), WARN = every degradation (signature reject, retry, permanent send failure, queue drop, dedupe degraded), ERROR = never on a healthy run. Grep-level rule: the only phone-number log key that may exist is `phone_last4`.
- **PII (NFR-4)**: full MSISDNs live in memory and (from 2.3) in `participants` — never in logs, never in error strings returned upward (client errors must be scrubbed before wrapping).
- **Config**: env vars only; fail-fast with all offenders in one message (existing pattern); no new env vars introduced (the four `WHATSAPP_*` already exist).
- **Glossary**: transport-layer names (`wa_message_id`, `InboundMessage`) are new vocabulary, consistent with the architecture's own usage (`wa_message_id` is its canonical log key); game-domain nouns (`Participant`, `Game`) do not appear in this story's identifiers because no game logic exists here.

### Latest tech intelligence (verified 2026-07-19)

- **Graph API v25.0** is current (released 2026-02-18; versions run ~2 years; v26.0 expected ~Sep 2026). Base URL `https://graph.facebook.com/v25.0`. Send-text endpoint and payload shape in Task 4 verified current.
- **Webhook signature**: HMAC-SHA256 over the **raw request body bytes** with the **App Secret** (not the verify token), header `X-Hub-Signature-256: sha256=<hex>`; timing-safe compare mandatory. Any body mutation before verification breaks it — read raw, verify, then parse.
- **Webhook retries**: Meta retries non-200 and timed-out deliveries with growing backoff (up to ~7 days) — the dedupe table and 200-always posture exist because of this.
- **Throughput**: 80 msg/sec default per business phone number (Cloud API), upgradeable to 1,000 when eligible — architecture's numbers confirmed. **New intel — per-recipient pair rate limit**: sustained sends to the *same* recipient are throttled by Meta (order of ~1 msg/6s sustained); irrelevant for 2.1's smoke test, but 2.2's Universal Reply should know a hostile rapid-fire sender may see Meta-side throttling of our replies (accept + WARN; no client-side per-recipient limiter in the pilot).
- **Test numbers**: free, per-app, up to 5 verified recipients, temporary access token expires ~24h (regenerate per dev session); System User token (Business Settings → System users → generate token with `whatsapp_business_messaging` permission) is the long-lived path before the pilot.
- **Service-window economics** (re-verified 2026-07-13, README): service replies free AND exempt from tier messaging limits — the ≈₪0 assumption holds; nothing in 2.1 sends outside a service window (smoke test replies to a message the recipient initiated… note: the dispatcher smoke send is business-initiated unless the recipient messages first — have the test recipient send any message to the test number right before the smoke send, which also exercises intake).
- **cloudflared quick tunnel**: `cloudflared tunnel --url http://localhost:8080`, no account, ephemeral URL per run (re-verify webhook each session); `winget install Cloudflare.cloudflared` on Windows.

### Project Structure Notes

New files: `server/migrations/00006_wa_inbound_messages.sql` · `server/internal/store/queries/wa_messages.sql` · `server/internal/store/wa_messages.go` · `server/internal/wa/webhook.go` (+ `webhook_test.go`) · `server/internal/wa/client.go` (+ `client_test.go`) · `server/internal/wa/dispatch.go` (+ `dispatch_test.go`) · `server/internal/wa/redact.go`.

Modified: `server/internal/store/gen/` (regenerated) · `server/internal/config/config.go` (+ `config_test.go`) · `server/internal/httpapi/router.go` (+ `router_test.go` — one new param at existing call sites) · `server/cmd/server/main.go` (client/dispatcher/webhook wiring + stub inbound) · `server/go.mod`/`go.sum` (`golang.org/x/time`) · `server/.env.example` · `README.md`.

**Variances from the canonical architecture tree (documented):** (1) migration numbering continues the repo offset (00006 here; the architecture's illustrative list had no dedupe migration at all — the table itself is an implementation detail of "dedupe by message ID"). (2) `redact.go` added to `wa/` (helper with no canonical home). (3) `NewRouter` signature grows a `webhook http.Handler` param (composition-root pattern, keeps import direction clean). Frontend untouched; `deps.go` pin file untouched (`x/time` is imported by real code immediately).

### Testing standards

Go stdlib `testing`, co-located, in-test stubs, injected `*slog.Logger` for log assertions (new pattern this story — keep it; later stories assert WARN/redaction the same way). Unit coverage per Task 3/4 lists; store wrapper is a thin pass-through covered by E2E against real Postgres (1.1–1.5 posture: no real-DB tests in CI). Deterministic tests only — backoff via injectable sleep, no `time.Sleep` assertions, no live-network tests in `go test` (the Meta live pass is manual E2E). Frontend: no changes, gates still run.

### References

- [Source: _bmad-output/planning-artifacts/epics.md#Story-2.1] — story + ACs (verbatim), Epic 2 context, Meta-test-number note
- [Source: _bmad-output/planning-artifacts/epics.md#Additional-Requirements] — webhook HMAC + dedupe mandate, dispatcher worker-pool/token-bucket/retry spec, env-var list, `phone_last4` (NFR-4), NFR-7/NFR-8
- [Source: _bmad-output/planning-artifacts/architecture.md#API-&-Communication-Patterns] — Meta Cloud API direct, 80 msg/sec, dedupe-by-message-ID mandate, retry/backoff, service-window cost model
- [Source: _bmad-output/planning-artifacts/architecture.md#Architectural-Boundaries] — `/webhooks/whatsapp` HMAC-authenticated branch, dependency direction, `store` pgx exclusivity
- [Source: _bmad-output/planning-artifacts/architecture.md#Complete-Project-Directory-Structure] — `wa/client.go`, `wa/webhook.go`, `wa/dispatch.go` placement; `inbound.go`/`messages_he.go` deferred to 2.2
- [Source: _bmad-output/planning-artifacts/architecture.md#Implementation-Patterns-&-Consistency-Rules] — slog canonical keys (`wa_message_id`, `phone_last4`), WARN-degradation rule, retry ownership
- [Source: _bmad-output/implementation-artifacts/epic-1-retro-2026-07-17.md] — action items 1 (joint Meta walkthrough, success = verify handshake) and 2 (placeholder validation in config), insight #1 (infrastructure review budget), Windows-tax posture
- [Source: _bmad-output/implementation-artifacts/deferred-work.md] — 1.1 item: WhatsApp placeholder validation "remains for Story 2.1" (closed by Task 1)
- [Source: _bmad-output/implementation-artifacts/1-5-question-bank-package-import.md] — store/sqlc/test/Windows patterns carried forward
- [Source: README.md#Meta-WhatsApp-Business-setup] — setup plan + pricing re-verification (2026-07-13), aged FB account
- [Source: server/internal/config/config.go] — `validateSessionSecret` shape to mirror; `require()` presence pattern
- [Source: server/internal/httpapi/router.go] — mount point, `isReservedPath` already covers `/webhooks`
- Web (2026-07-19): Graph API v25.0 current ([Meta versions](https://developers.facebook.com/docs/graph-api/changelog/versions/), [v25 announcement](https://developers.facebook.com/blog/post/2026/02/18/introducing-graph-api-v25-and-marketing-api-v25/)); signature verification raw-body + timing-safe ([Meta webhook endpoint doc](https://developers.facebook.com/documentation/business-messaging/whatsapp/webhooks/create-webhook-endpoint/), [Hookdeck guide](https://hookdeck.com/webhooks/platforms/guide-to-whatsapp-webhooks-features-and-best-practices)); throughput + pair rate limit ([Fyno guide](https://www.fyno.io/blog/whatsapp-rate-limits-for-developers-a-guide-to-smooth-sailing-clycvmek2006zuj1oof8uiktv), [wuseller 2026](https://www.wuseller.com/whatsapp-business-knowledge-hub/scale-whatsapp-cloud-api-master-throughput-limits-upgrades-2026/))

## Dev Agent Record

### Agent Model Used

Claude Fable 5 (claude-fable-5), via the bmad-dev-story workflow, 2026-07-20.

### Debug Log References

- Local API E2E script (ASCII, stdlib `hmac`/`hashlib`, 1.5 pattern): `e2e_2_1.py` run in the session scratchpad, not committed (temporary, deleted-in-spirit per the 1.5 precedent — the file lives outside the repo working tree).
- Server log capture during E2E: confirmed `"inbound message accepted"` (first delivery), `"duplicate webhook delivery skipped"` (retry), `"webhook signature rejected" reason=mismatch` (tampered body), zero `"level":"ERROR"` lines, zero occurrences of the full test phone number (only `phone_last4` present).
- Migration 00006 verified three ways: fresh scratch DB (`wa21_fresh_test`, dropped after) shows `"migrations applied","count":6`; existing dev DB already had it applied; `goose down`/`up` round-trip via `go run github.com/pressly/goose/v3/cmd/goose@v3.27.2` confirmed the table drops and recreates cleanly.
- Fail-fast boot confirmed: `WHATSAPP_ACCESS_TOKEN=dummy` → exit 1, single ERROR line naming the variable and pointing at the README runbook (this ERROR is the expected/correct boot-failure log, not a "healthy run" violation of NFR-8).

### Completion Notes List

**Tasks 1–6 and the first two subtasks of Task 7 are complete and verified. Task 0 and Task 7's "Live Meta E2E" subtask are BLOCKED — they require Avraham directly** (his Facebook account, phone, and hands-on time in Meta's console; nothing an agent can do on his behalf). Story left at **Status: in-progress** rather than "review" per the workflow's own completion gate (Step 9: "if any task is incomplete, HALT — complete remaining tasks before marking ready for review").

- **Task 1** — `config.go` gained `validateWhatsAppValues`, rejecting `dummy`/`change-me*` for the four `WHATSAPP_*` vars in one collected error (mirrors `validateSessionSecret`); `ANTHROPIC_API_KEY` deliberately excluded (Epic 3). 6 new table-driven tests, all passing. `.env.example` rewritten with per-variable Meta-console guidance.
- **Task 2** — migration `00006_wa_inbound_messages.sql` (TEXT PK, no surrogate id); `wa_messages.sql` (`:execrows` ON CONFLICT DO NOTHING); `store.MarkWaMessageProcessed` thin wrapper (no store-level unit test — matches the existing "thin wrapper, E2E-covered" convention for DB-touching store methods; verified against real Postgres in Task 7). `sqlc generate` clean, no pgtype leak (confirmed `string`/`time.Time` only in the generated model).
- **Task 3** — `wa/webhook.go` + `wa/redact.go` (pulled forward from Task 4 since Task 3's own logging needs `PhoneLast4`). 13 tests covering every listed scenario plus one not explicitly listed but implied by the Dev Notes design decision: dedupe-store-failure → WARN `dedupe_degraded:true` + process-anyway.
- **Task 4** — `wa/client.go` (Graph API v25.0, functional `ClientOption`s for base URL/http.Client injection) and `wa/dispatch.go` (8-worker pool, shared `rate.NewLimiter(70,70)`, injectable `sleep` field for deterministic retry tests). **Documented deviation**: the Dev Notes narrative says "backoff 1s → 2s → 4s" (3 values) but both the task bullet ("up to 3 attempts total") and the test-list bullet ("exactly 3 attempts") pin 3 total attempts, which has only 2 gaps — implemented `dispatchBackoff = {1s, 2s}` and dropped the unreachable 4s slot; a comment in `dispatch.go` records the reasoning. Concurrency-safe test logging required a mutex-wrapped `syncBuffer` (a plain `bytes.Buffer` would race between worker-goroutine writes and the polling test goroutine — `go test -race` isn't available in this environment because cgo/gcc isn't installed, so this was reasoned through rather than tool-verified; the fix is standard and low-risk).
- **Task 5** — `router.go`'s `NewRouter` gained a trailing `webhook http.Handler` param (nil omits the branch); ~24 existing test call sites updated via a verified-safe scripted append (`, nil)`) after confirming every call site's `NewRouter(...)` ended exactly at end-of-line. `main.go` wires `wa.NewClient` → `wa.NewDispatcher` (started via `go dispatcher.Run(ctx)` on the existing shutdown-aware ctx) → `wa.NewWebhookHandler` with a `stubInboundHandler` (logs only; story 2.2 replaces it). 3 new router tests confirm the webhook branch is reachable with no session cookie, a nil webhook still 404s cleanly, and `/api` behavior is unchanged.
- **Task 6** — README "Meta WhatsApp Business setup" rewritten from a deferred-decision note into a 6-step runbook (portfolio → app → WhatsApp product → test recipients → four env values → webhook subscription), a new "Local webhook development" section (cloudflared quick tunnel), and an added Railway-runbook warning mirroring the 1.2 `SESSION_SECRET` pattern. Also folded in a network-filter workaround note (Meta dashboard screens need an unfiltered connection such as a mobile hotspot; the tunnel and local server themselves are unaffected) — this is an operational fact already established in project memory from Avraham's own 2026-07-19 testing, included here because it's exactly what AC-5 asks the README to document accurately, not new scope. Also corrected two now-stale README passages (`Setup` step 1's "dummy is fine" claim, and the Railway runbook's "five secrets, dummy acceptable" line) that this story's placeholder validation made incorrect.
- **Task 7 (partial)** — all local gates green (gofmt, vet, `go test ./...` — 0 regressions across auth/config/httpapi/store/wa, sqlc diff-check clean, eslint, tsc, frontend build, both filter-safety greps). Local API E2E (Python/stdlib, ASCII payloads) against the Docker dev Postgres confirmed every AC-1/2/3 scenario end to end: GET handshake (correct token → exact challenge echo; wrong token → 403), POST valid signature/text → 200 + accepted + DB row, duplicate wamid → 200 + skipped (no second handler call), tampered body → 403 + WARN, statuses-only → 200 silent, sticker message → 200 accepted with empty body, fail-fast boot on `WHATSAPP_ACCESS_TOKEN=dummy`, zero ERROR log lines, zero full-phone-number occurrences in logs. Migration 00006 verified on both a fresh scratch DB and the existing dev DB, plus a `goose down`/`up` round-trip. E2E test rows cleaned from `wa_inbound_messages` afterward. **Not run**: the "Live Meta E2E" subtask (real Meta test number, real WhatsApp message, dispatcher smoke to a live recipient) — blocked on Task 0.
- **Local-dev-only env values**: `server/.env`'s `WHATSAPP_*` values were changed from `dummy` to non-Meta local placeholders (random hex strings) purely so the server boots locally under the new Task 1 validation; these are clearly commented as not-real-Meta-credentials and must be replaced with Task 0's real values before any live Meta interaction.

**What Avraham needs to do to unblock the rest of this story:**
1. Complete Task 0 (README "Meta WhatsApp Business setup" is now the full runbook) — Business Portfolio, app, WhatsApp product, test number, register himself as a verified test recipient, produce the four real env values, put them in `server/.env` and Railway Variables.
2. Run the "Live Meta E2E" subtask of Task 7 with the dev agent: cloudflared tunnel, Meta "Verify and save", send a real WhatsApp message, confirm it's accepted with `phone_last4` in the logs, and a dispatcher smoke send arrives on his phone.
3. Once both are done, re-run `bmad-dev-story` on this story to check off the remaining boxes and move it to "review".

### File List

**New:**
- `server/migrations/00006_wa_inbound_messages.sql`
- `server/internal/store/queries/wa_messages.sql`
- `server/internal/store/wa_messages.go`
- `server/internal/store/gen/wa_messages.sql.go` (sqlc-generated)
- `server/internal/wa/webhook.go`
- `server/internal/wa/webhook_test.go`
- `server/internal/wa/client.go`
- `server/internal/wa/client_test.go`
- `server/internal/wa/dispatch.go`
- `server/internal/wa/dispatch_test.go`
- `server/internal/wa/redact.go`

**Modified:**
- `server/internal/store/gen/models.go` (sqlc-generated `WaInboundMessage`)
- `server/internal/config/config.go`
- `server/internal/config/config_test.go`
- `server/internal/httpapi/router.go`
- `server/internal/httpapi/router_test.go`
- `server/internal/httpapi/games_test.go` (added trailing `nil` webhook arg to `NewRouter` calls)
- `server/internal/httpapi/packages_test.go` (same)
- `server/cmd/server/main.go`
- `server/go.mod` / `server/go.sum` (`golang.org/x/time`)
- `server/.env.example`
- `server/.env` (local-only, gitignored — not part of the committed diff)
- `README.md`

## Change Log

- 2026-07-19: Story created by create-story workflow — ultimate context engine analysis completed (epics 2.1 + Epic 2 notes, PRD §4.1/NFR-4/7/8, architecture WhatsApp integration + patterns + boundaries, Epic 1 retro action items 1–2, deferred-work WhatsApp-validation item, 1.5 story intelligence, live codebase read: config.go, router.go, main.go, errors.go, middleware.go, go.mod, migrations list, .env.example, Makefile, sqlc.yaml, README; web research: Graph API v25.0, HMAC raw-body verification, 80 msg/s + pair rate limit, test-number/token mechanics). Status: ready-for-dev.
- 2026-07-20: Tasks 1–6 implemented and verified (config validation, dedupe ledger, webhook intake, outbound client + dispatcher, router/main wiring, README runbook); Task 7's local gates and local API E2E complete. Task 0 and Task 7's Live Meta E2E subtask left unchecked — both require Avraham directly. Status left at in-progress (not review) pending his participation.
